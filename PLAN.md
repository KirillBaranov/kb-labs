PIPELINE_STATUS: NEEDS_IMPLEMENTATION

## Summary
A complete implementation of `kb config show` (including the required secret-redaction fix) already exists on branch `issue-257-auto` (13 commits ahead of `main`), but is **not present on the current branch/HEAD** and has two test gaps: no same-root (`sameLocation: true`) e2e case, and no e2e assertion that real secrets are actually redacted end-to-end. Work needed is: merge/rebase that branch onto current work, then close the two test gaps.

## Root cause / context
`core/runtime/src/config-loader.ts`'s `loadPlatformConfig()` already returns the merged config plus `sources.fields`, `sources.ignoredProjectFields`, and `sameLocation` exactly as the issue describes. On `issue-257-auto`, `cli/commands/src/commands/system/config/show.ts` implements `configShowCommand` (registered as `config show`), rendering both `--json` and table output by calling `loadPlatformConfig()` directly — no merge-logic duplication. It also implements the required redaction: a `SENSITIVE_FIELD_PATTERN = /key|secret|token|password|jwt|credential/i` and a recursive `redactValue()`/`redactConfig()` applied to both output modes, including "ignored" rows. Unit tests (`cli/commands/src/__tests__/config-show.test.ts`, 8 cases) cover registration, provenance rows, ignored-field display, JSON shape, and redaction in both text and JSON. The Go e2e suite (`tools/kb-create/e2e/e2e_test.go`) only covers the split-root case (`sameLocation=false`); it doesn't cover same-root/monorepo mode, and never asserts that a real secret value is actually redacted through the full installed-CLI path — only mocked unit tests prove redaction.

## Implementation steps
1. Merge or rebase branch `issue-257-auto` onto the current base branch (confirm with user before doing so, since it changes branch history) — this brings in `core/runtime/src/config-loader.ts` provenance support, `cli/commands/src/commands/system/config/show.ts`, and the existing unit tests.
2. Add a same-root (`sameLocation: true`) e2e case to `tools/kb-create/e2e/e2e_test.go`, adjacent to the existing `config_show` split-root subtest — set up `platformDir == projectDir` (monorepo-style single `.kb/kb.config.jsonc`) and assert via `kb config show --json` that every field in `sources.fields` reports `both` and `sources.ignoredProjectFields` is empty.
3. Extend the existing split-root `config_show` e2e subtest (or add a new one) to seed the installed platform config with a fake but realistic secret-shaped field (e.g. `adapterOptions.llm.apiKey` or `gateway.jwtSecret`) and assert the raw secret string never appears in either `--json` or table stdout, only `***REDACTED***`.
4. Re-run `pnpm --filter <cli-commands-pkg> test` and the Go e2e suite (`cd tools/kb-create/e2e && go test ./...`) to confirm both new cases pass and nothing regresses.

## Tests / verification
- Unit: `cli/commands/src/__tests__/config-show.test.ts` already exists and should continue passing post-merge.
- E2E (Go): existing `config_show` split-root subtest plus the two new cases above (same-root sameLocation, and secret-redaction-through-real-CLI). Per repo's bug-fix rule, each new case must be shown to fail before its corresponding code exists (or on `main` without the branch) and pass after merge — since the feature itself is already implemented on `issue-257-auto`, the "before" state is simply the missing e2e coverage, so write the two new subtests first against `main` to confirm they'd fail without the redaction/sameLocation logic, then confirm they pass once merged.