{
  "description": "Lightweight telemetry client for KB Labs platform. Zero dependencies. Use in any Node.js/browser product to send events to the platform.",
  "devDependencies": {
    "@kb-labs/devkit": "workspace:*",
    "@types/node": "^22.0.0",
    "rimraf": "^6.0.1",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "name": "@kb-labs/telemetry-client",
  "scripts": {
    "build": "tsup",
    "clean": "rimraf dist",
    "dev": "echo 'todo: dev'",
    "lint": "eslint . --ignore-pattern 'dist/**' --ignore-pattern 'coverage/**'",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "type": "module",
  "types": "./dist/index.d.ts",
  "version": "2.14.0"
}
