{
  "schema": "kb.scenario/2",
  "id": "custom",
  "title": "Custom platform",
  "selection": {"components": []},
  "pages": [{
    "id": "access",
    "sections": [{
      "id": "gateway",
      "fields": [{
        "id": "access.mode",
          "type": "select",
        "label": "Studio access",
        "default": "local",
        "options": [
          {"value": "local", "label": "Local (no login)", "effects": ["gateway.access.local"]},
          {"value": "secured", "label": "Secured", "effects": ["gateway.access.secured"]}
        ]
      }]
    }]
  }]
}
