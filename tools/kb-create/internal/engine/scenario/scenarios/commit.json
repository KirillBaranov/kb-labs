{
  "schema": "kb.scenario/2",
  "id": "commit",
  "title": "AI commits",
  "install": {
    "components": [{ "id": "plugin:commit" }],
    "providerPreferences": [{ "capability": "cache", "field": "cache" }]
  },
  "pages": [
    {
      "id": "providers",
      "sections": [
        {
          "id": "cache",
          "title": "Cache provider",
          "fields": [
            {
              "id": "cache",
              "type": "select",
              "required": true,
              "default": "state-broker",
              "options": [
                { "value": "state-broker", "label": "StateBroker" },
                { "value": "redis-cache", "label": "Redis" }
              ]
            }
          ]
        }
      ]
    }
  ]
}
