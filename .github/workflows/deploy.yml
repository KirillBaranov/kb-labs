name: Deploy

on:
  push:
    branches: [main]
    paths:
      - 'sites/web/**'
      - 'plugins/gateway/**'
      - 'plugins/marketplace-registry/**'
      - 'adapters/openai/**'
      - 'adapters/redis/**'
      - 'adapters/sqlite/**'
      - 'adapters/pino/**'
      - 'adapters/fs/**'
      - 'adapters/log-sqlite/**'
      - 'adapters/log-ringbuffer/**'
      - 'adapters/eventbus-cache/**'
      - 'core/**'
      - 'shared/**'
      - 'tools/kb-deploy/**'
      - '.kb/deploy.yaml'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:
    inputs:
      target:
        description: 'Target to deploy (leave empty for affected)'
        required: false
        default: ''

permissions:
  contents: read
  packages: write  # required for docker push to ghcr.io

concurrency:
  group: deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # full history for multi-commit push diff

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache-dependency-path: tools/kb-deploy/go.sum

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache-dependency-path: tools/kb-devkit/go.sum

      - name: Build kb-devkit
        run: go build -o kb-devkit .
        working-directory: tools/kb-devkit

      - name: Build kb-deploy
        run: make build
        working-directory: tools/kb-deploy

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: ./tools/kb-devkit/kb-devkit run build

      - name: Deploy gateway-app (pnpm deploy)
        run: pnpm deploy --filter @kb-labs/gateway-app --prod .kb/deploy/gateway-app

      - name: Deploy marketplace-registry-app (pnpm deploy)
        run: pnpm deploy --filter @kb-labs/marketplace-registry-app --prod .kb/deploy/marketplace-registry-app

      - name: Provision VPS env (idempotent)
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_KEY }}
          KB_REGISTRY_ADMIN_TOKEN: ${{ secrets.KB_REGISTRY_ADMIN_TOKEN }}
        run: |
          echo "$SSH_KEY" > /tmp/deploy_key && chmod 600 /tmp/deploy_key
          # Write admin token to VPS .env (idempotent)
          ssh -i /tmp/deploy_key -o StrictHostKeyChecking=no "$SSH_USER@$SSH_HOST" \
            "grep -q KB_REGISTRY_ADMIN_TOKEN ~/kb-labs/.env 2>/dev/null || printf '%s\n' 'KB_REGISTRY_ADMIN_TOKEN=$KB_REGISTRY_ADMIN_TOKEN' >> ~/kb-labs/.env"
          # Patch docker-compose to expose KB_REGISTRY_ADMIN_TOKEN to the container (idempotent)
          ssh -i /tmp/deploy_key -o StrictHostKeyChecking=no "$SSH_USER@$SSH_HOST" \
            "grep -q 'KB_REGISTRY_ADMIN_TOKEN' ~/kb-labs/docker-compose.backend.yml || sed -i 's/- KB_REGISTRY_URL=/- KB_REGISTRY_ADMIN_TOKEN\n      - KB_REGISTRY_URL=/' ~/kb-labs/docker-compose.backend.yml"
          rm -f /tmp/deploy_key

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Deploy
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_KEY }}
          KB_DEPLOY_BASE_REF: ${{ github.event.before }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -n "${{ inputs.target }}" ]; then
            ./tools/kb-deploy/kb-deploy run ${{ inputs.target }}
          else
            ./tools/kb-deploy/kb-deploy run
          fi

      - name: Show status
        run: ./tools/kb-deploy/kb-deploy status
