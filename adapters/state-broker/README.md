# `@kb-labs/adapters-state-broker`

`ICache` adapter backed by the KB Labs StateBroker. It uses the local StateBroker daemon when `url` is configured and can run in memory when no daemon is available, so users do not need to provision Redis for the default installation.
