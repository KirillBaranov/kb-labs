# @kb-labs/adapters-analytics-duckdb

DuckDB analytics adapter for the KB Labs platform. Provides SQL-native time-series analytics with support for date aggregation, metric filtering, and the full `EventsQuery` interface.
