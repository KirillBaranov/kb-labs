# @kb-labs/adapters-analytics-sqlite

SQLite analytics adapter for the KB Labs platform. Stores and queries event data with concurrent writes and no lock contention, suitable for single-node deployments.
