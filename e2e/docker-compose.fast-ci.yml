# Fast PR E2E override. Internal packages are supplied by the exact base
# snapshot plus PR overlay; missing internal packages fail the bootstrap.
services:
  verdaccio:
    volumes:
      - ./verdaccio/config.fast.yaml:/verdaccio/conf/config.yaml:ro
