# Baseline scenario for marketplace e2e domain.
# Applies no overlays — the unmodified project config is in effect.
# The runner ensures this scenario before any other; reset on completion.
name: marketplace-default
overlays: []
restarts: []
