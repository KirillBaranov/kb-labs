name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e

      - name: Build and push web
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a
        with:
          context: .
          file: apps/web/Dockerfile
          push: true
          tags: ghcr.io/kb-labs-team/kb-labs-web:latest
          cache-from: type=gha,scope=web
          cache-to: type=gha,mode=max,scope=web

      - name: Build and push docs
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a
        with:
          context: .
          file: apps/docs/Dockerfile
          push: true
          tags: ghcr.io/kb-labs-team/kb-labs-docs:latest
          cache-from: type=gha,scope=docs
          cache-to: type=gha,mode=max,scope=docs

      - name: Deploy to server
        uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker pull ghcr.io/kb-labs-team/kb-labs-web:latest
            docker pull ghcr.io/kb-labs-team/kb-labs-docs:latest
            docker stop kb-labs-web kb-labs-docs 2>/dev/null || true
            docker rm kb-labs-web kb-labs-docs 2>/dev/null || true
            docker run -d --name kb-labs-web --restart unless-stopped -p 3000:3000 ghcr.io/kb-labs-team/kb-labs-web:latest
            docker run -d --name kb-labs-docs --restart unless-stopped -p 3001:3001 ghcr.io/kb-labs-team/kb-labs-docs:latest
            docker image prune -f
