# <%= it.scope ? it.scope + '/' : '' %><%= it.name %>

> <%= it.vars.description %>

![license](https://img.shields.io/badge/license-<%= it.vars.license %>-green)
![platform](https://img.shields.io/badge/kb--labs-%3E%3D0.1.0-orange)

---

## Overview

<!-- TODO: 2-3 sentences describing what this plugin solves from the user's perspective. -->

---

## Features

<!-- TODO: bullet list of key user-facing capabilities -->
- Feature one
- Feature two
- Feature three

---

## Requirements

**KB Labs platform** `>= 0.1.0`

**Platform services**

| Service | Required | Purpose |
|---------|----------|---------|
| `llm` | Optional | AI-powered generation |
| `cache` | Required | Result caching |
| `storage` | Required | Persistent state |

<!-- TODO: add / remove rows based on what the manifest declares -->

---

## Installation

```bash
pnpm kb marketplace install <%= it.scope ? it.scope + '/' : '' %><%= it.name %>-entry
```

---

## Commands

```bash
kb <%= it.name %> hello               # say hi
kb <%= it.name %> hello --who=World   # same, explicit
```

| Command | Description |
|---------|-------------|
| `kb <%= it.name %> hello` | Greet the platform |

<!-- TODO: replace with real commands once implemented -->

---

## Configuration

```jsonc
{
  "<%= it.name %>": {
    // TODO: document config options here
  }
}
```

---

<!--
## REST API

Base path: `/<%= it.name %>`

| Method | Path | Description |
|--------|------|-------------|
| `GET`  | `/<%= it.name %>/status` | Plugin status |

Remove the comment markers above/below if this plugin registers REST routes.
-->

<!--
## Studio

Adds a **<%= it.name %>** section to KB Labs Studio.

| Page | Route | Description |
|------|-------|-------------|
| Overview | `/p/<%= it.name %>` | Status and quick actions |

Remove the comment markers above/below if this plugin adds Studio pages.
-->

## Permissions

| Category | Scope | Reason |
|----------|-------|--------|
| Filesystem | `.kb/<%= it.name %>/**` | Plugin state |
| Platform | `llm`, `cache` | Core functionality |

<!-- TODO: update to match manifest permissions -->

---

## Changelog

### 0.1.0

- Initial release

---

## License

<%= it.vars.license %>

---

<details>
<summary>Development</summary>

**Layout**

```
packages/
  <%= it.name %>-contracts/   # public types (request/response shapes)
  <%= it.name %>-core/        # pure business logic (testable in isolation)
  <%= it.name %>-entry/       # V3 manifest + CLI command handlers
```

**Getting started**

```bash
pnpm install
pnpm -w build
pnpm kb <%= it.name %> hello
```

**What to edit**

- Business logic: `packages/<%= it.name %>-core/src/hello.ts`
- CLI commands: `packages/<%= it.name %>-entry/src/manifest.ts`
- Shared types: `packages/<%= it.name %>-contracts/src/index.ts`

**Link into a workspace**

```bash
pnpm kb marketplace plugins link .
pnpm kb plugins clear-cache
```

**Health check**

```bash
pnpm kb scaffold doctor --path .
```

</details>
