{
  "name": "@test/medium-project",
  "description": "Medium complexity test project for Mind fixtures",
  "version": "1.2.0",
  "type": "module",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint . --ignore-pattern 'dist/**'",
    "lint:fix": "eslint . --fix --ignore-pattern 'dist/**'",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "lodash": "^4.17.21",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.0",
    "vitest": "^3.2.4"
  }
}
