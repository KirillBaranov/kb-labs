# QA Run — 2026-06-14

**Environment:** Dev monorepo (not kb-env — kb-env not yet provisioned)  
**Tester:** Claude Code (automated)  
**Scope:** P0 scenarios + selected P1

## Service Status at Run Start

```
● state-daemon      alive   http://localhost:7777
● gateway           alive   http://localhost:4000
✕ marketplace       failed  unix socket (health check fails)
✕ marketplace-reg.  failed  unix socket
● rest              alive   http://localhost:5050
● studio            alive   http://localhost:3000
● workflow          alive   unix:…/workflow.sock
○ mcp-daemon        dead    unix socket
```

5 alive · 2 failed · 1 dead

---

## Results

### PC — Platform Core

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| PC-002 | First start — all services up | ⚠️ | 5/8 up; marketplace × 2 failed, mcp-daemon dead |
| PC-003 | Health & diagnostics | ✅ | `GET /health` → `{"status":"healthy"}` |

---

### GW — Gateway

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| GW-001 | LLM request proxied end-to-end | ⚠️ | `/api/v1/llm/chat` → 404; LLM adapter healthy in `/health`. Route missing in gateway. |
| GW-006 | Auth — valid/invalid token | ✅ | `auth.enabled=false`: `/auth/me` → local-admin without token; `/auth/providers` public |

---

### WF — Workflow Engine

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| WF-001 | Create workflow from YAML | ⚠️ | No `register` command — workflows loaded from `.kb/workflows/` dir. Manual file drop works. |
| WF-002 | Run with inputs | ⬜ | Not tested |
| WF-003 | Cancel running run | ❌ | Cancel returns `cancellation requested` but run ends with `SUCCESS` (step stays `queued`). Bug: cancel doesn't actually kill job. |
| WF-006 | Approval gate — approve | ✅ | test-approval paused at gate, `runs approve <shortId>` worked, post-gate ran |
| WF-007 | Approval gate — reject | ✅ | `runs approve --reject` worked, run stopped |
| WF-008 | Step failure — visible | ✅ | e2e-fail: exit code 1 shown inline, `FAILED` status |
| WF-013 | Short ID resolution | ✅ | 8-char IDs work in `view`, `approve`, `cancel`, `logs` |
| WF-015 | Run history and logs | ✅ | History persists; `runs list` shows all past runs |

---

### OB — CLI Observability

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| OB-001 | List runs — table | ✅ | Table renders with Workflow, Trigger, When, Status, Step, Dur, ID columns |
| OB-003 | View run — tree | ✅ | Tree with job header, step statuses, inline error messages |
| OB-005 | Logs — tail step output | ✅ | `runs logs <shortId>` returns full log with timestamps |
| OB-006 | Approve pending run from CLI | ✅ | Short ID works; gate approved via CLI |
| OB-007 | Reject pending run from CLI | ✅ | `--reject` flag works |
| OB-008 | PENDING_APPROVAL visible in list | ✅ | False alarm — timing issue on first check. `runs-list.ts:74` uses `hasPendingApproval` flag correctly. Confirmed working with `--json`. |

---

### ST — Studio

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| ST-001 | No login flash | ⬜ | Not tested manually (requires browser) |
| ST-002 | Runs list loads with data | ⬜ | Not tested manually |

---

### CF — Config

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| CF-001 | auth.enabled=false — Studio open | ✅ | `/auth/me` returns `local-admin`, no token needed |
| CF-003 | KB_SOCKET_HASH injected | ⚠️ | Warning in CLI: `unresolved placeholder ${KB_SOCKET_HASH}`. Socket works but hash must be set manually (`KB_SOCKET_HASH=86a20aa2`). Should be auto-injected. |
| CF-004 | Unresolved placeholder → clear error | ✅ | Warning logged clearly: "unresolved placeholder ${KB_SOCKET_HASH}" |

---

### CLI — CLI Framework

| ID | Title | Status | Notes |
|----|-------|--------|-------|
| CLI-002 | `--help` on every command | ✅ | `kb workflow --help`, `kb workflow runs --help` all work |
| CLI-003 | `--json` output is valid JSON | ⚠️ | Output is `{"ok": true, "data": [...]}` wrapper, not a raw array. Parseable but not a bare array as expected. |
| CLI-005 | Ctrl+C clean exit | ⬜ | Not tested (requires interactive terminal) |

---

## Bugs Found

| # | ID | Severity | Description |
|---|---|----------|-------------|
| 1 | WF-003 | P1 | Cancel returns `cancellation requested` but run ends `SUCCESS` with step `queued`. **Fixed:** `worker.ts` — track `wasCancelled` flag, call `markJobInterrupted` on cancel path. Test added. |
| 2 | OB-008 | — | False alarm. Timing issue on first check. Code already correct. |
| 3 | GW-001 | P1 | `/api/v1/llm/chat` route not found (404). **Fixed:** route path was `/llm/v1/chat/completions` instead of `/api/v1/llm/chat/completions`. Test updated. |
| 4 | CLI-003 | P2 | `--json` wraps output in `{"ok": true, "data": [...]}` instead of bare array. Scripts expecting `jq '.[]'` will break. |
| 5 | CF-003 | P1 | `KB_SOCKET_HASH` not auto-injected when CLI is run outside kb-dev context. Warning printed but CLI still works. |
| 6 | MK | P0 | marketplace service failing health check. |

---

## Notes

- This run was against the **dev monorepo**, not a fresh `kb-env` install. Some failures may be dev-env specific.
- WF-001: workflow YAML loaded from `.kb/workflows/` directory — no `register` CLI command. Scenarios need updating to reflect the actual API.
- Workflow `run:` shorthand works (e2e-hello, e2e-fail both execute correctly).
- Short ID resolution works end-to-end across all `:runId` commands.
- `hasPendingApproval` field present in JSON — backend is correct, only the CLI display is missing it.
