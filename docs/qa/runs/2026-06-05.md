# QA Run Log — 2026-06-05

**Date:** 2026-06-05  
**Tester:** Claude (assisted manual)  
**Platform:** `2.94.0` built from source + Verdaccio  
**OS / Node:** macOS Darwin 24.5.0 / Node 20.19.4  

---

## Summary

| # | Scenario | Result | Key finding |
|---|---|---|---|
| S-001 | Solo — Install & First Run | ⚠️ PARTIAL | B-001 (no .env), B-013 (scaffold manifest) |
| S-002 | Solo — First Workflow | ⚠️ PARTIAL | CLI blocked B-001, HTTP API works |
| S-006 | Workflow — Create from YAML | ⚠️ PARTIAL | Discovery ✅, invalid YAML silently accepted ❌ |
| S-007 | Workflow — Run with inputs | ⚠️ PARTIAL | Run ✅, hello-world 404 ❌, no input validation ❌ |
| S-008 | Workflow — Cancel | ✅ **PASS** | Full pass including edge cases |
| S-009 | Workflow — History & Logs | ⚠️ PARTIAL | List ✅, jobs ✅, all runs `dlq` ❌ |
| S-010 | Marketplace — Install plugin | ⚠️ PARTIAL | Install ✅, idempotent ✅, `kb --help` stale ❌ |
| S-011 | Marketplace — Enable/Disable | ❌ FAIL | B-014: `findCommand` crash after update |
| S-012 | Marketplace — Update plugin | ⚠️ PARTIAL | Update runs ✅, no version bump (already latest) |
| S-013 | Plugin — Full cycle | ❌ FAIL | B-013: segments missing, B-020: type-check error in template |
| S-015 | AI Code Review | ⚠️ PARTIAL | Heuristic runs ✅, no LLM (B-001) |
| S-016 | Mind — RAG search | ❌ FAIL | `mind search` unknown command after install |
| S-017 | Gateway — Own key | ⚠️ PARTIAL | Gateway health ✅, LLM available ✅, config switch untested |
| S-018 | Gateway — Switch provider | ⚠️ PARTIAL | Config structure readable, switch untested |
| S-019 | Gateway — Usage/quota | ❌ FAIL | `kb gateway usage` unknown, no API endpoint |
| S-020 | Studio — First login | ⚠️ PARTIAL | Page loads ✅, rate limiting ✅, no bootstrap user ❌ |
| S-021 | Studio — Workflow runs | ⚠️ PARTIAL | API works ✅, UI untested (no auth) |
| S-022 | Studio — Trigger workflow | ⬜ SKIP | Blocked by S-020 (no login) |
| S-023 | Platform — Update | ⚠️ PARTIAL | Update ✅, B-014: registry flip breaks CLI |
| S-024 | Platform — Rollback | ❌ FAIL | `no releases installed` on fresh platform |
| S-025 | Diagnose broken setup | ⚠️ PARTIAL | doctor ✅, `marketplace clear-cache` unknown ❌ |
| S-026 | Plugin — Publish to marketplace | ⚠️ PARTIAL | Publish ✅, in Verdaccio ✅, duplicate version gives wrong error |

**Overall: FAIL**  
3 PASS-ish · 11 PARTIAL · 6 FAIL · 1 SKIP  
**1 clean PASS: S-008 (Workflow Cancel)**

---

## Full Bug Backlog

| ID | Priority | Scenario | Description |
|---|---|---|---|
| **B-001** | P0 | S-001 | `--llm` bootstrap: gateway 401 → no `.env` → no real LLM anywhere |
| **B-013** | P0 | S-013 | Scaffold template missing `segments` on commands → MANIFEST_VALIDATION_FAILED |
| **B-014** | P0 | S-023 | `kb-create update` flips registry to npmjs → version mismatch → `findCommand` crash |
| B-015 | P1 | S-006 | Invalid/incomplete workflow YAML silently accepted as `active` |
| ~~B-016~~ | FIXED | S-007 | File-based workflow with inner `id:` ≠ filename: listed under inner id but POST /runs 404 (get resolves by filename). **Fixed:** filename is canonical id; list()/get() agree. |
| B-017 | P1 | S-007 | Missing required workflow inputs not validated — run accepted, fails in job |
| ~~B-018~~ | FIXED | S-009 | Subsumed by B-029: step failures went to `dlq`. Now → `failed`. (Example workflows failing on missing host tools is environmental, not a platform bug.) |
| ~~B-019~~ | — | S-010 | **NOT A BUG.** CLI discovery cache auto-invalidates on `marketplace.lock` hash change (discover.ts:1129-1148). A properly built+installed plugin (e.g. scaffolded `@kb-labs/green`) IS visible in `kb --help` immediately, even after clearing the cache. Original observation conflated with B-021. |
| B-020 | P1 | S-013 | Scaffold template: type error in generated `error.ts` (`MessageOptions` mismatch) |
| ~~B-021~~ | FIXED | S-016 | **Root cause + fix.** `kb marketplace install mind` records `mind` in the lock with `resolvedPath: ./node_modules/mind` but the package is never installed there — "mind" resolves to a wrong/nonexistent npm package, not `@kb-labs/mind-entry`. Install reports success while the package is absent → discovery finds nothing. Marketplace resolution + silent-failure bug. **Fixed:** detectKind no longer falls back to 'plugin'; install rejects non-entity packages with InvalidEntityError (HTTP 422), rolls back the npm install, and writes nothing to the lock. Note: short-name → @kb-labs/*-entry resolution is a separate UX item (user must use the scoped name). |
| B-022 | P1 | S-019 | `kb gateway usage` command doesn't exist, no API endpoint |
| B-023 | P1 | S-020 | No bootstrap user created on fresh install → can't login to Studio |
| B-024 | P1 | S-024 | `kb-create rollback` fails on fresh platform — "no releases installed" |
| ~~B-025~~ | FIXED | S-025 | Wrong command name in docs/scenario — correct command is `kb marketplace plugins refresh`. Also fixed genuinely-broken `marketplace:clear-cache` (nonexistent) in package.json release scripts + site/docs prose. |
| B-026 | P1 | S-026 | Duplicate version publish gives ENEEDAUTH error instead of "version exists" |
| B-008 | P1 | S-002 | `kb-dev start` exits 0 even when services fail |
| B-009 | P1 | S-002 | Studio port 3000 conflicts with kb-web dev service |
| B-003 | P1 dev | S-001 | `kb` alias `pnpm --silent kb` overrides binary in dev env |
| B-007 | P2 | S-001 | `unmet peer zod@>=4.1.5` in pnpm output |

---

## Fixed since 2.93.4

| ID | Description |
|---|---|
| ~~B-004~~ | Raw stack trace on expired creds → graceful heuristic fallback |
| ~~B-005~~ | Scaffold exits 0 but creates nothing → creates correctly |
| ~~B-002~~ | `kb-create status` wrong project path |
| ~~B-006~~ | `marketplace.lock` not created → works now |
| ~~B-010~~ | All CLI blocked → graceful fallback |
| ~~B-011~~ | Example workflow dlq (reproduced as B-018, different root) |

---

## Scenario Detail

### S-006 — Workflow Create from YAML ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 1 create file | ✅ | |
| 2 appears in API list | ✅ | hot reload works |
| 3 `kb workflow list` | ❌ | CLI auth blocked |
| 5 invalid YAML silently accepted | ❌ | B-015: shows as `active` |
| 6 hot reload after edit | ✅ | |

### S-007 — Run with Inputs ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 4 POST /runs → `{ok, runId}` | ✅ | healthcheck workflow |
| 5 GET /runs/:id shows inputs | ✅ | |
| 6 missing required input → error | ❌ | B-017: accepted silently |
| hello-world POST | ❌ | B-016: 404 |

### S-008 — Cancel ✅ PASS
| # | Status | Note |
|---|---|---|
| 4 POST /cancel → cancelled | ✅ | |
| 5 status = `cancelled` | ✅ | |
| 7 fake runId → 404 | ✅ | |
| 6 cancel completed run | ⬜ | not tested |

### S-009 — History & Logs ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 1 GET /runs list | ✅ | |
| 4 run detail with jobs | ✅ | |
| 5 timestamps present | ✅ | |
| 6 failed job has error | ✅ | |
| all runs in `dlq` | ❌ | B-018 |

### S-010 — Marketplace Install ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 4 `kb marketplace install mind` | ✅ | mind@0.0.2 |
| 6 marketplace.lock updated | ✅ | |
| 5 `kb --help` shows mind | ❌ | B-019: needs restart |
| 8 re-install idempotent | ✅ | |

### S-012 — Update Plugin ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 3 `kb marketplace update mind` | ✅ | exits 0 |
| 4 lock updated | ✅ | same version (already latest) |
| — no version bump available | ⬜ | can't test upgrade |

### S-013 — Plugin Full Cycle ❌ FAIL
| # | Status | Note |
|---|---|---|
| 1–4 scaffold + lock | ✅ | |
| 5 pnpm install | ✅ | |
| 6 pnpm build | ✅ | |
| 14 type-check | ❌ | B-020: `MessageOptions` mismatch in generated `error.ts` |
| 7 manifest validation | ❌ | B-013: segments missing |
| 9 `kb myplugin hello` | ❌ | MANIFEST_VALIDATION_FAILED |

### S-015 — AI Code Review ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 2 `kb review run` | ✅ | heuristic, 0 issues |
| 4 exit 0 | ✅ | |
| — LLM review | ❌ | B-001 |

### S-016 — Mind RAG ❌ FAIL
| # | Status | Note |
|---|---|---|
| install | ✅ | mind@0.0.2 |
| `kb mind search` | ❌ | B-021: unknown command |

### S-019 — Gateway Usage ❌ FAIL
| # | Status | Note |
|---|---|---|
| `kb gateway usage` | ❌ | B-022: unknown command |
| GET /api/v1/gateway/usage | ❌ | 404 |

### S-020 — Studio Login ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 1 page loads | ✅ | HTML served, SPA |
| 7 wrong creds → error | ✅ | `{"error":"invalid_credentials"}` |
| 8 rate limit after 5 attempts | ✅ | HTTP 429 on 6th |
| 4 valid login | ❌ | B-023: no bootstrap user |

### S-023 — Platform Update ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 3 `kb-create update` exits 0 | ✅ | |
| 5 lock preserved | ✅ | |
| — registry flip | ❌ | B-014: switches to npmjs |

### S-024 — Rollback ❌ FAIL
| # | Status | Note |
|---|---|---|
| 1 `kb-create releases` | ❌ | "no releases installed" |
| 3 `kb-create rollback` | ❌ | B-024: args error |

### S-025 — Diagnose ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 1 doctor 8/8 | ✅ | |
| 9 port conflict message | ✅ | clear message |
| 10 `kb marketplace clear-cache` | ❌ | B-025: unknown command |

### S-026 — Plugin Publish ⚠️ PARTIAL
| # | Status | Note |
|---|---|---|
| 5 publish to Verdaccio | ✅ | `@kb-labs/myplugin-entry@0.1.0` |
| 6 in registry | ✅ | versions: ['0.1.0'] |
| 13 no auth → ENEEDAUTH | ✅ | |
| 14 duplicate version | ❌ | B-026: wrong error (ENEEDAUTH not "version exists") |

---

## Not run (requires VPS / real LLM / team)
S-004, S-005, S-014, S-021 (UI), S-022, S-027
