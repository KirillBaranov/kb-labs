# Run Log — 2026-06-05

**Date:** 2026-06-05  
**Tester:** Claude (assisted)  
**Scope:** S-001 (two passes: old binaries 2.93.4 + fresh source 2.94.0), S-002 (old binaries)  
**Platform version:** `2.94.0` built from source + Verdaccio  
**OS / Node:** macOS Darwin 24.5.0 / Node 20.19.4  

## Results

| Scenario | Result | Bugs |
|---|---|---|
| S-001 Solo — Install & First Run | ⚠️ PARTIAL | B-001, B-007, B-013 |
| S-002 Solo — First Workflow | ❌ FAIL (old binaries) | B-008, B-009, B-010, B-011 |
| S-003 Solo — Create & Run Plugin | ⬜ not run separately | |
| S-004 Team Admin — Cloud Deploy | ⬜ not run | |
| S-005 Team Member — Connect | ⬜ not run | |

## Overall: FAIL (P0 blockers present)

---

## Full Bug Backlog

| ID | Scenario | Priority | Status | Description |
|---|---|---|---|---|
| B-001 | S-001 | **P0** | open | `--llm` bootstrap: gateway registration 401 → `.env` not written → AI features fall back to heuristics |
| B-013 | S-001 | **P0** | open | Scaffold template missing `segments` field on commands → `MANIFEST_VALIDATION_FAILED` → plugin unusable |
| B-008 | S-002 | P1 | open | `kb-dev start` exits 0 even when services fail |
| B-009 | S-002 | P1 | open | Studio port 3000 conflicts with kb-web from dev infrastructure |
| B-011 | S-002 | P1 | open | Example workflow `deploy-with-approval` goes to `dlq` on first run |
| B-003 | S-001 | P1 dev | open | `kb` alias `pnpm --silent kb` overrides binary in dev env |
| B-007 | S-001 | P2 | open | `unmet peer zod@>=4.1.5: found 3.25.76` in pnpm output |

## Fixed on 2.94.0 (vs old 2.93.4)

| ID | Description |
|---|---|
| B-004 | Raw stack trace on bad creds → now graceful heuristic fallback (exit 0, readable output) |
| B-005 | `kb scaffold` exiting 0 but creating nothing → now creates correctly |
| B-002 | `kb-create status` wrong project path → fixed (shows correct path with fresh `--platform`) |
| B-006 | `marketplace.lock` not created after update → scaffold now registers in lock correctly |
| B-010 | CLI unusable without valid creds → now falls back gracefully |

## Root cause chain

**B-001** (gateway 401) → all AI features fall back to heuristics (not blocked, but degraded)  
**B-013** (scaffold template) → independent, must fix in template source

## QA Environment Notes

- Proper test requires: build `tools/kb-create`, `pack-all.sh`, Verdaccio v5, publish script, `--platform /tmp/qa-platform`
- Old platform dir (`~/kb-platform`) had stale `core-config` without `loadOverlays` → use fresh `--platform` always
- `kb-dev start` needs `--config .kb/devservices.dev.yaml` in worktree env (hook blocks default)
