# Test Catalog

> Auto-generated by `node scripts/test-catalog.mjs`. Do not edit by hand.  
> Last updated: 2026-05-15 — 5069 test cases across 514 files.

## Summary

| Package | CLI | SSE | WS | Integration | E2E | Unit | Total |
|---|---|---|---|---|---|---|---|
| `adapters-analytics-file` | — | — | — | — | 10 | 12 | **22** |
| `adapters-environment-docker` | — | — | — | — | — | 8 | **8** |
| `adapters-eventbus-cache` | — | — | — | — | — | 9 | **9** |
| `adapters-fs` | — | — | — | — | — | 50 | **50** |
| `adapters-log-ringbuffer` | — | — | — | — | — | 21 | **21** |
| `adapters-log-sqlite` | — | — | — | — | — | 37 | **37** |
| `adapters-pino` | — | — | — | — | — | 2 | **2** |
| `adapters-snapshot-localfs` | — | — | — | — | — | 1 | **1** |
| `adapters-sqlite` | — | — | — | — | — | 30 | **30** |
| `adapters-transport` | — | — | — | — | — | 14 | **14** |
| `adapters-workspace-agent` | — | — | — | — | — | 14 | **14** |
| `adapters-workspace-localfs` | — | — | — | — | — | 1 | **1** |
| `adapters-workspace-worktree` | — | — | — | — | — | 5 | **5** |
| `agent-core` | — | — | — | — | — | 312 | **312** |
| `agent-history` | — | — | — | — | — | 30 | **30** |
| `agent-kernel` | — | — | — | — | — | 3 | **3** |
| `agent-mcp` | — | — | — | — | — | 31 | **31** |
| `agent-runtime` | — | — | — | — | — | 13 | **13** |
| `agent-store` | — | — | — | — | — | 1 | **1** |
| `agent-tools` | — | — | — | — | — | 146 | **146** |
| `agent-tracing` | — | — | — | — | — | 4 | **4** |
| `cli-bin` | 4 | — | — | — | — | 90 | **94** |
| `cli-commands` | — | — | — | 3 | 10 | 82 | **95** |
| `cli-runtime` | — | — | — | 7 | — | 81 | **88** |
| `clickup-entry` | 88 | — | — | — | — | — | **88** |
| `commit-contracts` | — | — | — | — | — | 3 | **3** |
| `commit-core` | — | — | — | — | — | 17 | **17** |
| `commit-entry` | — | — | — | — | 8 | 5 | **13** |
| `core-bundle` | — | — | — | — | 13 | — | **13** |
| `core-config` | — | — | — | — | 112 | — | **112** |
| `core-contracts` | — | — | — | — | — | 7 | **7** |
| `core-discovery` | — | — | — | — | 85 | — | **85** |
| `core-ipc` | — | — | — | — | — | 40 | **40** |
| `core-platform` | — | — | — | — | 34 | 147 | **181** |
| `core-policy` | — | — | — | — | 36 | — | **36** |
| `core-registry` | — | — | — | — | 57 | — | **57** |
| `core-resource-broker` | — | — | — | — | — | 22 | **22** |
| `core-runtime` | — | — | — | — | 24 | 214 | **238** |
| `core-sandbox` | — | — | — | — | — | 38 | **38** |
| `core-state-broker` | — | — | — | — | — | 37 | **37** |
| `core-state-daemon` | — | — | — | — | — | 47 | **47** |
| `core-sys` | — | — | — | — | 8 | — | **8** |
| `core-tenant` | — | — | — | — | — | 20 | **20** |
| `core-workspace` | — | — | — | — | 20 | — | **20** |
| `e2e` | — | — | — | — | 133 | — | **133** |
| `e2e-gateway` | — | — | — | — | 14 | — | **14** |
| `e2e-marketplace` | — | — | — | — | 13 | — | **13** |
| `e2e-platform` | — | — | — | — | 16 | — | **16** |
| `e2e-plugins` | — | — | — | — | 16 | — | **16** |
| `e2e-services` | — | — | — | — | 15 | — | **15** |
| `e2e-studio` | — | — | — | — | 28 | — | **28** |
| `e2e-workflows` | — | 11 | 7 | — | 30 | — | **48** |
| `gateway-app` | — | — | — | 19 | 55 | 190 | **264** |
| `gateway-auth` | — | — | — | — | — | 30 | **30** |
| `gateway-contracts` | — | — | — | — | — | 24 | **24** |
| `gateway-core` | — | — | — | — | — | 62 | **62** |
| `gateway-runtime-server` | — | — | — | — | — | 11 | **11** |
| `host-agent-app` | — | — | — | — | — | 18 | **18** |
| `host-agent-client` | 4 | — | — | — | — | — | **4** |
| `host-agent-core` | — | — | — | — | — | 17 | **17** |
| `host-agent-entry` | 19 | — | — | — | — | — | **19** |
| `host-agent-fs` | — | — | — | — | — | 33 | **33** |
| `host-agent-transport` | — | — | — | — | — | 6 | **6** |
| `impact-core` | — | — | — | — | — | 17 | **17** |
| `infra-worker-core` | — | — | — | — | 4 | — | **4** |
| `llm-router` | — | — | — | — | — | 52 | **52** |
| `marketplace-api` | — | — | — | — | 3 | — | **3** |
| `marketplace-core` | — | — | — | — | 46 | — | **46** |
| `marketplace-entry` | 27 | — | — | — | — | 7 | **34** |
| `marketplace-npm` | — | — | — | — | 5 | — | **5** |
| `mind-adapters` | — | — | — | — | 4 | — | **4** |
| `mind-contracts` | — | — | — | — | — | 3 | **3** |
| `mind-core` | — | — | — | — | 32 | 19 | **51** |
| `mind-engine` | — | — | — | — | 101 | — | **101** |
| `mind-entry` | — | — | — | — | 22 | — | **22** |
| `mind-indexer` | — | — | — | — | 11 | — | **11** |
| `mind-orchestrator` | — | — | — | — | 25 | — | **25** |
| `perm-presets` | — | — | — | — | — | 60 | **60** |
| `platform-client` | 12 | — | — | — | — | — | **12** |
| `plugin-contracts` | — | — | — | — | — | 14 | **14** |
| `plugin-execution` | — | — | 38 | — | — | 1 | **39** |
| `plugin-execution-factory` | — | — | — | 4 | — | 68 | **72** |
| `plugin-runtime` | — | — | — | 3 | — | 470 | **473** |
| `policy-contracts` | — | — | — | — | — | 49 | **49** |
| `qa-core` | — | — | — | — | 66 | — | **66** |
| `quality-contracts` | — | — | — | — | — | 3 | **3** |
| `release-manager-core` | — | — | — | — | — | 60 | **60** |
| `rest-api-app` | — | — | — | — | 184 | — | **184** |
| `rest-api-core` | — | — | — | — | 25 | 8 | **33** |
| `review-entry` | 8 | — | — | — | — | — | **8** |
| `scaffold` | 10 | — | — | — | — | 1 | **11** |
| `scaffold-core` | — | — | — | — | 5 | 23 | **28** |
| `sdk` | — | — | — | — | — | 22 | **22** |
| `shared-cli-ui` | — | — | — | — | 14 | 11 | **25** |
| `shared-command-kit` | — | — | — | — | 71 | 67 | **138** |
| `shared-http` | — | — | — | — | — | 6 | **6** |
| `shared-testing` | — | — | — | — | — | 89 | **89** |
| `shared-testing-e2e` | — | — | — | — | 14 | — | **14** |
| `shared-tool-kit` | — | — | — | — | — | 19 | **19** |
| `studio-app` | — | — | — | — | — | 15 | **15** |
| `studio-data-client` | — | — | — | — | 20 | — | **20** |
| `studio-event-bus` | — | — | — | — | — | 9 | **9** |
| `studio-federation` | — | — | — | — | — | 31 | **31** |
| `telemetry-client` | 12 | — | — | — | — | — | **12** |
| `workflow-contracts` | — | — | — | — | — | 65 | **65** |
| `workflow-daemon` | — | — | — | 43 | 5 | 12 | **60** |
| `workflow-engine` | — | — | — | — | — | 49 | **49** |
| `workflow-entry` | 22 | — | — | — | — | — | **22** |
| `workflow-runtime` | — | — | — | — | — | 55 | **55** |
| `workflow-steps` | — | — | — | — | — | 44 | **44** |
| **Total** | **206** | **11** | **45** | **79** | **1394** | **3334** | **5069** |

## Test Cases by Package

### `@kb-labs/adapters-analytics-file`

#### E2E / journey tests

**`adapters/analytics-file/src/__tests__/daily-stats.spec.ts`**

- **FileAnalytics - getDailyStats**
- **LLM events**
  - should aggregate LLM events by day with correct metrics
  - should filter by date range
  - should return empty array when no events match
- **Embeddings events**
  - should aggregate embeddings events with correct metrics
- **VectorStore events**
  - should count operations correctly
- **Cache events**
  - should calculate hit rate correctly
  - should handle zero gets gracefully
- **Storage events**
  - should aggregate bytes read and written
- **Sorting**
  - should sort results by date ascending
- **Empty metrics**
  - should not include metrics object for unknown event types

#### Unit tests

**`adapters/analytics-file/src/__tests__/scoped-analytics.test.ts`**

- **Analytics Source Attribution**
- **Default behavior (current implementation)**
  - should use source from AnalyticsContext
  - should use default source if no context provided
- **Multiple plugins tracking events**
  - should track events from different plugins with same analytics instance
- **Event schema validation**
  - should create events with kb.v1 schema
- **getEvents filtering by source**
  - should filter events by source.product
  - should return empty when filtering for non-existent source
- **getStats aggregation by source**
  - should aggregate events by source

**`adapters/analytics-file/src/index.test.ts`**

- **FileAnalytics - Manifest-based Context Injection**
- **Context Priority - options.workspace and options.analytics**
  - should use options.workspace when provided (manifest-based injection)
  - should use options.analytics when provided (manifest-based injection)
  - should fall back to process.cwd() when no cwd provided
  - should use "unknown" source when no analytics context provided
- **Real-world Scenario - REST API**
  - should work as in REST API with manifest-based injection

### `@kb-labs/adapters-environment-docker`

#### Unit tests

**`adapters/environment-docker/src/index.test.ts`**

- **DockerEnvironmentAdapter**
  - creates environment via docker run with expected args
  - maps docker running state to ready
  - uses runtimeCommand when request command is omitted
  - uses image default when no command and no runtimeCommand
  - returns terminated for missing containers on status
  - destroy is idempotent for missing containers
  - renews lease with requested ttl
  - returns provider capabilities

### `@kb-labs/adapters-eventbus-cache`

#### Unit tests

**`adapters/eventbus-cache/src/index.test.ts`**

- **CacheEventBusAdapter**
- **publish**
  - should store event in cache
  - should store multiple events in order
- **subscribe**
  - should receive published events via polling
  - should return unsubscribe function
  - should support multiple subscribers on same topic
  - should not receive events from other topics
- **disconnect**
  - should stop all subscriptions
- **createAdapter**
  - should create adapter with default config
  - should create adapter with custom config

### `@kb-labs/adapters-fs`

#### Unit tests

**`adapters/fs/src/index.test.ts`**

- **FilesystemStorageAdapter**
- **Basic Operations**
  - should write and read a file
  - should return null when reading nonexistent file
  - should write file in nested directory
  - should check if file exists
  - should delete a file
  - should not throw when deleting nonexistent file
- **List Operations**
  - should list files with prefix
  - should return empty array when no files match prefix
- **Security**
  - should prevent directory traversal attacks
  - should prevent absolute path escaping baseDir
- **Extended Methods - stat()**
  - should return file metadata
  - should return null for nonexistent file
  - should detect content types correctly
  - should return octet-stream for unknown extensions
- **Extended Methods - copy()**
  - should copy file
  - should copy to nested directory
- **Extended Methods - move()**
  - should move file
  - should move to nested directory
  - should overwrite existing file when moving
- **Extended Methods - listWithMetadata()**
  - should list files with metadata
  - should return empty array when no files match
  - should include correct content types

**`adapters/fs/src/secure-storage.test.ts`**

- **SecureStorageAdapter**
- **Operation Permissions**
  - should allow read when permission granted
  - should deny read when permission not granted
  - should allow write when permission granted
  - should deny write when permission not granted
  - should allow delete when permission granted
  - should deny delete when permission explicitly set to false
- **Path Allowlist**
  - should allow access to allowlisted paths
  - should deny access to non-allowlisted paths
  - should work with empty allowlist (allow all)
  - should work with undefined allowlist (allow all)
- **Path Denylist**
  - should deny access to denylisted paths
  - should allow access to non-denylisted paths
  - should give denylist precedence over allowlist
- **List Operations with Permissions**
  - should list files respecting permissions
  - should deny list for non-allowlisted paths
- **Exists with Permissions**
  - should check existence when allowed
  - should return false for denied paths (security by obscurity)
- **Extended Methods with Permissions**
  - should allow stat when permitted
  - should deny stat when not permitted
  - should allow copy when both paths permitted
  - should deny copy when source not permitted
  - should deny copy when destination not permitted
  - should allow move when both paths permitted
  - should allow listWithMetadata when permitted
- **Complex Permission Scenarios**
  - should handle multiple allowlist patterns
  - should handle multiple denylist patterns
  - should combine allowlist and denylist correctly
  - should handle read-only permissions

### `@kb-labs/adapters-log-ringbuffer`

#### Unit tests

**`adapters/log-ringbuffer/src/__tests__/ring-buffer.test.ts`**

- **LogRingBufferAdapter**
- **append**
  - should append logs to buffer
  - should evict oldest log when buffer is full
  - should notify subscribers when log is appended
  - should handle subscriber errors gracefully
- **query**
  - should return all logs without filters
  - should filter by level
  - should filter by source
  - should filter by timestamp range
  - should apply limit
  - should combine multiple filters
- **subscribe**
  - should allow multiple subscribers
  - should unsubscribe correctly
- **getStats**
  - should return correct stats
  - should return zeros for empty buffer
- **clear**
  - should clear all logs
- **TTL eviction**
  - should evict expired logs based on TTL
  - should evict multiple expired logs
- **edge cases**
  - should handle zero maxSize
  - should handle negative timestamps
  - should handle empty fields
  - should handle complex fields

### `@kb-labs/adapters-log-sqlite`

#### Unit tests

**`adapters/log-sqlite/src/__tests__/log-persistence.test.ts`**

- **LogSQLitePersistence**
- **write**
  - should write log to database
  - should flush when batch size is reached
  - should preserve log fields
  - should handle empty fields
  - should generate ID if not provided
  - should preserve custom ID
- **writeBatch**
  - should write multiple logs in batch
  - should handle empty batch
- **query**
  - should return all logs without filters
  - should filter by level
  - should filter by source
  - should filter by timestamp range
  - should apply limit
  - should apply offset
  - should sort by timestamp ascending
  - should combine multiple filters
  - should handle pagination correctly
- **getById**
  - should retrieve log by ID
  - should return null for non-existent ID
- **search**
  - should search logs by text
  - should search with multiple terms
  - should apply limit to search results
  - should return empty results for no matches
  - should handle pagination in search
  - should fallback to phrase search for hyphenated plain text
  - should auto-rebuild stale FTS index when count and rows diverge
- **deleteOlderThan**
  - should delete logs older than timestamp
  - should return 0 if no logs deleted
  - should delete all logs if timestamp is in future
- **getStats**
  - should return stats for empty database
  - should return correct stats
- **close**
  - should flush pending logs on close
- **transaction support**
  - should rollback on error
- **edge cases**
  - should handle very long messages
  - should handle special characters in message
  - should handle unicode characters
  - should handle negative timestamps

### `@kb-labs/adapters-pino`

#### Unit tests

**`adapters/pino/src/index.test.ts`**

- **PinoLoggerAdapter**
  - includes child bindings in onLog records
  - propagates nested child bindings in onLog records

### `@kb-labs/adapters-snapshot-localfs`

#### Unit tests

**`adapters/snapshot-localfs/src/index.test.ts`**

- **LocalFsSnapshotAdapter**
  - captures and restores workspace snapshot

### `@kb-labs/adapters-sqlite`

#### Unit tests

**`adapters/sqlite/src/index.test.ts`**

- **SQLiteAdapter**
- **Basic Operations**
  - should create a table
  - should insert and select data
  - should update data
  - should delete data
- **Transactions**
  - should commit a transaction
  - should rollback a transaction
  - should not allow operations after commit
  - should not allow operations after rollback
- **Query Results**
  - should return field metadata
  - should return rowCount
- **Error Handling**
  - should throw on invalid SQL
  - should throw when using after close
- **In-Memory Database**
  - should support in-memory database

**`adapters/sqlite/src/secure-sql.test.ts`**

- **SecureSQLAdapter**
- **Permission Checks**
  - should allow read when permission granted
  - should deny read when permission not granted
  - should allow write when permission granted
  - should deny write when permission not granted
  - should allow delete when permission granted
  - should deny delete when permission not granted
- **Table Allowlist**
  - should allow access to allowlisted tables
  - should deny access to non-allowlisted tables
  - should handle joins with allowlisted tables
- **Table Denylist**
  - should deny access to denylisted tables
  - should denylist take precedence over allowlist
- **Schema Modification Prevention**
  - should deny CREATE TABLE when schema=false
  - should deny ALTER TABLE when schema=false
  - should deny DROP TABLE when schema=false
  - should allow schema modifications when schema=true
- **Transactions with Permissions**
  - should enforce permissions in transactions
  - should allow valid operations in transactions

### `@kb-labs/adapters-transport`

#### Unit tests

**`adapters/transport/src/__tests__/unix-socket-server.test.ts`**

- **UnixSocketServer**
- **Server Lifecycle**
  - should start server and create socket file
  - should remove existing socket file on start
  - should clean up socket file on close
  - should use default socket path if not specified
- **Client Connections**
  - should accept client connections
  - should handle multiple concurrent clients
  - should clean up client on disconnect
- **Adapter Call Handling**
  - should execute adapter call and return result
  - should handle errors in adapter call handler
  - should handle non-Error exceptions
  - should preserve error stack traces
- **Message Protocol**
  - should parse newline-delimited JSON messages
  - should ignore empty lines
  - should handle malformed JSON gracefully

### `@kb-labs/adapters-workspace-agent`

#### Unit tests

**`adapters/workspace-agent/src/__tests__/adapter.test.ts`**

- **AgentWorkspaceAdapter**
- **materialize**
  - calls /internal/dispatch with filesystem.fetchWorkspace
  - writes fetched files to cacheDir/workspaceId/
  - generates workspaceId when not provided
  - stores file count in descriptor metadata
  - throws when dispatch returns non-array
  - throws when fetch returns non-ok status
  - passes hostId when configured
  - uses basePath for remotePath
- **attach / getStatus / release**
  - attach returns mountPath
  - getStatus returns ready after materialize
  - getStatus throws for unknown workspace
  - release transitions status to released
- **getCapabilities**
  - reports remote:true
- **createAdapter factory**
  - exports createAdapter default function

### `@kb-labs/adapters-workspace-localfs`

#### Unit tests

**`adapters/workspace-localfs/src/index.test.ts`**

- **LocalFsWorkspaceAdapter**
  - materializes and attaches workspace

### `@kb-labs/adapters-workspace-worktree`

#### Unit tests

**`adapters/workspace-worktree/src/index.test.ts`**

- **WorktreeWorkspaceAdapter**
  - should export createAdapter factory
  - should create adapter with default config
  - should return capabilities
  - should return released status for unknown workspace
  - should reject invalid branch names

### `@kb-labs/agent-core`

#### Unit tests

**`plugins/agents/core/src/agents/__tests__/agent-registry.test.ts`**

- **AgentRegistry**
- **built-in presets**
  - has researcher preset
  - has coder preset with write access
  - has reviewer preset (read-only)
  - has orchestrator preset with maxDepth 3
  - lists 4 built-in presets
- **register()**
  - registers a custom definition
  - overrides an existing preset
  - throws when id is empty
- **get() and getOrThrow()**
  - returns undefined for unknown id
  - throws for unknown id on getOrThrow()
  - lists available ids in error message
- **has()**
  - returns true for registered id
  - returns false for unknown id
- **resolveFeatureFlags()**
  - merges DEFAULT_FEATURE_FLAGS with preset overrides
  - returns default flags for unknown id
  - coder has todoSync and reflection enabled
- **list()**
  - returns all definitions

**`plugins/agents/core/src/agents/__tests__/orchestrator.test.ts`**

- **SubAgentOrchestrator**
  - wraps runner error in success=false result
- **SubAgentOrchestrator > spawnOne()**
  - spawns a single sub-agent and returns legacy result
- **spawnMany() — sequential**
  - runs requests in sequence by default
  - returns all results
- **spawnMany() — parallel**
  - spawns requests concurrently
- **resolveAgentType()**
  - resolves from the registry
  - returns undefined for unknown type
  - uses custom registry when provided
- **stats()**
  - returns executor stats

**`plugins/agents/core/src/agents/__tests__/parallel-executor.test.ts`**

- **ParallelExecutor**
- **basic execution**
  - runs a single request and returns result
  - returns empty array for empty request list
  - executes multiple requests
- **parallel execution (3 agents simultaneously)**
  - runs up to maxConcurrent agents in parallel
- **cancel tree**
  - returns abort error for all pending requests when parent aborts
  - immediately returns abort error when signal is already aborted
- **budget partition**
  - equal partition splits budget evenly
  - weighted partition distributes proportionally
  - passes 0 budget when parentTokenBudget is 0 (unlimited)
- **dedupe**
  - same task submitted twice shares one execution
  - different dedupeKey submits two executions
- **maxDepth**
  - rejects all requests when depth exceeds maxDepth
  - allows requests at exactly maxDepth
- **backpressure**
  - rejects requests when queue is full
- **join timeout**
  - marks result as timedOut when agent takes too long
  - collects fast results even when some time out
- **stats()**
  - returns running/queued/deduped counts

**`plugins/agents/core/src/analytics/__tests__/run-metrics-emitter.test.ts`**

- **getKpiBaselineKey**
  - uses sessionRootDir when available
  - falls back to workingDir
  - falls back to workspace when both empty
- **extractToolErrorCode**
  - extracts from errorDetails.code
  - extracts from metadata.errorCode
  - extracts from error string prefix
  - returns null when no code found
  - returns null for success with no details
  - ignores blank metadata.errorCode
  - prefers errorDetails.code over metadata
- **detectQualityRegression**
  - does not regress with too few samples
  - detects drift rate regression
  - detects evidence density regression
  - detects tool error rate regression
  - detects partial quality near budget limit
  - does not regress when metrics are within thresholds
  - can detect multiple regressions at once
- **updateKpiBaseline**
  - initializes EMA on first sample
  - applies EMA on subsequent samples
  - appends to token history
  - caps history at 50 items
  - increments sample count
  - appends to iterationUtilizationHistory
  - appends to qualityScoreHistory
- **RunMetricsEmitter**
- **recordTierEscalation**
  - pushes escalation to array
  - emits analytics event when analytics available
  - silently handles analytics error
- **trackToolOutcome**
  - skips when no analytics
  - fires event when analytics available
- **emitRunKpis**
  - skips when no analytics
  - fires run_completed event
  - calculates derived metrics correctly
- **reset**
  - clears tier escalations

**`plugins/agents/core/src/execution/__tests__/linear-execution-loop.test.ts`**

- **LinearExecutionLoop**
  - report wins over other tool calls in same response
- **LinearExecutionLoop > report tool**
  - stops on report tool call
- **no tool calls**
  - stops naturally when LLM returns no tool calls
  - does not complete when forced report after nudge is blocked
- **abort**
  - stops immediately when abort signal is fired
  - stops when run.aborted is set
- **hard budget**
  - stops when BudgetMiddleware sets budget.exhausted in meta
- **max iterations**
  - stops at maxIterations
- **loop detection**
  - stops early when same intent repeats without new evidence
  - detects repeated tool call pattern
- **LLM error**
  - returns failure on LLM call error
- **token tracking**
  - accumulates tokens and stores in meta
- **iteration counter**
  - increments run.iteration each iteration
- **run evaluation**
  - stores evaluation output in run meta
  - nudges synthesis when evaluator says evidence gain has flattened

**`plugins/agents/core/src/execution/__tests__/loop.test.ts`**

- **ExecutionLoop**
- **ExecutionLoop > natural stop (no tool calls)**
  - returns complete when LLM responds with no tool calls
  - runs multiple iterations before natural stop
- **report tool**
  - stops immediately when report tool is present (even at iteration 1)
  - report takes priority over max iterations on last iteration
- **abort signal**
  - stops immediately on abort
  - respects abort signal mid-loop (on next iteration)
- **hard token limit**
  - stops before LLM call when limit is exceeded
- **max iterations**
  - returns complete with max_iterations after exhausting budget
- **loop detection**
  - stops with loop_detected when detectLoop returns a reason
- **tier escalation**
  - returns escalate when evaluateEscalation says shouldEscalate
  - does not escalate when evaluateEscalation returns null
- **budget extension**
  - extends budget when extendBudget returns higher value
- **error handling**
  - catches LLM errors and returns complete with error code
  - catches tool execution errors without crashing the loop
- **hooks (onIterationStart, onIterationEnd, onStop)**
  - calls onIterationStart and onIterationEnd for each iteration
  - calls onStop with the stop condition when stopping
- **token tracking**
  - passes accumulated token count to callLLM via totalTokens

**`plugins/agents/core/src/execution/__tests__/stop-conditions.test.ts`**

- **StopConditionEvaluator**
  - detects abort signal
- **StopConditionEvaluator > individual conditions**
  - returns null when no conditions fire
  - detects report tool call
  - detects hard budget exceeded
  - does not fire hard budget when limit is 0 (disabled)
  - detects max iterations reached
  - does not fire max iterations when not at last
  - detects loop
  - detects no tool calls (implicit completion)
  - detects no tool calls when toolCalls is undefined
- **priority ordering**
  - report beats max-iterations (THE BUG FIX)
  - report beats hard budget (collision test)
  - abort signal beats everything
  - hard budget beats max-iterations
  - max-iterations beats loop detected
- **functional API**
  - evaluateStopConditions works the same as class
- **report with other tool calls**
  - detects report even when mixed with other tool calls

**`plugins/agents/core/src/middleware/__tests__/pipeline.test.ts`**

- **MiddlewarePipeline**
  - runs after-hooks in descending order
- **MiddlewarePipeline > ordering**
  - runs before-hooks in ascending order
- **enabled() gating**
  - skips middleware when enabled() returns false
  - runs middleware when enabled() returns true
- **fail-open policy**
  - continues pipeline when middleware throws with fail-open
- **fail-closed policy**
  - stops pipeline when middleware throws with fail-closed
- **timeout enforcement**
  - times out slow middleware with fail-open
  - times out slow middleware with fail-closed
- **beforeIteration — ControlAction**
  - returns first non-continue action
  - returns escalate action
- **beforeLLMCall — LLMCallPatch merging**
  - merges patches from multiple middlewares
  - returns empty patch when no middleware modifies
- **beforeToolExec — skip gate**
  - returns skip if any middleware says skip
  - returns execute if all allow
- **afterToolExec**
  - calls all middlewares afterToolExec in descending order
- **onStop**
  - calls all middlewares onStop in descending order

**`plugins/agents/core/src/middleware/builtin/__tests__/analytics-middleware.test.ts`**

- **AnalyticsMiddleware**
  - tracks tool outcomes
  - emits run metrics on stop
  - resets state

**`plugins/agents/core/src/middleware/builtin/__tests__/budget-middleware.test.ts`**

- **BudgetMiddleware**
  - returns stop when hard limit exceeded
- **BudgetMiddleware > beforeIteration**
  - returns continue when under budget
  - returns continue when policy inactive
  - sets forceSynthesis meta when forceSynthesisOnHardLimit is true and hardStop is false
- **beforeLLMCall — convergence nudge**
  - injects nudge at soft limit
  - does NOT nudge twice
  - does NOT nudge when under soft limit
- **reset**
  - resets convergence nudge flag

**`plugins/agents/core/src/middleware/builtin/__tests__/change-tracking-middleware.test.ts`**

- **ChangeTrackingMiddleware**
  - has correct name, order, and fail-open policy
  - onStart captures requestId as runId
- **afterToolExec — ignored cases**
  - skips failed tool results
  - skips non-tracked tools
  - skips when changeSnapshot is missing from metadata
  - skips when sessionId is absent
- **afterToolExec — fs_write**
  - captures a new file write and adds summary to meta
  - captures an overwrite and marks isNew=false
- **afterToolExec — fs_patch**
  - captures patch with line metadata
- **afterToolExec — multiple changes**
  - accumulates summaries across multiple tool calls
- **changeStore getter**
  - exposes the underlying store for external queries
- **rollbackRun**
  - restores files written in the run
  - deletes files that were newly created by the agent
- **getFileChangeSummaries**
  - returns empty array when nothing was set

**`plugins/agents/core/src/middleware/builtin/__tests__/experimental-middlewares.test.ts`**

- **SearchSignalMiddleware**
  - is gated by feature flag
  - tracks search signal hits
  - tracks no-result streak
  - ignores non-search tools
  - resets on stop
- **TodoSyncMiddleware**
  - is gated by feature flag
  - injects todo nudge at iteration 2+
  - does not nudge twice
  - calls onFinalize on stop
- **ReflectionMiddleware**
  - is gated by feature flag
  - triggers reflection after N tool calls
  - triggers reflection on high failure rate
- **TaskClassifierMiddleware**
  - is gated by feature flag
  - classifies action tasks
  - classifies discovery tasks
  - classifies analysis tasks
  - defaults to action for ambiguous tasks
  - stores classification in context meta

**`plugins/agents/core/src/middleware/builtin/__tests__/progress-middleware.test.ts`**

- **ProgressMiddleware**
  - detects stuck state
- **ProgressMiddleware > stuck detection**
  - increments iterationsSinceProgress each iteration
  - resets counter on successful tool output
- **loop detection**
  - detects repeating tool call pattern
  - does not false-positive on different calls
- **reset**
  - resets all state

**`plugins/agents/core/src/modes/__tests__/mode-registry.test.ts`**

- **ModeRegistry**
  - throws on duplicate registration without override
- **ModeRegistry > register()**
  - registers a custom mode
  - allows override with { override: true }
  - throws for empty mode name
  - throws for whitespace-only mode name
  - supports async factory
- **built-in modes**
  - has execute registered by default
  - has plan registered by default
  - has edit registered by default
  - has debug registered by default
  - has spec registered by default
  - lists all 5 built-in modes
  - can override a built-in mode
- **get()**
  - falls back to execute when mode is not found
  - throws when mode and execute fallback both missing
- **list()**
  - returns all registered mode names
- **global singleton**
  - modeRegistry is a ModeRegistry instance
  - getModeHandlerFromRegistry falls back to execute for unknown mode

**`plugins/agents/core/src/modes/__tests__/plan-profile.test.ts`**

- **createPlanRuntimeProfile**
  - provides a blocking validator for incomplete plans
  - maps final markdown into TaskResult.plan through the profile result mapper
  - writes plan artifacts through the profile artifact writer
  - uses a scoped tool policy for narrow planning tasks
  - keeps delegation tools for broad complex planning tasks

**`plugins/agents/core/src/planning/__tests__/plan-document-service.test.ts`**

- **PlanDocumentService**
  - createDraft writes a plan with TOC and valid heading links
  - reviseDraft recalculates TOC when sections are changed
  - refreshToc includes all phases for long plans (10+)
  - refreshToc is idempotent and does not duplicate TOC blocks
  - createDraft prefers plan markdown body when provided

**`plugins/agents/core/src/planning/__tests__/plan-generator.test.ts`**

- **PlanGenerator**
  - uses tool-driven two-step flow: plan_research then plan_generate
  - updates existing plan through plan_update tool and keeps plan identity
  - re-generates refactoring plans when first draft is audit-heavy
  - keeps one-shot draft when quality issues are non-critical
  - supports markdown-first plan tool output and derives executable phases from markdown headings
  - builds fallback executable phases when markdown output is placeholder-only
  - derives phases from markdown that uses ## Phase headings
  - synthesizes executable phases from refactor changeSets when phases are missing
  - emits detailed tracing events for research, generation, and update
  - propagates requested tier to llm initialization and llm:start events

**`plugins/agents/core/src/planning/__tests__/plan-validator.test.ts`**

- **PlanValidator**
  - should fail on generic template without file paths
- **PlanValidator > validate()**
  - should fail on empty markdown
  - should pass on a well-crafted plan with file paths and commands
  - should score specificity based on unique file paths
  - should score actionability based on steps with file paths or commands
  - should score completeness based on section substance
  - should detect missing verification commands
  - should find commands outside verification section as fallback
  - should return error issues for plans with no steps

**`plugins/agents/core/src/planning/__tests__/spec-validator.test.ts`**

- **SpecValidator**
  - should fail on spec with no sections
- **SpecValidator > validate()**
  - should pass on a well-formed spec with full coverage
  - should fail on spec with no code changes
  - should warn on partial phase coverage
  - should reduce precision score when before is empty
  - should score files based on valid paths
- **validateMarkdown()**
  - should fail on empty markdown
  - should pass on well-formed spec markdown
  - should fail on markdown with no code blocks
  - should fail when step coverage is too low even with diff blocks
  - should expose uncovered step metrics

**`plugins/agents/core/src/prompt/__tests__/system-prompt-builder.test.ts`**

- **SystemPromptBuilder**
  - builds a prompt containing core rules
  - includes delegation section for main agents
  - excludes delegation section for sub-agents
  - includes session continuity note when session present
  - includes workspace section when discovery present
  - does not include workspace section when no repos
  - includes memory context when memory provided
  - static prompt is cached between builds
- **loadProjectInstructions**
  - returns null when no instruction files exist
  - returns content of first found instruction file
  - skips empty files

**`plugins/agents/core/src/tools/__tests__/context-retrieve.test.ts`**

- **context_retrieve tool**
- **Tool Definition**
  - should create valid LLM tool definition
- **Filtering by Iteration**
  - should retrieve messages by iteration number
  - should return empty for non-existent iteration
- **Filtering by Tool Call ID**
  - should retrieve message by tool_call_id
  - should return empty for non-existent tool_call_id
- **Filtering by Tool Name**
  - should retrieve messages by tool name
  - should only return tool messages
- **Filtering by Topic**
  - should retrieve messages by topic (case-insensitive)
  - should perform case-insensitive search
  - should search in tool outputs
- **Combined Filters**
  - should combine iteration and tool_name filters
  - should combine topic and iteration filters
- **Truncation Removal**
  - should remove truncation metadata
- **Validation**
  - should require at least one filter
- **Result Formatting**
  - should format successful result
  - should format error result
  - should format empty result
  - should truncate long content in preview
  - should show tool name in formatted output

**`plugins/agents/core/src/tools/__tests__/core-tool-pack.test.ts`**

- **CoreToolPack**
  - wraps all legacy tools
  - marks read-only tools correctly
  - assigns capability categories
  - delegates execution to legacy registry
  - integrates with ToolManager

**`plugins/agents/core/src/tools/__tests__/tool-manager.test.ts`**

- **ToolManager**
  - skips disabled packs
- **ToolManager > registration**
  - registers a pack and its tools
  - throws on duplicate pack id
- **conflict resolution**
  - throws on name conflict with conflictPolicy "error"
  - namespace-prefix: both tools available as qualified names
  - override: higher priority wins
  - no conflict when tools have different names
- **getTools / getDefinitions**
  - filters by readOnly
  - filters by capability
  - filters by namespace
  - getDefinitions returns tool definitions
  - getToolNames returns sorted names
- **execute**
  - executes a registered tool
  - returns error for unknown tool
- **permissions**
  - blocks denied commands
  - allows non-denied commands
  - blocks paths outside allowedPaths
  - allows paths within allowedPaths
- **audit trail**
  - calls onAudit when pack has auditTrail enabled
  - does not call onAudit when auditTrail is false
- **lifecycle**
  - calls initialize on all packs
  - calls dispose on all packs

**`plugins/agents/core/tests/tracer/incremental-trace-writer.test.ts`**

- **IncrementalTraceWriter**
  - should create NDJSON file on trace
  - should auto-increment seq numbers
  - should flush on buffer size limit
  - should redact secrets
  - should redact paths
  - should create index on finalize
  - should cleanup old traces

### `@kb-labs/agent-history`

#### Unit tests

**`plugins/agents/history/src/__tests__/change-store.test.ts`**

- **ChangeStore**
- **save**
  - saves a new file write and returns correct FileChange
  - saves an overwrite with before content
  - saves a patch with line metadata
- **get**
  - returns the change from in-memory cache
  - returns the change from disk when not in cache
  - returns null for unknown changeId
- **listSession**
  - returns all changes in the session in chronological order
  - returns empty array for unknown session
- **listRun**
  - filters changes by runId
- **listFile**
  - filters changes by filePath
- **changedFiles**
  - returns unique file paths
- **summariesForRun**
  - returns FileChangeSummary objects with no before/after content
- **deleteSession**
  - removes all snapshots from disk
- **toSummary**
  - produces a summary without content

**`plugins/agents/history/src/__tests__/snapshot-storage.test.ts`**

- **SnapshotStorage**
- **saveSnapshot**
  - should save snapshot to disk
  - should create session directories if they do not exist
  - should store complete change data
  - should update index with file change
- **loadSnapshot**
  - should load saved snapshot
  - should return null for non-existent snapshot
  - should return null for non-existent session
- **listSnapshots**
  - should return all snapshots for session
  - should return empty array for non-existent session
  - should return snapshots in chronological order
- **cleanupOldSessions**
  - should delete sessions older than maxAgeDays
  - should keep only maxSessions most recent sessions
  - should respect custom retention policy
- **storage size limits**
  - should enforce maxTotalSizeMb limit
- **error handling**
  - should handle corrupted snapshot files gracefully
  - should handle missing index file gracefully

### `@kb-labs/agent-kernel`

#### Unit tests

**`plugins/agents/kernel/src/__tests__/kernel.test.ts`**

- **agent-kernel**
  - invalidates assumptions via corrections
  - stores decisions, evidence, and working summary
  - records baseline constraints directly without creating a pending commit

### `@kb-labs/agent-mcp`

#### Unit tests

**`plugins/agents/mcp/src/__tests__/mcp-agent-server.test.ts`**

- **MCPAgentServer**
- **run_task tool**
  - calls runTask callback and returns session_id
  - uses custom mode when provided
  - rejects empty task
- **get_session tool**
  - returns session data
  - returns error when session not found
- **get_plan tool**
  - returns plan markdown
  - returns error when no plan available
- **approve_plan tool**
  - calls approvePlan and returns approved: true
- **cancel_task tool**
  - calls cancelTask and returns cancelled: true
- **unknown tool**
  - returns isError for unknown tool names
- **rate limiting**
  - blocks requests exceeding maxRequestsPerMinute
  - does not rate limit when maxRequestsPerMinute is 0
- **list tools**
  - lists 5 tools
- **list prompts**
  - lists 2 prompts
- **get prompt**
  - execute-task prompt mentions run_task
  - plan-task prompt mentions approve_plan
  - unknown prompt throws

**`plugins/agents/mcp/src/__tests__/mcp-tool-pack.test.ts`**

- **MCPToolPack**
- **MCPToolPack > connect()**
  - connects via stdio and discovers tools
  - is idempotent — second connect() is a no-op
  - sets namespace as mcp.<name>
  - throws if stdio transport has no command
  - throws if sse transport has no url
- **allowlist**
  - filters tools by allowedTools list
  - exposes all tools when allowedTools is empty
- **execute()**
  - calls MCP server and returns output
  - returns error result when not connected
- **audit + redaction**
  - calls onAudit before execution
  - redacts specified input fields in audit log
  - redacts output patterns
- **dispose()**
  - disconnects and clears tools
- **callbacks**
  - calls onConnected with tool count after connect

### `@kb-labs/agent-runtime`

#### Unit tests

**`plugins/agents/runtime/src/__tests__/default-prompt-context-selector.test.ts`**

- **createDefaultPromptContextSelector**
  - prefers evidence/tool usage over working summary for recall-like follow-ups
  - uses response requirements to force recall-oriented context selection

**`plugins/agents/runtime/src/__tests__/default-turn-interpreter.test.ts`**

- **createDefaultTurnInterpreter**
  - uses a structured LLM pass when available
  - preserves explicit persist strategy from the LLM response
  - falls back to heuristic interpretation when the LLM step fails

**`plugins/agents/runtime/src/__tests__/runtime-engine.test.ts`**

- **RuntimeEngine**
  - persists kernel, turns, tool evidence and run handoff in canonical session artifacts
  - creates a compacted session rollup for long sessions without losing prompt continuity
  - can resolve direct file recall from persisted tool evidence without rereading files
  - supports custom session recall resolvers via the SDK extension surface
  - resolves and applies runtime profiles for prompt selectors, projectors, and evaluators
  - runs profile-scoped completion validators and artifact writers in completeRun
  - builds repository diagnostics from probes without assuming a node-only repository
  - merges repository probes through the default diagnostics provider

### `@kb-labs/agent-store`

#### Unit tests

**`plugins/agents/store/src/__tests__/store.test.ts`**

- **SessionArtifactStore**
  - creates canonical artifacts and persists kernel snapshots

### `@kb-labs/agent-tools`

#### Unit tests

**`plugins/agents/tools/src/__tests__/config.test.ts`**

- **FILESYSTEM_CONFIG**
  - has sensible limits
- **SEARCH_CONFIG**
  - has sensible limits
  - defaultExcludes includes common noise dirs
- **SHELL_CONFIG**
  - maxBuffer is a positive number
- **TODO_CONFIG**
  - cacheTtlMs is 7 days
  - cachePrefix has agent namespace
- **DELEGATION_CONFIG**
  - defaultMaxIterations is positive
- **SOURCE_FILE_EXTENSIONS**
  - covers TypeScript and JavaScript
  - covers common backend languages
- **ALL_SOURCE_EXTENSIONS**
  - is flat list including ts, py, go
  - has no duplicates
- **toRgIncludes**
  - builds --include flags for ripgrep
  - handles single extension
  - handles empty array
- **toFindNames**
  - builds -name flags joined with -o for find
  - handles single extension
  - handles empty array

**`plugins/agents/tools/src/__tests__/gateway.test.ts`**

- **ToolGateway**
  - filters definitions through policies and returns structured artifacts
  - filters tools through capability policies without hardcoding tool names

**`plugins/agents/tools/src/__tests__/registry.test.ts`**

- **ToolRegistry**
  - should register and retrieve a tool
  - should return undefined for unknown tool
  - should return definitions of all registered tools
  - should return sorted tool names
  - should execute a registered tool
  - should return error result when executing unknown tool
  - should return context from constructor
  - should overwrite tool when registering with same name

**`plugins/agents/tools/src/__tests__/utils.test.ts`**

- **normalizeOffsetLimit**
  - returns defaults when no input provided
  - parses valid offset and limit
  - floors non-integer values
  - clamps limit to maxLimit
  - treats negative offset as 0
  - treats zero limit as default
  - treats negative limit as default
  - treats NaN offset as 0
  - treats NaN limit as default
  - treats undefined fields as defaults
  - uses caller-provided defaultLimit and maxLimit

**`plugins/agents/tools/src/tools/__tests__/archive-recall.test.ts`**

- **archive_recall tool**
- **archive_recall tool > file_path mode**
  - should return full content of last read
  - should return error for non-archived file
- **keyword mode**
  - should return matched results
  - should handle no results
- **list_files mode**
  - should list all archived file paths
  - should handle empty archive
- **tool_name mode**
  - should return recent outputs of a specific tool
- **iteration mode**
  - should return all outputs from specific iteration
- **edge cases**
  - should return error when no archiveMemory in context
  - should return error when no query parameters provided

**`plugins/agents/tools/src/tools/__tests__/filesystem-change-snapshot.test.ts`**

- **fs_write — changeSnapshot in metadata**
  - includes changeSnapshot with operation=write and no beforeContent for new file
  - includes changeSnapshot with beforeContent for overwrite
  - does NOT include changeSnapshot when write fails (path validation)
- **fs_patch — changeSnapshot in metadata**
  - includes changeSnapshot with operation=patch and line metadata
  - does NOT include changeSnapshot when patch fails (unread file)

**`plugins/agents/tools/src/tools/__tests__/filesystem.test.ts`**

- **path traversal protection**
  - fs_read blocks paths outside workingDir
  - fs_write blocks paths outside workingDir
- **fs_read**
  - returns FILE_NOT_FOUND when file does not exist
  - returns NOT_A_FILE when path is a directory
  - returns FILE_TOO_LARGE for oversized files
  - reads file and returns content
  - returns OFFSET_OUT_OF_RANGE when offset exceeds file length
  - includes line numbers in output
- **fs_write**
  - writes file and returns success
  - rejects content larger than maxWriteSize
  - write succeeds even without prior read (no edit protection on fs_write)
- **fs_patch**
  - blocks editing a file that was not read (edit protection)
- **fs_list**
  - returns DIRECTORY_NOT_FOUND for non-existent directory
  - returns entries for existing directory
  - returns NOT_A_DIRECTORY when path is a file

**`plugins/agents/tools/src/tools/__tests__/mass-replace.test.ts`**

- **mass_replace — validation**
  - returns error when pattern is missing
  - returns error when scope is missing
  - returns error when files pattern is missing
  - returns error for unknown mode
- **mass_replace — path traversal**
  - blocks scope outside workingDir
- **mass_replace — scope validation**
  - returns error when scope directory does not exist
  - returns error when scope is not a directory
- **mass_replace — no matches**
  - returns success with zero counts when no files match
- **mass_replace — file count limit**
  - returns error when glob matches more than MAX_FILES (100)
- **mass_replace — literal mode**
  - replaces literal string in matching files
  - does not write when no replacements occur
  - dry-run does not write file
- **mass_replace — regex mode**
  - applies regex replacement
  - supports caseInsensitive flag
  - returns error for invalid regex
- **mass_replace — tool definition**
  - has correct name and required parameters

**`plugins/agents/tools/src/tools/__tests__/reporting.test.ts`**

- **createReportTool**
  - blocks report when claim verifier finds unsupported code claims
  - allows report when verifier marks shell recall as supported
  - allows file recall from existing fs_read evidence without forcing reread

**`plugins/agents/tools/src/tools/__tests__/search.test.ts`**

- **directory validation**
  - should return error when directory does not exist (glob_search)
  - should return error when directory does not exist (grep_search)
  - should return error when directory does not exist (find_definition)
  - should return error when directory does not exist (code_stats)
  - should show resolved path in error
  - should filter hidden dirs and node_modules from hints
  - should not validate when directory is "."
- **glob_search**
  - should return found files
  - should return "No files found" with hint on empty output
  - should handle timeout
  - should handle general errors
  - should include default excludes in args
  - should use custom excludes when provided
  - should search everywhere with empty exclude
  - should resolve directory relative to workingDir
- **grep_search**
  - should return matches with file paths and lines
  - should treat exit code 1 as no matches (not error)
  - should suggest filePattern when no matches and no filePattern
  - should not suggest filePattern when filePattern already provided
  - should handle timeout
  - should add --include when filePattern provided
  - should support pagination via offset/limit
  - should fallback to literal mode for invalid regex in auto mode
  - should include default excludes
  - should use custom excludes
  - should return "No matches" with hint on empty output
- **find_definition**
  - should find class definition
  - should return "No definition found" with hints on exit code 1
  - should handle timeout
  - should use custom filePattern
  - should search with language-agnostic patterns
  - should include default excludes
  - should show directory name in "no definition" message
  - should show "project root" when directory is default
- **code_stats**
  - should return aggregate stats
  - should count extensions from found files
  - should pass custom extensions to find args
  - should handle errors
- **shell injection safety (array args — no shell)**
  - passes backtick pattern as raw array arg
- **shell injection safety (array args — no shell) > glob_search — pattern injection**
  - passes pattern as raw array arg (shell metacharacters not interpreted)
  - passes workingDir path as raw array arg
  - passes user-supplied excludes as raw array args
- **grep_search — filePattern injection**
  - passes filePattern as raw array arg
  - passes user-supplied excludes as raw array args
- **find_definition — name and filePattern injection**
  - passes name inside pattern array arg (not shell-interpreted)
  - passes filePattern as raw array arg
- **code_stats — extensions injection**
  - passes extension wildcards as raw array args

**`plugins/agents/tools/src/tools/__tests__/shell.test.ts`**

- **shell_exec**
  - returns success with output
  - includes cwd in output
  - uses working directory as cwd by default
  - resolves relative cwd from workingDir
  - rejects cwd outside workingDir
  - returns placeholder text when command produces no output
  - returns SHELL_TIMEOUT on timeout
  - returns COMMAND_NOT_FOUND on exit code 127
  - returns PERMISSION_DENIED on exit code 126
  - returns NON_ZERO_EXIT for generic failures

**`plugins/agents/tools/src/tools/__tests__/todo.test.ts`**

- **todo tools cache integration**
  - should persist todo list to cache on create
  - should load from cache before update/get

### `@kb-labs/agent-tracing`

#### Unit tests

**`plugins/agents/tracing/src/__tests__/memory-trace-events.test.ts`**

- **Memory trace event factories**
- **Memory trace event factories > createFactAddedEvent()**
  - should create event with correct type and all fields
  - should include superseded field when provided
- **createArchiveStoreEvent()**
  - should create event with correct type and all fields
- **createSummarizationResultEvent()**
  - should create event with full input/output/delta/efficiency

### `@kb-labs/cli-bin`

#### CLI handler tests

**`cli/bin/src/__tests__/cli-smoke.test.ts`**

- **CLI Smoke Tests**
  - should initialize platform without crashing
  - should have core platform features
  - should handle reset and re-init
  - should be idempotent

#### Unit tests

**`cli/bin/src/__tests__/bootstrap-system-commands.test.ts`**

- **Bootstrap System Commands**
- **System Command Routing**
  - should execute system command in-process via cmd.run()
  - should pass correct PluginContextV3 to system command
  - should pass argv and flags to system command
  - should return exit code from system command
  - should handle system command errors gracefully
- **System vs Plugin Command Routing**
  - should NOT route system commands to plugin-executor
  - should route plugin commands to plugin-executor (not tested here)
- **Pure PluginContextV3 Integration**
  - should provide pure PluginContextV3 to system command (no tracker)
- **Context Field Validation**
  - should provide all required PluginContextV3 fields

**`cli/bin/src/__tests__/index.test.ts`**

- **CLI run function**
  - delegates execution to executeCli
  - propagates exit codes from executeCli

**`cli/bin/src/__tests__/limits.test.ts`**

- **handleLimitFlag**
  - renders product limits in text mode
  - renders command limits in json mode
  - renders error when product is unknown

**`cli/bin/src/__tests__/ui-adapter.test.ts`**

- **UI Adapter**
- **UIFacade Creation**
  - should create UIFacade with all required methods
- **UIFacade Methods**
  - should write raw text via write()
  - should output JSON via json()
  - should output newline via newline()
  - should output divider via divider()
  - should display table via table()
  - should delegate debug to presenter
- **Spinner Support**
  - should create spinner with all methods
  - should delegate spinner methods to presenter
- **Interactive Methods (Non-Interactive Mode)**
  - should return true for confirm() in non-interactive mode
  - should return empty string for prompt() in non-interactive mode
- **MessageOptions Support**
  - should support title in MessageOptions
  - should support sections in MessageOptions
  - should support timing in MessageOptions
- **Error Handling**
  - should handle Error object in error()
  - should handle string in error()
- **JSON Mode UI (createJsonModeUI)**
  - all UI methods except json() are no-ops in json mode
  - json() writes to stdout
  - sideBox and info produce no stdout output in json mode
- **Colors API**
  - should provide colors API from shared-cli-ui

**`cli/bin/src/runtime/__tests__/helpers/cmd-path.test.ts`**

- **normalizeCmdPath**
  - should split colon-separated command
  - should split multi-part colon command
  - should preserve array commands
  - should preserve single command without colon
  - should handle empty array
  - should handle multiple array elements
  - should only split single-element arrays with colons
  - should handle empty string
  - should handle colon at start
  - should handle colon at end
  - should handle multiple consecutive colons

**`cli/bin/src/runtime/__tests__/helpers/flags.test.ts`**

- **isTruthyBoolean**
  - should return false for boolean false
- **isTruthyBoolean > Boolean values**
  - should return true for boolean true
- **String values**
  - should accept "true"
  - should accept "yes"
  - should accept "y"
  - should accept empty string (flag without value)
  - should accept uppercase
  - should accept mixed case
  - should trim whitespace
  - should return false for other strings
- **Other types**
  - should return false for undefined
  - should return false for null
  - should return false for number
  - should return false for object
  - should return false for array
- **shouldShowLimits**
  - should return true when limit=true
  - should return true when limits=true
  - should return true when both are truthy
  - should return true when limit="true"
  - should return true when limits="yes"
  - should return true when limit="" (flag without value)
  - should return false when both are false
  - should return false when both are missing
  - should return false when both are falsy strings
  - should return true if either is truthy
  - should handle other flag names being present

**`cli/bin/src/runtime/__tests__/helpers/version.test.ts`**

- **resolveVersion**
  - should return options version when provided
  - should return env version when options not provided
  - should prefer options over env
  - should return "unknown" when neither provided
  - should handle empty env object
  - should handle empty string in options
  - should handle empty string in env

**`cli/bin/src/runtime/__tests__/platform-init.test.ts`**

- **Platform Initialization (Real)**
- **Config Discovery**
  - should find kb.config.json in .kb/
  - should find kb.config.json in root
  - should traverse up to find config
- **Fallback Behavior**
  - should use NoOp adapters when no config found
  - should use NoOp adapters when config is malformed JSON
  - should use NoOp adapters when config has no platform field
  - should handle file read errors gracefully
- **Platform Initialization**
  - should initialize platform with config
  - should preserve full config in rawConfig
- **Edge Cases**
  - should handle empty adapters object
  - should handle null platform field
  - should handle undefined adapters

### `@kb-labs/cli-commands`

#### Integration tests

**`cli/commands/src/registry/__tests__/integration.test.ts`**

- **Registry Integration**
  - registers a workspace manifest and executes successfully
  - propagates unavailable command metadata
  - prefers workspace manifests over node_modules

#### E2E / journey tests

**`cli/commands/src/registry/service.spec.ts`**

- **registerManifest with subgroups**
  - registers subgroup command with 3-part space and colon paths
  - registers top-level command with 2-part space and colon paths
  - creates synthetic subgroup accessible by space key
  - manifest lookup works by colon path (for plugin executor)
  - two subgroups with same command id do not collide
- **registerGroup with subgroups**
  - registers top-level commands
  - registers subgroup commands as 3-part paths
  - multiple subgroups do not collide
  - bare command names from subgroups do not register at top level
  - returns group object when looking up subgroup

#### Unit tests

**`cli/commands/src/__tests__/hello.test.ts`**

- **hello command**
  - has the correct command name
  - has a description mentioning "Hello World"
  - prints "Hello World" to stdout
  - outputs JSON when --json flag is set

**`cli/commands/src/registry/__tests__/availability.test.ts`**

- **checkRequires**
  - should return available: true for manifest without requires
  - should return available: true when all dependencies are resolved
  - should return available: false when dependency is missing
  - should check dependencies from current working directory

**`cli/commands/src/registry/__tests__/cache-staleness.test.ts`**

- **cache staleness — hybrid mtime + hash strategy**
  - fast path: serves from cache when both mtimes unchanged
  - mtime changed but hash identical → not stale (content-addressed build / touch)
  - mtime changed + hash changed → stale (real rebuild)
  - evicts entry when manifest file is deleted
  - evicts entry when package.json mtime changes

**`cli/commands/src/registry/__tests__/cache-synthetic.test.ts`**

- **createUnavailableManifest — _synthetic marker**
  - marks every synthetic manifest with _synthetic = true
- **resetInProcCache export**
  - is a function exported from discover module
  - causes discoverManifests to run fresh discovery after the in-proc cache is reset
- **saveCache — skips synthetic manifests**
  - does not include synthetic packages in the written cache
- **synthetic manifest _synthetic flag**
  - real manifests do not have _synthetic flag
  - synthetic manifests produced by discovery failure are skipped in cache
  - mixed results (some real, some synthetic) are cached normally
  - empty manifests array is not treated as all-synthetic

**`cli/commands/src/registry/__tests__/collision.test.ts`**

- **Collision Detection**
  - should prevent plugin from overriding system command with same canonical ID
  - should NOT shadow plugin when only bare id matches system command
  - should prevent plugin alias from overriding system command
  - should route system commands to in-process execution
  - should route plugin commands to subprocess execution
  - should handle CommandGroup routing
  - shadowed plugin is not routed to

**`cli/commands/src/registry/__tests__/discover.test.ts`**

- **discoverManifests**
  - should discover workspace packages with pnpm-workspace.yaml
  - should fallback to current package when no workspace file
  - should discover node_modules packages
  - should handle manifest load timeout
  - should rehydrate loader stub for cached manifest entries

**`cli/commands/src/registry/__tests__/help.test.ts`**

- **renderHelp**
  - should render text help by default
  - should render JSON help with source and shadowed fields
  - should filter unavailable commands when onlyAvailable is true
  - should group commands by group name
  - should sort commands within groups
  - should include aliases in JSON output

**`cli/commands/src/registry/__tests__/register.test.ts`**

- **registerManifests**
  - should register manifests with availability check
  - should handle unavailable commands with reason and hint
  - should handle shadowing: workspace shadows node_modules
  - should validate manifest schema
  - should NOT collide when same bare id is in different groups (cross-group)
  - should still collide when same group + same bare id (true collision)
  - should validate flag definitions

**`cli/commands/src/registry/__tests__/routing.test.ts`**

- **Canonical ID Strategy**
  - bare plugin (no group): canonical = id
  - 2-part plugin (group:id): canonical = group id
  - 3-part plugin (group:subgroup:id): canonical = group subgroup id
- **2-Part Shorthand for 3-Part Commands**
  - suggests ["marketplace", "list"] as "did you mean" for 3-part command (unambiguous)
  - getCommandAt does NOT resolve 2-part shorthand (exact lookup only)
  - getCommandAt resolves via full canonical segments
- **User-Defined Aliases**
  - resolves manifest aliases to the correct plugin
  - getCommandAt retrieves by canonical segments
- **Collision: System Always Wins**
  - system command beats plugin when canonical paths match exactly
  - system bare command does NOT shadow plugin in different group
  - system command beats plugin with same path
  - system group beats plugin with same name
  - logs warning when plugin collides with system command (same canonical)
  - shadowed plugin is stored in listCommands but not routed
  - plugin alias that collides with system command is blocked
- **Group Routing**
  - registered group returns type=system-group
  - subcommand within group resolves to system-cmd
  - listCommandsUnder returns plugin commands under a group
- **Multiple Plugins in Same Group**
  - registers multiple commands under same group:subgroup
  - listCommands returns all unique plugins
  - listCommandsUnder returns commands in that group
- **Cross-Group Bare ID (no false collisions)**
  - different groups with same bare id do not collide
  - system group bare subcommand does not shadow plugin with same bare id in different group
  - system bare command "status" does not shadow plugin group:status
  - multiple plugins with same bare id are all listed in listCommands
- **Edge Cases**
  - returns not-found for unknown command
  - getCommandAt returns non-null for registered plugin (full path)
  - getCommandAt returns null for 2-part shorthand (exact lookup only)
  - getCommandAt returns null for unknown command
  - listCommands returns no duplicates for multiply-keyed commands
  - getCommandAt retrieves by canonical segments

**`cli/commands/src/registry/__tests__/v3-manifest.test.ts`**

- **V3 Manifest Field**
  - should have v3Manifest field in RegisteredCommand type
  - should allow v3Manifest to be undefined for system commands
  - should preserve backward compatibility with manifestV2
  - should provide clean access to shell permissions via v3Manifest
  - should handle fallback from v3Manifest to manifestV2

### `@kb-labs/cli-runtime`

#### Integration tests

**`cli/runtime/src/__tests__/pipeline-integration.test.ts`**

- **CLI Runtime Pipeline Integration**
  - executes handler through empty middleware chain
  - executes handler through middleware chain with logging
  - middleware can intercept errors and return fallback exit code
  - formatter registry works end-to-end with runtime
  - context has request ID and cwd
  - dynamically registered middleware participates in pipeline
  - dynamically registered formatter is available

#### Unit tests

**`cli/runtime/src/__tests__/errors.test.ts`**

- **CliError**
  - creates error with code, message, and details
  - has a stack trace
- **isCliError**
  - returns true for CliError instances
  - returns true for duck-typed CliError objects
  - returns false for regular Error
  - returns false for null/undefined
  - returns false for objects with unknown error codes
- **mapCliErrorToExitCode**
  - maps E_INVALID_FLAGS to exit code 3
  - maps E_PREFLIGHT_CANCELLED to exit code 2
  - maps E_IO_READ to EX_IOERR (74)
  - maps E_IO_WRITE to EX_IOERR (74)
  - maps E_DISCOVERY_CONFIG to EX_CONFIG (78)
  - maps E_ENV_MISSING_VAR to EX_CONFIG (78)
  - maps E_TELEMETRY_EMIT to EX_SOFTWARE (70)
- **serializeCliError**
  - serializes CliError with code and details
  - serializes regular Error without code
  - includes stack when requested
  - excludes stack by default
  - handles non-Error values gracefully

**`cli/runtime/src/__tests__/flags.test.ts`**

- **parseArgs**
  - parses single command
  - parses multi-word command
  - returns empty cmdPath when only flags
  - parses --json
  - parses --help
  - parses --version
  - parses --quiet
  - parses --no-color
  - parses --dry-run and adds to flagsObj
  - parses --debug without level
  - parses --debug with level (verbose)
  - parses --debug=inspect (= syntax)
  - parses --log-level
  - parses --profile
  - parses --profiles-dir
  - parses --key value style
  - parses --key=value style
  - parses --key=value with = in value
  - parses boolean flag (no following value)
  - treats flag followed by another flag as boolean
  - stops parsing at -- separator
  - handles intermixed commands and flags
  - converts kebab-case keys to camelCase
- **validateCommandFlags**
  - passes when all flags are valid
  - throws CliError on wrong type (boolean expected, got string)
  - throws CliError on invalid choice
  - skips undefined flags without error

**`cli/runtime/src/__tests__/formatters-registry.test.ts`**

- **FormattersRegistry**
  - registers and retrieves a formatter
  - returns undefined for unregistered formatter
  - format() delegates to the registered formatter
  - format() throws when formatter not found
  - later registration overwrites earlier one with same name
  - supports multiple formatters side by side

**`cli/runtime/src/__tests__/lifecycle-manager.test.ts`**

- **LifecycleManager**
  - does nothing when manifest has no lifecycle
  - does nothing when plugin not found in registry
  - logs error when onLoad hook fails
  - does nothing when plugin was never loaded
  - does nothing when plugin was never loaded (enable)
  - does nothing when plugin was never loaded (disable)
  - shutdownAll completes when no plugins loaded
  - timeout rejects when hook takes too long

**`cli/runtime/src/__tests__/middleware-manager.test.ts`**

- **MiddlewareManager**
  - executes middlewares in priority order (lower runs first)
  - maintains order when priorities are equal (insertion order)
  - runs handler directly when no middlewares registered
  - passes context to each middleware
  - returns handler result through the chain
  - allows middleware to transform the result
  - allows middleware to short-circuit (skip handler)
  - propagates handler errors through the chain
  - propagates middleware errors
  - allows middleware to catch and handle errors
  - buildChain returns ordered middleware functions
  - buildChain returns empty array when no middlewares
  - supports wrapping pattern (before/after handler)
  - supports multiple wrapping middlewares (onion model)

**`cli/runtime/src/__tests__/runtime.test.ts`**

- **createCliRuntime**
  - returns runtime with context, middleware, and formatters
  - registers default formatters (json, yaml, table, markdown)
  - registerMiddleware adds to the chain
  - registerFormatter adds custom formatter
  - accepts pre-created context
  - accepts initial middlewares via options
  - accepts initial formatters via options

### `@kb-labs/clickup-entry`

#### CLI handler tests

**`plugins/clickup/entry/src/__tests__/cli/folder-create.cli.test.ts`**

- **clickup:folder.create**
  - FC-01: creates folder — exitCode 0, success message
  - FC-02: --json outputs folder object
  - FC-03: missing --space — exitCode 1, error shown
  - FC-04: missing --name — exitCode 1, error shown
  - FC-05: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/folder-delete.cli.test.ts`**

- **clickup:folder.delete**
  - FD-01: deletes folder with --force — exitCode 0, success message
  - FD-02: --json outputs { ok: true, deleted: true, folderId }
  - FD-03: missing folderId argv — exitCode 1, error shown
  - FD-04: missing --force — exitCode 1, error shown
  - FD-05: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/folder-update.cli.test.ts`**

- **clickup:folder.update**
  - FU-01: updates folder — exitCode 0, success message
  - FU-02: --json outputs updated folder
  - FU-03: missing folderId argv — exitCode 1, error shown
  - FU-04: missing --name — exitCode 1, error shown
  - FU-05: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/list-create.cli.test.ts`**

- **clickup:list.create**
  - LC-01: creates list in folder — exitCode 0, success message
  - LC-02: creates list in space when --space provided
  - LC-03: --json outputs list object
  - LC-04: missing --name — exitCode 1, error shown
  - LC-05: missing --folder and --space — exitCode 1, error shown
  - LC-06: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/list-delete.cli.test.ts`**

- **clickup:list.delete**
  - LD-01: deletes list with --force — exitCode 0, success message
  - LD-02: --json outputs { ok: true, deleted: true, listId }
  - LD-03: missing listId argv — exitCode 1, error shown
  - LD-04: missing --force — exitCode 1, error shown
  - LD-05: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/list-statuses.cli.test.ts`**

- **clickup:list.statuses**
  - LS-01: lists statuses — exitCode 0, table rendered
  - LS-02: --json outputs statuses array
  - LS-03: missing listId argv — exitCode 1, error shown
  - LS-04: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/list-tasks.cli.test.ts`**

- **clickup:list.tasks**
  - LT-01: lists tasks — exitCode 0, table rendered
  - LT-02: --json outputs tasks array
  - LT-03: empty task list — exitCode 0, info message shown
  - LT-04: missing listId argv — exitCode 1, error shown
  - LT-05: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/list-update.cli.test.ts`**

- **clickup:list.update**
  - LU-01: updates list — exitCode 0, success message
  - LU-02: --json outputs updated list
  - LU-03: missing listId argv — exitCode 1, error shown
  - LU-04: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/space-create.cli.test.ts`**

- **clickup:space.create**
  - SC-01: creates space — exitCode 0, success message
  - SC-02: --json outputs space object
  - SC-03: missing --name — exitCode 1, error shown
  - SC-04: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/space-delete.cli.test.ts`**

- **clickup:space.delete**
  - SD-01: deletes space with --force — exitCode 0, success message
  - SD-02: --json outputs { ok: true, deleted: true, spaceId }
  - SD-03: missing spaceId argv — exitCode 1, error shown
  - SD-04: missing --force — exitCode 1, error shown
  - SD-05: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/space-update.cli.test.ts`**

- **clickup:space.update**
  - SU-01: updates space — exitCode 0, success message
  - SU-02: --json outputs updated space
  - SU-03: missing spaceId argv — exitCode 1, error shown
  - SU-04: core function throws — exitCode 1, error shown

**`plugins/clickup/entry/src/__tests__/cli/task-comment-add.cli.test.ts`**

- **clickup:task.comment.add**
  - TCA-01: adds a comment and prints success
  - TCA-02: --json outputs comment object
  - TCA-03: missing taskId returns exitCode 1
  - TCA-04: missing --text returns exitCode 1
  - TCA-05: core error returns exitCode 1

**`plugins/clickup/entry/src/__tests__/cli/task-comment-list.cli.test.ts`**

- **clickup:task.comment.list**
  - TCL-01: lists comments and renders chain
  - TCL-02: --json outputs comments array
  - TCL-03: empty comments prints info message
  - TCL-04: missing taskId returns exitCode 1
  - TCL-05: core error returns exitCode 1

**`plugins/clickup/entry/src/__tests__/cli/task-create.cli.test.ts`**

- **clickup:task.create**
  - TC-01: creates a task and prints success
  - TC-02: --json outputs task object
  - TC-03: missing --list returns exitCode 1
  - TC-04: missing --name returns exitCode 1
  - TC-05: core error returns exitCode 1

**`plugins/clickup/entry/src/__tests__/cli/task-delete.cli.test.ts`**

- **clickup:task.delete**
  - TD-01: deletes a task with --force and prints success
  - TD-02: --json outputs { ok, deleted, taskId }
  - TD-03: missing taskId returns exitCode 1
  - TD-04: missing --force returns exitCode 1
  - TD-05: core error returns exitCode 1

**`plugins/clickup/entry/src/__tests__/cli/task-get.cli.test.ts`**

- **clickup:task.get**
  - TG-01: retrieves task and renders sideBox
  - TG-02: --json outputs { task, comments }
  - TG-03: missing taskId returns exitCode 1
  - TG-04: core error returns exitCode 1

**`plugins/clickup/entry/src/__tests__/cli/task-search.cli.test.ts`**

- **clickup:task.search**
  - TS-01: returns tasks and renders chain
  - TS-02: --json outputs tasks array
  - TS-03: empty results prints info message
  - TS-04: core error returns exitCode 1

**`plugins/clickup/entry/src/__tests__/cli/task-update.cli.test.ts`**

- **clickup:task.update**
  - TU-01: updates a task and prints success
  - TU-02: --json outputs updated task
  - TU-03: missing taskId returns exitCode 1
  - TU-04: core error returns exitCode 1

**`plugins/clickup/entry/src/__tests__/cli/workspace.cli.test.ts`**

- **clickup:workspace**
  - W-01: shows workspace hierarchy — exitCode 0, info rendered
  - W-02: --json outputs compact workspace object
  - W-03: --json --full outputs full workspace object
  - W-04: core function throws — exitCode 1, error shown

### `@kb-labs/commit-contracts`

#### Unit tests

**`plugins/commit/contracts/tests/contracts.manifest.test.ts`**

- **pluginContractsManifest**
  - is valid according to the schema
  - rejects malformed manifests
  - uses a semver-compatible contractsVersion

### `@kb-labs/commit-core`

#### Unit tests

**`plugins/commit/core/tests/analyzer/git-status.test.ts`**

- **getGitStatus**
  - should return empty status for clean repo
  - should detect untracked files
  - should detect staged files
  - should resolve to nested repo when cwd points to it
- **getAllChangedFiles**
  - should merge staged, unstaged, untracked without duplicates
- **hasChanges**
  - should return false for empty status
  - should return true when there are staged files

**`plugins/commit/core/tests/applier/apply.test.ts`**

- **applyCommitPlan - scoped repositories**
  - should apply commits when paths are relative to scope
  - should detect staleness when files no longer have changes
  - should bypass staleness check with force option
- **applyCommitPlan - path transformation edge cases**
  - should handle deeply nested file paths correctly
- **applyCommitPlan - multiple commits**
  - should apply multiple commits in order
  - should stop on first error and preserve repo state
  - should fail preflight when file appears in multiple commits
- **applyCommitPlan - staging area isolation**
  - should only commit specified files, not all staged files

**`plugins/commit/core/tests/benchmarks/commit-type-accuracy.test.ts`**

- **Commit Type Accuracy Benchmarks**
  - should load all test cases
  - should run benchmark suite and generate results

### `@kb-labs/commit-entry`

#### E2E / journey tests

**`plugins/commit/entry/tests/rest/handlers/files-handler.spec.ts`**

- **files-handler — shell injection safety (H10)**
  - calls execFile for git diff with args array, not a shell string
  - passes file with backtick in name as literal arg (no shell execution)
  - passes file with $() in name as literal arg (no shell execution)
  - calls execFile for wc -l with args array for untracked files

**`plugins/commit/entry/tests/rest/handlers/scope-resolver.spec.ts`**

- **resolveScopePath**
  - returns base directory for root-like scopes
  - joins scope path from id when no scopes config provided
  - resolves scope path from scopes config when id matches
  - falls back to id as path when scope not found in config

#### Unit tests

**`plugins/commit/entry/tests/rest/handlers/apply-handler.test.ts`**

- **apply-handler - path transformation for scoped repos**
  - should transform file paths from repoRoot-relative to scope-relative
  - should fail staleness check when file paths are not transformed
  - should bypass staleness check with force option
- **apply-handler - scope prefix edge cases**
  - should handle deeply nested scope paths correctly
  - should handle root scope (no prefix)

### `@kb-labs/core-bundle`

#### E2E / journey tests

**`core/bundle/src/__tests__/integration.spec.ts`**

- **Bundle Integration Tests**
- **Full Bundle Loading**
  - should load complete bundle
  - should resolve profile correctly
  - should merge configuration from profile
  - should work with production profile
  - should provide policy permits function
  - should provide merge trace
- **Error Handling**
  - should handle missing workspace config
  - should handle missing profile
  - should handle product not in profile
- **Cache Management**
  - should clear caches correctly
- **Multiple Products**
  - should load different products from same profile
- **Platform Configuration**
  - should load platform config from kb.config

**`core/bundle/src/__tests__/load-bundle-validation.spec.ts`**

- **bundle: loadBundle validation option**
  - warns when validate is warn and config invalid

### `@kb-labs/core-config`

#### E2E / journey tests

**`core/config/src/__tests__/cache.spec.ts`**

- **Filesystem Cache**
- **Cache Operations**
  - should cache file reads
  - should invalidate cache on file change
  - should clear caches
  - should handle YAML files
- **Cache Statistics**
  - should track cache size

**`core/config/src/__tests__/fs-atomic.spec.ts`**

- **fs-atomic**
- **writeFileAtomic**
  - should write file atomically
  - should create parent directories
  - should handle Uint8Array data
  - should clean up temp file on error
  - should handle cleanup errors gracefully
  - should overwrite existing file
  - should generate unique temp file names
- **ensureWithinWorkspace**
  - should allow paths within workspace
  - should allow relative paths within workspace
  - should throw error for paths outside workspace
  - should throw error for paths that escape via ..
  - should handle edge case with trailing slashes
  - should handle same directory as workspace
  - should throw error with correct error details
  - should handle empty workspace root
  - should handle empty target path
  - should resolve paths correctly

**`core/config/src/__tests__/get-product-config-edge-cases.spec.ts`**

- **getProductConfig Edge Cases**
- **Profile Layer Edge Cases**
  - should handle missing profile layer
  - should merge profile layer with workspace config
  - should handle scope layer with profile layer
- **CLI Overrides**
  - should apply CLI overrides with highest priority
  - should handle CLI overrides without profile layer
- **Merge Conflicts**
  - should handle nested object merges correctly
- **Cache Behavior**
  - should cache configuration results
  - should invalidate cache when config changes
- **Trace Generation**
  - should generate trace for all layers
- **Write Final Config**
  - should write final config when writeFinal=true

**`core/config/src/__tests__/init-workspace.spec.ts`**

- **initWorkspaceConfig**
  - creates new workspace config in YAML format
  - creates new workspace config in JSON format
  - is idempotent - second run skips unchanged config
  - detects conflicts without --force
  - supports dry-run mode
  - warns when deprecated profiles option is provided

**`core/config/src/__tests__/kb-error.spec.ts`**

- **KbError**
- **KbError > constructor**
  - should create error with all parameters
  - should create error with minimal parameters
  - should create error with code and message only
  - should create error with hint but no meta
  - should create error with meta but no hint
  - should handle complex meta objects
  - should handle empty string parameters
- **inheritance**
  - should be instanceof Error
  - should be instanceof KbError
  - should have Error prototype methods
- **serialization**
  - should serialize to JSON correctly
  - should handle undefined values in serialization
- **getExitCode**
  - should return 3 for ERR_FORBIDDEN
  - should return 2 for ERR_CONFIG_NOT_FOUND
  - should return 2 for ERR_CONFIG_EXISTS_CONFLICT
  - should return 2 for ERR_PATH_OUTSIDE_WORKSPACE
  - should return 1 for other ERR_ codes
  - should return 1 for non-ERR codes
  - should return 1 for empty code
- **ERROR_HINTS**
  - should contain all expected error codes
  - should have helpful hints for each error
  - should be readonly

**`core/config/src/__tests__/preset-lockfile.spec.ts`**

- **Preset and Lockfile System**
- **Preset Resolution**
  - should resolve preset from local node_modules
  - should get preset config for specific product
  - should throw error for invalid preset reference
  - should throw error for missing preset
- **Lockfile Management**
  - should create and read lockfile
  - should update lockfile with new hashes
  - should check if lockfile is up to date
  - should return null for missing lockfile

**`core/config/src/__tests__/product-config-profiles.spec.ts`**

- **Product Config with Profiles**
  - should merge profile overlays correctly
  - should handle missing profile gracefully
  - should include profile info in trace
  - should include scope layer when provided

**`core/config/src/__tests__/product-config.spec.ts`**

- **Product Configuration**
- **Product Normalization**
  - should convert ProductId to filesystem format
  - should convert filesystem format to ProductId
- **getProductConfig**
  - should return runtime defaults when no config files exist
  - should merge workspace config
  - should merge local config
  - should merge CLI overrides
  - should handle array overwrite
- **explainProductConfig**
  - should return trace without resolving config
- **Error Handling**
  - should throw KbError for invalid config
  - should handle preset resolution failures
  - should handle merge conflicts gracefully
  - should handle missing product in config
  - should handle invalid product configuration structure
  - should handle circular references in config
  - should handle file system errors

**`core/config/src/__tests__/profiles-resolver.spec.ts`**

- **resolveProfile**
  - resolves local profile with extends chain and merges overlays
  - throws when profile not found
  - detects circular extends
  - loads profiles from npm packages

**`core/config/src/__tests__/profiles-section.spec.ts`**

- **readProfilesSection**
  - returns empty list when kb.config is missing
  - returns empty list when profiles section absent
  - parses valid profiles array
  - throws KbError when profiles section invalid

**`core/config/src/__tests__/runtime.spec.ts`**

- **runtime utils**
- **findNearestConfig**
  - walks up directories and finds the nearest file
  - respects stopDir boundary
- **readJsonWithDiagnostics**
  - returns ok with data on valid JSON
  - returns diagnostics on parse error
  - returns diagnostics on read error
- **mergeDefined**
  - deep merges objects and arrays ignoring undefined
  - overlay wins on different types when defined
- **pickDefined**
  - picks only defined values
- **resolveConfig**
  - applies precedence: defaults < file < env < cli; and validate
  - env mapper reads real env; can be stubbed

**`core/config/src/__tests__/validate-config.spec.ts`**

- **config: validate product config**
  - validates aiReview config with review schema
  - allows unknown product (no schema)

**`core/config/src/utils/__tests__/env.spec.ts`**

- **toBool**
  - parses truthy values
  - parses falsy values
  - returns undefined for unknown or undefined
- **toInt**
  - parses valid integers
  - returns undefined for invalid numbers or undefined

### `@kb-labs/core-contracts`

#### Unit tests

**`core/contracts/src/__tests__/entrypoint-contracts.test.ts`**

- **core-contracts entrypoint contract**
  - exports required canonical contracts

**`core/contracts/src/__tests__/exports.snapshot.test.ts`**

- **core-contracts export surface snapshot**
  - matches entrypoint export keys

**`core/contracts/src/__tests__/layering-invariants.test.ts`**

- **core-contracts layering invariants**
  - does not import @kb-labs/plugin-contracts

**`core/contracts/src/__tests__/observability-validation.test.ts`**

- **observability validators**
  - accepts valid describe and health payloads
  - rejects invalid payloads
- **canonical metrics compliance**
  - detects missing canonical metric families
- **log correlation contract**
  - exports canonical structured log fields for service correlation

### `@kb-labs/core-discovery`

#### E2E / journey tests

**`core/discovery/src/__tests__/diagnostics.spec.ts`**

- **DiagnosticCollector**
  - starts with no events
  - collects events with correct severity
  - hasErrors returns true only when errors exist
  - countBySeverity returns correct counts
  - includes remediation and stack when provided
  - getEvents returns a copy
  - records timestamp on each event

**`core/discovery/src/__tests__/discovery-disabled-and-refresh.spec.ts`**

- **DiscoveryManager — disabled plugins**
  - skips disabled plugins and emits PLUGIN_DISABLED diagnostic
  - discovers enabled plugins alongside disabled ones
- **DiscoveryManager — local integrity refresh**
  - discovers local packages even when integrity hash is stale — no lock update during discovery
  - discovers local packages with matching integrity without touching lock
  - blocks marketplace packages with wrong integrity (not local)
  - warns about manifest ID mismatch but still discovers

**`core/discovery/src/__tests__/discovery-integration.spec.ts`**

- **Discovery Integration: install → discover → available**
  - discovers a single CLI plugin end-to-end
  - discovers multiple plugins with different entity kinds
  - installed → disabled → not discovered → enabled → discovered
  - tampered package is blocked (integrity mismatch)
  - removed package directory produces PACKAGE_NOT_FOUND diagnostic
  - no lock file produces empty result with diagnostic

**`core/discovery/src/__tests__/discovery-manager.spec.ts`**

- **DiscoveryManager**
  - returns empty result when no lock file exists
  - returns empty result when lock has no entries
  - reports PACKAGE_NOT_FOUND when resolvedPath does not exist
  - discovers plugin with kb.plugin.json
  - reports INTEGRITY_MISMATCH when hash does not match
  - skips integrity check when verifyIntegrity is false
  - reports SIGNATURE_MISSING as info when plugin has no signature
  - detects duplicate plugin IDs

**`core/discovery/src/__tests__/discovery-two-roots.spec.ts`**

- **DiscoveryManager — two-root mode (platformRoot + root)**
  - discovers plugins from platform lock when only platformRoot has a lock
  - discovers plugins from project lock when project has entries not in platform lock
  - project entry wins over platform entry for the same package id
  - resolves platform entry paths relative to platformRoot, not projectRoot
  - resolves project entry paths relative to projectRoot, not platformRoot
  - returns empty result when both locks are missing
  - does not add platformRoot entries twice when root === platformRoot

**`core/discovery/src/__tests__/extract-entity-kinds.spec.ts`**

- **extractEntityKinds**
  - always includes "plugin" as base kind
  - detects cli-command from manifest.cli.commands
  - detects rest-route from manifest.rest.routes
  - detects ws-channel from manifest.ws.channels
  - detects workflow from manifest.workflows.handlers
  - detects webhook from manifest.webhooks.handlers
  - detects job from manifest.jobs.handlers
  - detects cron from manifest.cron.schedules
  - detects studio-widget from manifest.studio.pages
  - detects studio-menu from manifest.studio.menus
  - returns multiple kinds for rich manifest
  - does not detect kind when array is empty

**`core/discovery/src/__tests__/integrity.spec.ts`**

- **computePackageIntegrity**
  - computes sha256 SRI hash of package.json
  - produces deterministic output for same content
  - produces different hash for different content
  - throws when package.json is missing
  - starts with sha256- prefix
- **computeFileIntegrity**
  - hashes arbitrary file content
  - returns different hash when file content changes
  - returns same hash when content is identical even if mtime differs
  - throws when file does not exist
- **computeManifestIntegrity**
  - produces same result as computeFileIntegrity for the same path
  - detects rebuild — hash changes when manifest content changes
- **parseIntegrity**
  - parses valid sha256 integrity string
  - parses sha512 integrity string
  - returns null for string without dash
  - returns null for string starting with dash
  - returns null for string ending with dash
  - handles hash with dashes inside

**`core/discovery/src/__tests__/manifest-loader.spec.ts`**

- **loadManifest**
  - loads manifest from kb.plugin.json
  - returns null with warning when kb.plugin.json is not valid ManifestV3
  - returns null with MANIFEST_NOT_FOUND when no manifest source exists
  - loads manifest from package.json kbLabs.manifest field
  - loads manifest from package.json kb.manifest field
  - rejects manifest path that escapes package root (path traversal)
  - continues to kb.plugin.json when package.json is missing
  - prefers package.json manifest field over kb.plugin.json

**`core/discovery/src/__tests__/marketplace-lock.spec.ts`**

- **marketplace-lock**
- **readMarketplaceLock**
  - returns null and info diagnostic when file does not exist
  - returns null with error on invalid JSON
  - returns null with error on wrong schema
  - reads valid lock file
- **writeMarketplaceLock**
  - creates .kb dir and writes atomically
- **addToMarketplaceLock**
  - creates lock file if it does not exist
  - adds to existing lock without overwriting others
- **removeFromMarketplaceLock**
  - returns false if lock does not exist
  - returns false if package not in lock
  - removes existing entry and persists
- **createMarketplaceEntry**
  - sets installedAt to current time
  - sets primaryKind from opts
  - defaults enabled to true
  - includes signature when provided

### `@kb-labs/core-ipc`

#### Unit tests

**`core/ipc/src/__tests__/child-ipc-server.test.ts`**

- **ChildIPCServer**
- **Lifecycle**
  - should start and listen for messages
  - should stop and remove listeners
  - should not start twice
  - should auto-stop on child exit
- **Adapter Call Dispatch**
  - should dispatch cache.get and send response
  - should dispatch llm.complete and send response
  - should dispatch vectorStore.search
  - should handle adapter method errors
  - should handle unknown method on adapter
- **Message Filtering**
  - should ignore WorkerMessage types (execute, result, etc.)
  - should ignore plain strings and numbers
- **Permission Enforcement (Layer 2)**
  - should allow adapter call when no permissions context
  - should allow adapter call when adapter is in allowed list
  - should deny adapter call when adapter not in allowed list
  - should allow when permissions.adapters is absent (no restrictions)
- **Connection State**
  - should not send response if child disconnected

**`core/ipc/src/__tests__/create-proxy-platform.test.ts`**

- **createProxyPlatform**
- **Structure**
  - should return object with all IPlatformAdapters fields
  - should use provided logger
  - should use noop logger by default
- **LLM Proxy**
  - should forward complete() via transport
- **Cache Proxy**
  - should forward get() via transport
  - should forward set() via transport
  - should forward sorted set operations
- **VectorStore Proxy**
  - should forward search() via transport
- **Storage Proxy**
  - should forward read() via transport
- **Noop Adapters**
  - should have noop analytics (no transport calls)
  - should have noop eventBus (no transport calls)
  - should have noop logs (no transport calls)

**`core/ipc/src/__tests__/unix-socket-server.test.ts`**

- **UnixSocketServer**
- **Server Lifecycle**
  - should start and listen on Unix socket
  - should return socket path
  - should close and cleanup socket file
  - should be idempotent on close
  - should remove existing socket file on start
- **Adapter Call Handling**
  - should handle cache.get call
  - should handle vectorStore.search call
  - should handle errors gracefully
  - should reject unknown adapter
  - should reject unknown method
- **Protocol Version Compatibility**
  - should log warning for version mismatch
- **Multiple Clients**
  - should handle multiple concurrent clients

### `@kb-labs/core-platform`

#### E2E / journey tests

**`core/platform/src/wrappers/analytics-llm-metadata.spec.ts`**

- **AnalyticsLLM - Metadata Tracking**
- **complete() - metadata in events**
  - should track tier in started and completed events
  - should track provider in events
  - should track full metadata from LLMRouter
  - should handle missing metadata gracefully (undefined tier/provider)
  - should track metadata in error events
- **stream() - metadata in events**
  - should track tier/provider in stream events
  - should track metadata in stream error events
- **chatWithTools() - metadata in events**
  - should track tier/provider in chatWithTools events
  - should track metadata in chatWithTools error events
- **Real-world routing scenarios**
  - should track small tier → OpenAI routing
  - should track medium tier → Anthropic routing
  - should track large tier → Opus routing

**`core/platform/src/wrappers/analytics-llm.spec.ts`**

- **AnalyticsLLM - Cost Estimation**
  - should match gpt-4o-2024-05-13 to gpt-4o pricing
- **AnalyticsLLM - Cost Estimation > Versioned Model Name Matching**
  - should match gpt-4o-mini-2024-07-18 to gpt-4o-mini pricing
  - should match gpt-4-turbo-2024-04-09 to gpt-4-turbo pricing
  - should match gpt-4-0125-preview to gpt-4 pricing
  - should match claude-3-opus-20240229 to claude-3-opus pricing
- **Model Name Priority (Longest First)**
  - should NOT match gpt-4o-mini-2024-07-18 to gpt-4o (wrong)
  - should match gpt-4o-mini before gpt-4o due to sorting
- **Fallback for Unknown Models**
  - should use gpt-4o-mini pricing for unknown models
- **Cost Calculation Precision**
  - should calculate cost per 1M tokens, not 1K tokens
  - should handle small token counts without rounding to zero
- **Case Insensitivity**
  - should match uppercase model names
  - should match mixed case model names
- **Real-world Examples**
  - should calculate realistic cost for typical query (gpt-4o-mini)
  - should calculate realistic cost for expensive model (gpt-4)

**`core/platform/src/wrappers/scoped-analytics.spec.ts`**

- **ScopedAnalytics**
- **Constructor**
  - should store scoped source
- **track()**
  - should delegate to real analytics
  - should override source.product with scoped value
- **identify()**
  - should delegate to real analytics
- **Multiple scoped instances**
  - should allow different plugins to have separate scopes
- **Nested wrapper chains**
  - should work with other analytics wrappers (AnalyticsEmbeddings, etc.)
- **Integration: ScopedAnalytics + handler-executor**
  - should demonstrate the complete flow
  - should support getSource/setSource restore pattern

#### Unit tests

**`core/platform/src/adapters/adapter-manifest.test.ts`**

- **AdapterManifest**
  - should define minimal core adapter manifest
- **AdapterManifest > Core Adapter Manifest**
  - should define valid core adapter manifest (Pino logger)
- **Extension Adapter Manifest**
  - should define extension adapter without dependencies (ring buffer)
  - should define extension adapter with dependencies (SQLite persistence)
  - should support default priority (0) when not specified
- **Proxy Adapter Manifest**
  - should define proxy adapter manifest (IPC proxy)
- **Adapter Dependencies**
  - should support short-form dependencies (string)
  - should support long-form dependencies with aliases
  - should support mixed required and optional dependencies
- **Adapter Capabilities**
  - should support boolean capabilities
  - should support custom capabilities
  - should allow empty capabilities object
- **AdapterFactory**
  - should define sync factory function
  - should define async factory function
  - should support factory with no dependencies
- **Manifest Versioning**
  - should require semver version strings
  - should support semver ranges in platform requirements
- **Type Constraints**
  - should only allow valid adapter types
  - should allow extension without priority (defaults to 0)
  - should allow extension with explicit priority
- **Real-world Examples**
  - should compile example from ADR: Pino Logger
  - should compile example from ADR: Ring Buffer
  - should compile example from ADR: SQLite Persistence

**`core/platform/src/execution-contracts.invariant.test.ts`**

- **core-platform execution contracts invariant**
  - does not define local execution contract interfaces

**`core/platform/src/logging/diagnostic-events.test.ts`**

- **logDiagnosticEvent**
  - logs structured reason codes and evidence
  - forwards errors on error-level diagnostic events

**`core/platform/src/logging/prefixed-logger.test.ts`**

- **createPrefixedLogger**
- **system field protection**
  - should rename system fields to plugin_* prefix
  - should protect all system fields from SYSTEM_LOG_FIELDS
  - should allow non-system fields without prefixing
- **REST context fields**
  - should protect REST-specific fields
- **Workflow context fields**
  - should protect Workflow-specific fields
- **Webhook context fields**
  - should protect Webhook-specific fields
- **Cron context fields**
  - should protect Cron-specific fields
- **logging methods**
  - should proxy all log methods correctly
- **custom options**
  - should use custom prefix
  - should use custom protected fields
  - should disable warnings when warnOnRename=false
- **development warnings**
  - should warn in development when renaming fields
  - should not warn in production
- **recursive wrapping**
  - should maintain protection through nested child() calls
- **edge cases**
  - should handle empty fields object
  - should handle undefined values
  - should handle complex nested objects
- **optional log buffer**
  - should proxy getLogBuffer() if available
  - should handle missing getLogBuffer()
- **SYSTEM_LOG_FIELDS constant**
  - should contain all observability fields
  - should contain REST context fields
  - should contain Workflow context fields
  - should contain Webhook context fields
  - should contain Cron context fields

**`core/platform/src/noop/adapters/__tests__/embeddings.test.ts`**

- **MockEmbeddings**
  - should have dimensions property
  - should have getDimensions method for IPC transport
  - should generate deterministic vectors
  - should generate different vectors for different texts
  - should generate normalized vectors
  - should handle batch embeddings
  - should be deterministic across batch and single calls

**`core/platform/src/serializable/serializer.test.ts`**

- **serializer**
  - should serialize undefined as null
- **serializer > primitives**
  - should serialize null
  - should serialize boolean
  - should serialize number
  - should serialize string
- **Buffer serialization**
  - should serialize Buffer to base64
  - should deserialize Buffer from base64
  - should round-trip Buffer
  - should handle empty Buffer
  - should handle binary data in Buffer
  - should throw on invalid Buffer data
  - should gracefully decode non-strict base64 input
- **Date serialization**
  - should serialize Date to ISO string
  - should deserialize Date from ISO string
  - should round-trip Date
  - should throw on invalid Date
  - should throw on invalid ISO string
  - should throw on missing iso field
- **Error serialization**
  - should serialize Error with stack
  - should serialize custom Error types
  - should serialize Error with code
  - should deserialize Error
  - should round-trip Error
  - should throw on missing message field
- **Array serialization**
  - should serialize empty array
  - should serialize array of primitives
  - should serialize nested arrays
  - should serialize array with special types
  - should deserialize array
  - should round-trip array
- **Object serialization**
  - should serialize empty object
  - should serialize flat object
  - should serialize nested objects
  - should serialize object with special types
  - should deserialize object
  - should round-trip object
- **circular reference detection**
  - should detect circular reference in object
  - should detect circular reference in nested object
  - should detect circular reference in array
  - should treat repeated object references as circular
- **unsupported types**
  - should throw on function
  - should throw on symbol
  - should throw on Map
  - should throw on Set
  - should throw on class instance
- **helper functions**
  - should handle empty args
- **helper functions > serializeArgs**
  - should serialize array of arguments
- **deserializeArgs**
  - should deserialize array of arguments
  - should handle empty args
- **performance**
  - should serialize 10K objects in < 500ms
  - should deserialize 10K objects in < 500ms
- **edge cases**
  - should handle deeply nested structures
  - should handle large arrays
  - should handle objects with many keys
  - should handle mixed special types

**`core/platform/src/wrappers/pii-detector.test.ts`**

- **RegexPIIDetector**
  - should detect multiple emails
- **RegexPIIDetector > detect()**
  - should detect email addresses
  - should detect IPv4 addresses
  - should detect OpenAI API keys
  - should detect GitHub tokens
  - should detect SSH keys
  - should detect connection strings
  - should detect passwords
  - should return empty array for clean text
- **redact()**
  - should replace PII with placeholders
  - should reuse placeholders for identical values
  - should assign different placeholders for different values
  - should use custom prefix
  - should return original text when no PII found
- **restore()**
  - should restore placeholders to original values
  - should handle multiple placeholders
  - should handle repeated placeholders in response
  - should return text unchanged with empty map
- **allowlist**
  - should skip allowlisted values
  - should support wildcard allowlist
- **selective patterns**
  - should only detect enabled categories
- **roundtrip**
  - should survive full redact → restore cycle

**`core/platform/src/wrappers/pii-redaction-llm.test.ts`**

- **PIIRedactionLLM**
  - should restore PII in response
- **PIIRedactionLLM > complete()**
  - should redact PII from prompt before sending to LLM
  - should pass through when no PII found
  - should not restore in one-way mode
- **chatWithTools()**
  - should redact PII in messages and restore in response
  - should use consistent placeholders across messages
  - should throw if underlying LLM does not support chatWithTools
- **stream()**
  - should redact PII in prompt and restore in chunks
  - should pass through without buffering when no PII
- **createPIIRedactionLLM()**
  - should return original LLM when disabled
  - should return original LLM when no config
  - should return wrapped LLM when enabled
- **systemPrompt redaction**
  - should redact PII in systemPrompt option

### `@kb-labs/core-policy`

#### E2E / journey tests

**`core/policy/src/__tests__/init-policy.spec.ts`**

- **initPolicy**
  - appends policy scaffold to YAML config
  - skips policy scaffold for JSON config
  - is idempotent - skips if already exists
  - supports dry-run mode

**`core/policy/src/__tests__/policy-edge-cases.spec.ts`**

- **Policy Engine Edge Cases**
- **Policy Engine Edge Cases > Policy Resolution Edge Cases**
  - should handle missing policy configuration
  - should resolve policy with preset bundle
  - should merge workspace overrides with preset
- **Permission Checking Edge Cases**
  - should permit action when explicitly allowed
  - should deny action when explicitly denied
  - should deny by default when no rule matches (deny-all default)
  - should permit by default when no rule matches (permit-all mode)
  - should handle conflicting allow/deny rules
  - should handle wildcard actions
  - should handle multiple roles
  - should handle empty roles
- **Policy Merge Edge Cases**
  - should merge multiple rules for same action
  - should handle conflicting allow/deny rules
- **Edge Cases**
  - should handle empty policy
  - should handle invalid action format
  - should handle action with special characters

**`core/policy/src/__tests__/policy.spec.ts`**

- **Policy System**
  - should use workspace overrides when provided
- **Policy System > Policy Resolution**
  - should use permit-all default when no policy configured
  - should merge preset and workspace policies
- **Permission Checking**
  - should allow admin to perform any action
  - should allow maintainer to publish releases
  - should allow developer to sync devkit and materialize profiles
  - should allow reviewer to run AI review
  - should deny unknown roles
  - should permit all when no policy rules
- **Permits Function**
  - should create permits function for specific identity
- **Permission Requirements**
  - should throw error when permission denied
  - should not throw when permission granted
- **Schema Validation**
  - should validate correct policy schema
  - should reject invalid policy schema
- **Policy Validation**
  - should validate correct policy structure
  - should reject invalid policy structure

### `@kb-labs/core-registry`

#### E2E / journey tests

**`core/registry/src/__tests__/diagnostics-reporter.spec.ts`**

- **buildDiagnosticReport**
  - builds empty report
  - counts errors and warnings
  - groups events by plugin
- **formatDiagnosticReport**
  - formats empty report
  - includes error details

**`core/registry/src/__tests__/entity-catalog.spec.ts`**

- **EntityCatalog**
  - starts empty
  - extracts plugin entity for every discovered plugin
  - extracts cli-command entities
  - extracts rest-route entities
  - extracts workflow entities
  - filters by pluginId
  - filters by multiple kinds
  - filters by search term
  - getEntity returns exact match by ref
  - getEntity returns null for missing ref
  - getKinds lists only kinds with entries
  - rebuild clears previous data
  - verified filter returns only signed entities

**`core/registry/src/__tests__/generators.spec.ts`**

- **generateOpenAPISpec**
  - generates valid OpenAPI 3.1.0 spec
  - generates empty paths for manifest without routes
- **mergeOpenAPISpecs**
  - returns empty spec for no inputs
  - returns single spec as-is
  - merges multiple specs deterministically
  - adds x-kb-plugin-id to each operation
- **generateStudioRegistry**
  - generates sorted registry
  - detects capabilities from manifest

**`core/registry/src/__tests__/integration.spec.ts`**

- **Registry Integration**
  - full lifecycle: install → discover → query → uninstall → refresh
  - multi-plugin registry with mixed entity types
  - snapshot persistence and reload
  - diagnostics report for broken plugins
  - onChange fires on refresh with added/removed plugins
  - generators: OpenAPI and Studio registry
  - health snapshot reflects registry state

**`core/registry/src/__tests__/registry.spec.ts`**

- **EntityRegistry**
  - initializes empty when no lock file
  - discovers plugins from marketplace.lock
  - queryEntities returns correct entities by kind
  - getEntity returns exact entity
  - snapshot produces valid registry/1 schema
  - getOpenAPISpec returns spec for plugin with REST routes
  - getStudioRegistry returns all plugins
  - getDiagnostics returns structured report
  - onChange notifies on refresh
  - getManifest returns null for unknown plugin
  - getSystemHealth returns valid health snapshot

**`core/registry/src/__tests__/snapshot-utils.spec.ts`**

- **snapshot-utils**
  - handles nested objects
- **snapshot-utils > stableStringify**
  - sorts object keys
  - handles arrays
  - filters undefined values
  - handles null
  - handles primitives
  - is deterministic for same data
- **computeSnapshotChecksum**
  - returns consistent sha256 hex for same input
  - changes when data changes
- **cloneValue**
  - deep clones objects
- **safeParseInt**
  - returns number as floor
  - parses string numbers
  - returns 0 for invalid input

### `@kb-labs/core-resource-broker`

#### Unit tests

**`core/resource-broker/src/wrappers/__tests__/queued-embeddings.test.ts`**

- **QueuedEmbeddings**
  - should have dimensions property
  - should have getDimensions method for IPC transport
  - should embed single text through queue
  - should embed batch through queue
  - should support priority setting
  - should handle empty batch
  - should produce deterministic results

**`core/resource-broker/src/wrappers/__tests__/queued-llm.test.ts`**

- **QueuedLLM**
- **metadata.resource routing**
  - should use default "llm" resource when no metadata provided
  - should use metadata.resource when provided by LLMRouter
  - should route to llm:openai for OpenAI provider
  - should route to llm:vibeproxy for Vibeproxy provider
  - should fallback to "llm" when metadata exists but resource is undefined
  - should preserve full metadata in args for downstream wrappers
- **priority handling**
  - should use default "normal" priority
  - should use provided priority
- **token estimation**
  - should estimate tokens from prompt
  - should estimate more tokens for longer prompts
- **error handling**
  - should throw when broker returns failure
  - should throw generic error when broker fails without error object
- **stream passthrough**
  - should bypass queue for streaming (real-time UX)
- **chatWithTools passthrough**
  - should pass through to realLLM (not queued yet)
  - should throw if realLLM does not support chatWithTools

### `@kb-labs/core-runtime`

#### E2E / journey tests

**`core/runtime/src/__tests__/discover-adapters.spec.ts`**

- **discoverAdapters (lock-based)**
  - returns empty map when no lock file exists
  - returns empty map when lock has no adapters
  - skips disabled adapters
  - skips adapters without built dist
  - discovers adapter with createAdapter export
- **discoverAdapters — project wins over platform**
  - returns empty when neither root has a lock
  - returns platform-only adapters when project lock is absent
  - returns project-only adapters when platform lock is absent
  - project adapter wins over platform adapter with same id
  - merges non-overlapping adapters from both roots
  - behaves identically when projectRoot === platformRoot (dev mode)

**`core/runtime/src/__tests__/platform-sync.spec.ts`**

- **platformSync**
  - returns ok with lockMissing when there is no lock file
  - validate mode: passes when every local entry exists
  - validate mode: reports missing local entry without installing
  - validate mode: reports integrity mismatch for marketplace entries
  - validate mode: accepts matching integrity
  - validate mode: empty integrity is allowed (future prod-lock case)
  - validate mode: skips disabled entries
  - reconcile mode: installs missing marketplace entry via injected installer
  - reconcile mode: surfaces installer errors and marks result not-ok
  - reconcile mode: refuses to auto-install missing local entries
  - dry-run: never installs even when reconcile is requested
  - auto mode: picks validate when pnpm-workspace.yaml exists
  - auto mode: picks reconcile when there is no monorepo marker

#### Unit tests

**`core/runtime/src/__tests__/adapter-loader.test.ts`**

- **DependencyGraph**
  - should perform topological sort with no dependencies
  - should sort with linear dependency chain
  - should detect circular dependency
- **AdapterLoader**
  - should build dependency graph from configs
  - should throw error for missing required dependency
  - should explain runtime token vs manifest id mismatch
  - should handle optional dependencies gracefully
  - should load adapters in dependency order
  - should connect extensions to core adapters
  - should sort extensions by priority (higher first)
- **validateExtensionConnection (warning paths)**
  - should warn and skip when target adapter is not in adapters map
  - should warn and skip when target adapter has no hook method
  - should warn and skip when extension instance has no method
  - should warn for invalid extension but still connect valid ones

**`core/runtime/src/__tests__/broker-rps-test.test.ts`**

- **ResourceBroker RPS**
  - should enforce 1 request per second using requestsPerSecond

**`core/runtime/src/__tests__/cache-proxy.test.ts`**

- **CacheProxy**
- **Basic Cache Operations**
  - should call get via IPC
  - should call get and return null when not found
  - should call set via IPC
  - should call set without TTL
  - should call delete via IPC
  - should call clear with pattern
  - should call clear without pattern
- **Sorted Set Operations**
  - should call zadd via IPC
  - should call zrangebyscore via IPC
  - should call zrem via IPC
- **Atomic Operations**
  - should call setIfNotExists via IPC and return true
  - should call setIfNotExists via IPC and return false

**`core/runtime/src/__tests__/config-interpolation.test.ts`**

- **interpolateString**
  - replaces a single placeholder
  - replaces multiple placeholders in one string
  - leaves non-placeholder text unchanged
  - returns unchanged string when no placeholders
  - throws on missing env var when required=true (default)
  - leaves placeholder unresolved when required=false
  - handles empty string
  - replaces same placeholder appearing twice
- **interpolateConfig**
  - passes numbers through unchanged
  - passes booleans through unchanged
  - passes null through unchanged
  - passes undefined through unchanged
  - interpolates top-level string
  - interpolates string values in a flat object
  - interpolates nested objects recursively
  - interpolates strings inside arrays
  - interpolates strings inside array of objects
  - throws on missing var in nested object when required=true
  - leaves missing placeholder unresolved when required=false
  - handles deeply nested mixed structure

**`core/runtime/src/__tests__/config-loader.test.ts`**

- **loadPlatformConfig**
  - merges project adapters when platform has none (adapters are mergeable)
  - loads only platform defaults when project has no config
  - adapters deep-merge: project overrides platform per key
  - returns empty-adapters base when neither layer exists
  - reads single file once when platformRoot === projectRoot (dev mode)
  - silently degrades when config file is malformed JSON
  - tolerates config without a `platform` section
  - loads .env file from projectRoot when loadEnvFile is true
  - does not load .env when loadEnvFile is false

**`core/runtime/src/__tests__/config-schemas.test.ts`**

- **ContainerExecutionConfig validation**
  - accepts a valid container config
  - rejects invalid gatewayDispatchUrl
  - rejects empty gatewayInternalSecret
  - accepts optional fields
  - rejects invalid pullPolicy
- **ExecutionConfig mode**
  - accepts mode: in-process
  - accepts mode: worker-pool with workerPool config
  - accepts mode: auto (no extra required fields)
  - accepts empty config (all optional)
  - rejects unknown mode
- **ExecutionConfig cross-field rules**
  - requires container block when mode=container
  - does not require container block when mode is not container
  - requires remote.endpoint when mode=remote
  - accepts mode=remote with endpoint
- **validateExecutionConfig error message**
  - includes all issue paths in the error message
  - lists multiple issues when there are multiple errors
- **ExecutionRetryConfig**
  - accepts valid retry config
  - rejects maxAttempts < 1
  - rejects negative initialDelayMs

**`core/runtime/src/__tests__/container.test.ts`**

- **PlatformContainer**
- **Singleton Creation**
  - should create singleton using Symbol.for()
  - should return same instance across multiple imports
  - should have initial state
- **Adapter Management**
  - should set and get adapter
  - should return undefined for non-existent adapter
  - should set multiple adapters
  - should replace existing adapter
- **NoOp Fallbacks**
  - should return NoOp analytics when not configured
  - should return MockLLM when not configured
  - should return MemoryCache when not configured
  - should return MemoryVectorStore when not configured
  - should return configured adapter instead of fallback
- **Core Features**
  - should return NoOp core features before initialization
  - should initialize core features
  - should include core features in configured services after init
- **Resource Broker**
  - should throw error when accessing resourceBroker before init
  - should initialize resource broker
  - should include resourceBroker in configured services after init
- **Service Configuration Check**
  - should detect configured adapters
  - should detect configured core features
  - should return all configured services
- **Platform Reset**
  - should clear all adapters
  - should clear core features
  - should clear resource broker
  - should reset to NoOp fallbacks
- **Lazy Getters**
  - should use lazy getter for llm
  - should use lazy getter for cache
  - should use lazy getter for vectorStore
- **Cross-Realm Singleton**
  - should use Symbol.for() for cross-realm access
  - should store singleton in process object
  - should maintain singleton across multiple accesses

**`core/runtime/src/__tests__/environment-lease-store.test.ts`**

- **EnvironmentLeaseStore**
  - ensures schema and upserts lease
  - maps expired rows from query result

**`core/runtime/src/__tests__/loader.test.ts`**

- **Platform Loader**
- **initPlatform - Basic Initialization**
  - should initialize platform with empty config
  - should initialize core features
  - should initialize resource broker
  - should be idempotent
  - should set isInitialized flag
- **Adapter Configuration**
  - should handle config with no adapters
  - should create ConfigAdapter automatically
  - should use NoOp fallbacks when adapters not configured
- **Core Features Configuration**
  - should initialize core features with default config
  - should pass core config to features
  - should initialize with resourceBroker config
- **Service Registration**
  - should register core features in configured services
  - should register config adapter in configured services
  - should detect configured vs fallback adapters
- **Reset and Re-initialization**
  - should reset platform state
  - should allow re-initialization after reset
  - should clear adapters on reset
  - should re-create core features after reset
- **Adapter Options**
  - should handle empty adapterOptions
  - should initialize without adapterOptions field
- **Multi-Adapter Configuration**
  - should accept single adapter as string (backward compatible)
  - should accept multiple adapters as array
  - should accept null adapter value (NoOp)
  - should handle mixed adapter formats
  - should use first adapter in array as primary
- **Platform Config Extraction**
  - should extract adapters from config
  - should handle partial config
  - should use defaults when config fields missing
- **Singleton Behavior**
  - should return same platform instance
  - should preserve adapters across calls
  - should use Symbol.for() for cross-realm singleton
- **Resource Broker Wrapping**
  - should not wrap adapters when not configured
  - should initialize resource broker even without adapters
- **Initialization Order**
  - should initialize in correct order
- **Graceful Degradation**
  - should continue if ConfigAdapter fails to import
  - should continue if core features initialization fails
  - should continue if ResourceBroker initialization fails
  - should continue if UnixSocketServer fails to start
  - should initialize all components independently
  - should handle partial initialization gracefully
- **Workflow Degradation**
  - should handle null workflows gracefully
  - should initialize other core features when workflows unavailable
- **ExecutionBackend Initialization**
  - should initialize ExecutionBackend by default
  - should initialize ExecutionBackend with in-process mode by default
  - should initialize ExecutionBackend with config from execution section
  - should respect execution mode from config
  - should register executionBackend in configured services if initialized
  - should allow access to executionBackend via platform if initialized
  - should shutdown ExecutionBackend when platform shuts down
  - should preserve ExecutionBackend across multiple init calls
  - should re-create ExecutionBackend after reset

**`core/runtime/src/__tests__/service-bootstrap.test.ts`**

- **createServiceBootstrap**
  - initializes platform with NoOp adapters when no kb.config.json exists
  - returns platform singleton
  - registers lifecycle hooks under appId without throwing
  - is idempotent — second call returns immediately without re-initializing
  - does not re-register lifecycle hooks on second call
  - loads .env from repoRoot by default
  - skips .env loading when loadEnv: false
  - does not overwrite already-set env vars
- **.env file parsing**
  - parses plain KEY=VALUE
  - strips double quotes
  - strips single quotes
  - strips backtick quotes
  - ignores comment lines
  - ignores empty lines
  - ignores lines without = separator
  - handles missing .env file gracefully
  - does not throw when storeRawConfig: false and no config file
  - resetServiceBootstrap allows re-initialization

**`core/runtime/src/__tests__/start-full-cycle.test.ts`**

- **startFullCycle**
  - delegates execution to run orchestrator

**`core/runtime/src/loader.test.ts`**

- **initPlatform - Manifest-based Context Injection**
- **Context Registry**
  - should create runtime contexts registry with workspace and analytics
- **Context Injection based on Manifest**
  - should inject only requested contexts from manifest
  - should inject only workspace context when adapter requests only workspace
  - should inject no contexts when manifest does not request any
  - should handle unknown context names gracefully
- **Config Building with Contexts**
  - should merge contexts with baseOptions, allowing baseOptions to override
  - should not inject contexts if baseOptions already has them
- **FileAnalytics Integration**
  - should pass workspace and analytics contexts to FileAnalytics via config
- **Backward Compatibility**
  - should handle adapters without contexts field in manifest

**`core/runtime/src/services/__tests__/hybrid-log-reader.test.ts`**

- **HybridLogReader**
- **constructor**
  - should accept both persistence and buffer
  - should accept only persistence
  - should accept only buffer
  - should accept neither (will error on use)
- **query**
  - should pass query options to persistence
- **query > with persistence**
  - should query from persistence when available
- **with buffer only**
  - should query from buffer when persistence unavailable
  - should apply pagination from buffer
  - should sort buffer results
- **with neither backend**
  - should throw error when no backend available
- **getById**
  - should get log from persistence when available
  - should fallback to buffer when persistence unavailable
  - should return null when log not found
  - should return null when no backend available
- **search**
  - should use persistence FTS when available
  - should fallback to simple text matching in buffer
  - should throw error when no backend available
- **subscribe**
  - should subscribe to buffer with no filters
  - should apply filters to subscription
  - should throw error when buffer unavailable
- **getStats**
  - should return combined stats from both backends
  - should return only buffer stats when persistence unavailable
  - should return empty stats when no backend available
- **getCapabilities**
  - should report all capabilities with both backends
  - should report limited capabilities with buffer only
  - should report limited capabilities with persistence only
  - should report no capabilities when no backend available

### `@kb-labs/core-sandbox`

#### Unit tests

**`core/sandbox/src/__tests__/abort-controller.test.ts`**

- **createTimeoutSignal**
  - returns an AbortSignal
  - aborts after timeout
  - does not abort before timeout

**`core/sandbox/src/__tests__/env-filter.test.ts`**

- **pickEnv**
  - returns only allowed variables
  - returns empty object when allowlist is empty
  - returns empty object when allowlist is undefined
  - skips variables not present in env
  - handles env with undefined values
  - returns empty object for empty env

**`core/sandbox/src/__tests__/handler-error.test.ts`**

- **normalizeError**
  - normalizes Error instance with code and details
  - defaults to HANDLER_CRASHED for Error without code
  - normalizes string error
  - normalizes object with code and message
  - normalizes object without code
  - normalizes null
  - normalizes undefined
  - normalizes number
- **HandlerErrorCode**
  - has all expected codes

**`core/sandbox/src/__tests__/handler-result.test.ts`**

- **normalizeHandlerResult**
  - normalizes number to HandlerResultObject
  - normalizes non-zero exit code
  - passes through HandlerResultObject as-is
  - normalizes AsyncIterable to exitCode 0 with data
  - preserves metadata fields

**`core/sandbox/src/__tests__/ipc-serializer.test.ts`**

- **serializeContext**
  - serializes minimal context
  - preserves optional fields when present
  - throws on undefined context
  - serializes adapter metadata when present
  - serializes CLI adapter context data
  - skips large extensions (>100KB)
  - skips function extensions
  - omits extensionsData when no serializable extensions
  - includes platformConfig when present

**`core/sandbox/src/__tests__/resource-tracker.test.ts`**

- **ResourceTracker**
  - tracks and removes temporary files on cleanup
  - runs cleanup callbacks
  - handles missing tmp files gracefully
  - handles failing cleanup callbacks gracefully
  - clears tracking after cleanup
  - supports multiple tmp files and callbacks

### `@kb-labs/core-state-broker`

#### Unit tests

**`core/state-broker/src/__tests__/http-broker.test.ts`**

- **HTTPStateBroker - Graceful Degradation**
  - should silently fail when daemon is down (set)
- **HTTPStateBroker - Graceful Degradation > Daemon Unavailable**
  - should return null when daemon is down (get)
  - should silently fail when daemon is down (delete)
  - should silently fail when daemon is down (clear)
  - should throw on malformed URL
- **URL Encoding**
  - should encode special characters in keys
  - should encode pattern in clear
- **Stop Operation**
  - should stop without errors

**`core/state-broker/src/__tests__/in-memory-broker.test.ts`**

- **InMemoryStateBroker**
- **Basic Operations**
  - should set and get value
  - should return null for non-existent key
  - should delete value
  - should clear all entries when no pattern
  - should clear entries by pattern prefix
- **TTL Expiration - Real Timing**
  - should expire entry after TTL
  - should return null when checking expired entry
  - should not expire entry before TTL
- **Background Cleanup**
  - should cleanup expired entries in background
- **Namespace Extraction**
  - should extract namespace from simple key
  - should extract namespace from tenant key
  - should use key itself as namespace for plain keys
  - should track multiple namespaces
- **Multi-Tenancy Stats**
  - should extract tenant from tenant-prefixed keys
  - should use default tenant for non-tenant keys
  - should track size per tenant
- **Statistics Tracking**
  - should track hits and misses
  - should track total entries
  - should estimate total size
  - should track uptime
  - should track evictions from cleanup
- **Health Status**
  - should return ok health status
  - should include stats in health
- **Cleanup on Stop**
  - should clear store on stop
  - should stop cleanup interval on stop
- **Edge Cases**
  - should handle large values
  - should handle special characters in keys
  - should handle null/undefined values
  - should handle concurrent operations

### `@kb-labs/core-state-daemon`

#### Unit tests

**`plugins/state/daemon/core-state-daemon/src/__tests__/server.test.ts`**

- **StateDaemonServer**
- **Health Endpoints**
  - should return health status
  - should return stats
  - should expose versioned observability documents
  - should return prometheus metrics
- **State Operations**
  - should set and get value
  - should return 404 for non-existent key
  - should delete value
  - should clear all entries
  - should clear entries by pattern
- **TTL Expiration - Real Timing**
  - should expire entry after TTL
- **Special Characters in Keys**
  - should handle URL-encoded keys
  - should handle colon-separated keys
- **Large Payloads**
  - should handle large values
- **Concurrent Operations**
  - should handle concurrent requests
- **CORS Headers**
  - should include CORS headers
  - should handle OPTIONS preflight
- **Error Handling**
  - should return 404 for unknown endpoint
  - should handle malformed JSON
- **Statistics Tracking**
  - should track namespace stats
  - should track hit/miss rates

**`plugins/state/daemon/src/__tests__/bootstrap.test.ts`**

- **StateDaemonServer construction**
  - creates server with custom port and host
  - creates server with default config
  - creates server with custom logger
- **manifest**
  - has correct schema and id
  - specifies correct default port 7777
  - specifies health check endpoint
  - has entry point defined

**`plugins/state/daemon/src/__tests__/server.test.ts`**

- **StateDaemonServer**
- **Health Endpoints**
  - should return health status
  - should return stats
  - should expose versioned observability documents
  - should return prometheus metrics
- **State Operations**
  - should set and get value
  - should return 404 for non-existent key
  - should delete value
  - should clear all entries
  - should clear entries by pattern
- **TTL Expiration - Real Timing**
  - should expire entry after TTL
- **Special Characters in Keys**
  - should handle URL-encoded keys
  - should handle colon-separated keys
- **Large Payloads**
  - should handle large values
- **Concurrent Operations**
  - should handle concurrent requests
- **CORS Headers**
  - should include CORS headers
  - should handle OPTIONS preflight
- **Error Handling**
  - should return 404 for unknown endpoint
  - should handle malformed JSON
- **Statistics Tracking**
  - should track namespace stats
  - should track hit/miss rates

### `@kb-labs/core-sys`

#### E2E / journey tests

**`core/sys/src/fs/__tests__/fs.spec.ts`**

- **toAbsolute**
  - returns baseDir when maybeRelative is falsy
  - returns absolute path as-is
  - joins relative path to base

**`core/sys/src/repo/__tests__/repo.spec.ts`**

- **findRepoRoot**
  - finds directory containing any marker (e.g., .git) when called from subdir
  - falls back to start directory if no marker is found up the tree
- **discoverSubRepos**
  - parses .gitmodules and returns SubRepo objects
  - falls back to flat layout scan when .gitmodules is absent
  - returns empty array when no sub-repos exist

### `@kb-labs/core-tenant`

#### Unit tests

**`core/tenant/src/__tests__/rate-limiter.test.ts`**

- **TenantRateLimiter**
  - allows requests under the limit
  - blocks requests when limit is reached
  - decrements remaining count with each request
  - uses free tier quotas by default (no quota set)
  - uses pro tier quotas
  - uses enterprise tier quotas
  - supports custom quotas
  - rate limits workflows separately from requests
  - rate limits plugins resource
  - isolates rate limits between tenants
  - resetAt is within the next minute
  - trackTokens accumulates consumption across calls
  - trackTokens blocks when daily budget is exhausted
  - trackTokens is unlimited for enterprise
  - getTokenUsage returns current consumption without incrementing
- **types — helper functions**
  - getDefaultTenantId returns "default" when no env var
  - getDefaultTenantTier returns "free" when no env var
  - getQuotasForTier returns a copy of tier quotas
  - DEFAULT_QUOTAS has all tiers defined
  - enterprise tier has unlimited daily requests (-1)

### `@kb-labs/core-workspace`

#### E2E / journey tests

**`core/workspace/src/workspace/__tests__/root-resolver.spec.ts`**

- **resolveProjectRoot**
  - returns explicit cwd when provided
  - prefers KB_PROJECT_ROOT when set
  - accepts legacy KB_LABS_WORKSPACE_ROOT for backwards compatibility
  - accepts legacy KB_LABS_REPO_ROOT for backwards compatibility
  - finds nearest kb-labs config ancestor
  - falls back to repo root when config missing
  - returns fallback when no markers found
- **resolveWorkspaceRoot (deprecated alias)**
  - behaves identically to resolveProjectRoot
- **resolvePlatformRoot**
  - returns explicit cwd when provided
  - prefers KB_PLATFORM_ROOT env var
  - resolves platform root via moduleUrl when node_modules/@kb-labs/cli-bin is present
  - walks up from startDir to find node_modules/@kb-labs/cli-bin
  - walks up from startDir to find pnpm-workspace.yaml (dev mode)
  - prefers pnpm-workspace.yaml above a nested package marker (dev-mode correction)
  - prefers top-most pnpm-workspace.yaml in nested-workspace layouts
  - ignores platform markers inside a pnpm virtual store
  - falls back to repo root when no markers match
- **resolveRoots**
  - returns sameLocation: true in dev-mode workspace (both resolve to same root)
  - returns sameLocation: false in installed mode
  - env vars override discovery for both roots independently

### `@kb-labs/e2e`

#### E2E / journey tests

**`e2e/tests/specs/adapters/llm.spec.ts`**

- A-01: LLM adapter registered in REST API
- A-02: storage adapter registered
- A-03: LLM adapter responds to a real completion request

**`e2e/tests/specs/commit/ai-commit.spec.ts`**

- CM-01: commit plugin registered in platform
- CM-02: commit plugin manifest has commit command

**`e2e/tests/specs/gateway/auth.spec.ts`**

- GW-01: invalid token on gateway route → 401
- GW-02: missing token on gateway protected route → 401
- GW-03: auth/token endpoint exists and rejects bad credentials
- GW-04: /health is public (no auth required)
- GW-05: valid token → protected route returns 200
- GW-06: refresh token endpoint works and returns valid access token
- GW-07: register → token → authenticated request succeeds end-to-end

**`e2e/tests/specs/gateway/cross-service.spec.ts`**

- XS-01: authenticated request passes through gateway to REST upstream
- XS-02: gateway rejects proxied request without token
- XS-03: gateway routes requests to correct upstream (workflow)
- XS-04: expired/invalid token rejected on gateway-owned routes

**`e2e/tests/specs/marketplace/lifecycle.spec.ts`**

- ML-01: install package → appears in GET /packages
- ML-02: disable package → disabled flag set in listing
- ML-03: enable package → enabled flag restored in listing
- ML-04: POST /packages/update — responds without error
- ML-05: uninstall package → gone from GET /packages

**`e2e/tests/specs/marketplace/packages.spec.ts`**

- MKT-01: marketplace lists installed packages
- MKT-02: marketplace diagnostics — lock file OK, no errors
- MKT-03: install a test package and verify it appears in listing
- MKT-04: install entity from remote registry
- MKT-05: disable package → commands disappear from CLI

**`e2e/tests/specs/marketplace/sync.spec.ts`**

- MS-01: POST /workspace/sync — returns structured response
- MS-02: POST /workspace/sync — idempotent (second call ≤ first)
- MS-03: sync with autoEnable=true — packages appear in listing

**`e2e/tests/specs/platform/config.spec.ts`**

- CFG-01: platform config endpoint responds
- CFG-02: platform config has adapters section (no empty config)
- CFG-03: LLM adapter is configured (not placeholder)
- CFG-04: storage adapter is configured

**`e2e/tests/specs/platform/plugins.spec.ts`**

- PL-01: plugin registry is populated after startup
- PL-02: plugin registry health — no validation errors
- PL-03: studio plugin registry loaded (MF pages)
- PL-04: commit plugin is registered and has expected commands
- PL-05: scaffold plugin is registered and has expected templates
- PL-06: workflow plugin is registered with daemon URL

**`e2e/tests/specs/platform/roots.spec.ts`**

- ROOT-01: platform config endpoint is reachable and schema is valid
- ROOT-02: projectRoot config overrides platformRoot adapter config
- ROOT-03: plugin registry uses projectRoot for local plugin discovery
- ROOT-04: start with custom projectRoot env var → config reflects that root
- ROOT-05: workflow daemon discovers .kb/workflows from projectRoot not platformRoot
- ROOT-06: marketplace lock file is read from projectRoot/.kb/marketplace.lock

**`e2e/tests/specs/plugins/discovery.spec.ts`**

- P-01: platform plugin registry contains expected plugins after boot
- P-02: rest-api /api/v1/routes lists registered routes
- P-03: unknown gateway route returns 404 not 500
- P-04: plugin commands appear in kb --help
- P-05: plugin manifest loads without errors

**`e2e/tests/specs/review/cli.spec.ts`**

- RV-01: review plugin is registered in platform
- RV-02: review heuristic command registered
- RV-03: review-entry plugin manifest loads correctly

**`e2e/tests/specs/scaffold/plugin.spec.ts`**

- SC-01: scaffold lists available templates
- SC-02: scaffold plugin template has required structure
- SC-03: scaffold run creates plugin structure

**`e2e/tests/specs/services/health.spec.ts`**

- ${svc.id}: ${svc.name} is healthy

**`e2e/tests/specs/services/peer-deps.spec.ts`**

- PD-01: gateway health reports consistent version
- PD-02: services report consistent platform version

**`e2e/tests/specs/services/ready.spec.ts`**

- R-01: rest-api /ready — registry loaded and plugins mounted
- R-02: workflow /ready — engine + catalog + scheduler ready
- R-03: state-daemon /ready — broker ready
- R-04: rest-api /ready — all plugin routes registered
- R-05: marketplace /ready — registry loaded

**`e2e/tests/specs/services/state.spec.ts`**

- SD-01: PUT then GET returns stored value
- SD-02: DELETE removes the key (GET returns 404 after)
- SD-03: GET on missing key returns 404
- SD-04: stored value can be overwritten
- SD-05: TTL expires entry (1s TTL, checked after 1.5s)
- SD-06: stats reflect stored entries
- SD-07: clear with pattern removes matching keys

**`e2e/tests/specs/studio/app.spec.ts`**

- SA-01: gateway / — returns HTML with <html> tag
- SA-02: MF manifest — valid JSON with remotes map
- SA-03: MF chunk for workflow plugin resolves
- SA-04: REST /api/v1/status responds for studio health bar
- SA-05: static assets have caching headers

**`e2e/tests/specs/studio/mf-loading.spec.ts`**

- ST-01: gateway serves studio app
- ST-02: gateway serves MF manifest JSON
- ST-03: all MF chunks load without 404
- ST-04: studio app renders without JS console errors

**`e2e/tests/specs/studio/mf-proxy.spec.ts`**

- MF-01: MF manifest is served with correct Content-Type
- MF-02: remoteEntry.js served as JavaScript (not HTML/text)
- MF-03: MF assets have CORS headers (required for cross-origin MF loading)
- MF-04: JS chunks return 200 not redirect (proxy rewrite must not double-prefix)
- MF-05: unknown studio asset returns 404 not 500
- MF-06: all remoteEntry URLs in manifest resolve to 200
- MF-07: studio app boots in browser without JS errors (Playwright page test)

**`e2e/tests/specs/studio/mf-widgets.spec.ts`**

- MW-01: REST /api/v1/studio/registry — returns plugin list
- MW-02: every remoteEntryUrl resolves to application/javascript (not HTML)
- MW-03: workflow remoteEntry.js — returns JavaScript through gateway
- MW-04: gateway /plugins/* proxy — no double-prefix redirect
- MW-05: remoteEntry.js has short Cache-Control (must-revalidate)
- MW-06: hash-named JS chunks served correctly through gateway
- MW-07: remoteEntry.js has CORS headers for cross-origin MF loading

**`e2e/tests/specs/studio/realtime.spec.ts`**

- RT-01: WebSocket upgrade succeeds through gateway (/hosts/connect)
- RT-02: SSE endpoint streams events (not buffered by proxy)
- RT-03: gateway WS route is registered (not 404)
- RT-04: workflow status updates delivered over WS in real-time
- RT-05: SSE reconnects after gateway restart within 5s

**`e2e/tests/specs/workflows/basic.spec.ts`**

- W-01: create workflow run returns run ID
- W-02: GET /runs/:id returns a valid initial status
- W-03: e2e-hello workflow reaches completed within 30s
- W-04: e2e-fail workflow reaches failed status within 30s

**`e2e/tests/specs/workflows/discovery.spec.ts`**

- WFD-01: workflow catalog endpoint is accessible
- WFD-02: cron catalog is populated from .kb/workflows
- WFD-03: workflow refresh rescans .kb/workflows without restart
- WFD-04: workflow found by name from .kb/workflows is runnable
- WFD-05: workflow defined in projectRoot/.kb/workflows overrides platformRoot definition
- WFD-06: invalid workflow YAML in .kb/workflows is reported in /ready diagnostics

**`e2e/tests/specs/workflows/engine.spec.ts`**

- WF-01: workflow stats endpoint responds with counts
- WF-02: workflow catalog is populated (e2e-hello is present)
- WF-03: e2e-hello run reaches terminal state within 30s
- WF-04: runs list is accessible and includes recent run
- WF-05: stats running count increases during active run
- WF-06: e2e-fail workflow ends with failed status
- WF-07: cron catalog endpoint is accessible

**`e2e/tests/specs/workflows/journey.spec.ts`**

- J-01: run → reaches success within 30s
- J-02: run → cancel → status is cancelled or failed
- J-03: run detail has required fields and jobs endpoint responds 200
- J-04: list runs → newly started run appears in the list

**`e2e/tests/specs/workflows/plugin-templates.spec.ts`**

- WFD-P01: plugin workflow templates appear in catalog
- WFD-P02: plugin workflow template has source=plugin
- WFD-P03: plugin workflow template has description
- WFD-P04: plugin template is resolvable by ID

**`e2e/tests/specs/workflows/runs.spec.ts`**

- WR-01: GET /runs — returns a runs array
- WR-02: GET /runs?status=success — returns only success runs
- WR-03: POST /workflows/:id/runs — returns a valid runId
- WR-04: GET /runs/:runId — returns run detail
- WR-05: run reaches terminal state within 30s
- WR-06: POST cancel — accepted for a running or pending run
- WR-07: GET /jobs — returns a jobs array
- WR-08: REST proxy GET /plugins/workflow/runs responds 200
- WR-09: REST proxy — POST /plugins/workflow/workflows/:id/run returns runId

### `@kb-labs/e2e-gateway`

#### E2E / journey tests

**`e2e/gateway/specs/auth.spec.ts`**

- GW-01: invalid token on gateway route → 401
- GW-02: missing token on gateway protected route → 401
- GW-03: auth/token endpoint exists and rejects bad credentials
- GW-04: /health is public (no auth required)
- GW-05: valid token → protected route returns 200
- GW-06: refresh token endpoint works and returns valid access token
- GW-07: register → token → authenticated request succeeds end-to-end

**`e2e/gateway/specs/cross-service.spec.ts`**

- XS-01: authenticated request passes through gateway to REST upstream
- XS-02: gateway rejects proxied request without token
- XS-03: gateway routes requests to correct upstream (workflow)
- XS-04: expired/invalid token rejected on gateway-owned routes

**`e2e/gateway/specs/ws-heartbeat.spec.ts`**

- WS-GH01: heartbeat round-trip — client ping receives server ack
- WS-GH02: invalid token → connection rejected immediately
- WS-GH03: clean disconnect — gateway remains healthy after client close

### `@kb-labs/e2e-marketplace`

#### E2E / journey tests

**`e2e/marketplace/specs/lifecycle.spec.ts`**

- ML-01: install package → appears in GET /packages
- ML-02: disable package → disabled flag set in listing
- ML-03: enable package → enabled flag restored in listing
- ML-04: POST /packages/update — responds without error
- ML-05: uninstall package → gone from GET /packages

**`e2e/marketplace/specs/packages.spec.ts`**

- MKT-01: marketplace lists installed packages
- MKT-02: marketplace diagnostics — lock file OK, no errors
- MKT-03: install a test package and verify it appears in listing
- MKT-04: install entity from remote registry
- MKT-05: disable package → commands disappear from CLI

**`e2e/marketplace/specs/sync.spec.ts`**

- MS-01: POST /workspace/sync — returns structured response
- MS-02: POST /workspace/sync — idempotent (second call ≤ first)
- MS-03: sync with autoEnable=true — packages appear in listing

### `@kb-labs/e2e-platform`

#### E2E / journey tests

**`e2e/platform/specs/config.spec.ts`**

- CFG-01: platform config endpoint responds
- CFG-02: platform config has adapters section (no empty config)
- CFG-03: LLM adapter is configured (not placeholder)
- CFG-04: storage adapter is configured

**`e2e/platform/specs/plugins.spec.ts`**

- PL-01: plugin registry is populated after startup
- PL-02: plugin registry health — no validation errors
- PL-03: studio plugin registry loaded (MF pages)
- PL-04: commit plugin is registered and has expected commands
- PL-05: scaffold plugin is registered and has expected templates
- PL-06: workflow plugin is registered with daemon URL

**`e2e/platform/specs/roots.spec.ts`**

- ROOT-01: platform config endpoint is reachable and schema is valid
- ROOT-02: projectRoot config overrides platformRoot adapter config
- ROOT-03: plugin registry uses projectRoot for local plugin discovery
- ROOT-04: start with custom projectRoot env var → config reflects that root
- ROOT-05: workflow daemon discovers .kb/workflows from projectRoot not platformRoot
- ROOT-06: marketplace lock file is read from projectRoot/.kb/marketplace.lock

### `@kb-labs/e2e-plugins`

#### E2E / journey tests

**`e2e/plugins/specs/adapters.spec.ts`**

- A-01: LLM adapter registered in REST API
- A-02: storage adapter registered
- A-03: LLM adapter responds to a real completion request

**`e2e/plugins/specs/commit.spec.ts`**

- CM-01: commit plugin registered in platform
- CM-02: commit plugin manifest has commit command

**`e2e/plugins/specs/discovery.spec.ts`**

- P-01: platform plugin registry contains expected plugins after boot
- P-02: rest-api /api/v1/routes lists registered routes
- P-03: unknown gateway route returns 404 not 500
- P-04: plugin commands appear in kb --help
- P-05: plugin manifest loads without errors

**`e2e/plugins/specs/review.spec.ts`**

- RV-01: review plugin is registered in platform
- RV-02: review heuristic command registered
- RV-03: review-entry plugin manifest loads correctly

**`e2e/plugins/specs/scaffold.spec.ts`**

- SC-01: scaffold lists available templates
- SC-02: scaffold plugin template has required structure
- SC-03: scaffold run creates plugin structure

### `@kb-labs/e2e-services`

#### E2E / journey tests

**`e2e/services/specs/health.spec.ts`**

- ${svc.id}: ${svc.name} is healthy

**`e2e/services/specs/peer-deps.spec.ts`**

- PD-01: gateway health reports consistent version
- PD-02: services report consistent platform version

**`e2e/services/specs/ready.spec.ts`**

- R-01: rest-api /ready — registry loaded and plugins mounted
- R-02: workflow /ready — engine + catalog + scheduler ready
- R-03: state-daemon /ready — broker ready
- R-04: rest-api /ready — all plugin routes registered
- R-05: marketplace /ready — registry loaded

**`e2e/services/specs/state.spec.ts`**

- SD-01: PUT then GET returns stored value
- SD-02: DELETE removes the key (GET returns 404 after)
- SD-03: GET on missing key returns 404
- SD-04: stored value can be overwritten
- SD-05: TTL expires entry (1s TTL, checked after 1.5s)
- SD-06: stats reflect stored entries
- SD-07: clear with pattern removes matching keys

### `@kb-labs/e2e-studio`

#### E2E / journey tests

**`e2e/studio/specs/app.spec.ts`**

- SA-01: gateway / — returns HTML with <html> tag
- SA-02: MF manifest — valid JSON with remotes map
- SA-03: MF chunk for workflow plugin resolves
- SA-04: REST /api/v1/status responds for studio health bar
- SA-05: static assets have caching headers

**`e2e/studio/specs/mf-loading.spec.ts`**

- ST-01: gateway serves studio app
- ST-02: gateway serves MF manifest JSON
- ST-03: all MF chunks load without 404
- ST-04: studio app renders without JS console errors

**`e2e/studio/specs/mf-proxy.spec.ts`**

- MF-01: MF manifest is served with correct Content-Type
- MF-02: remoteEntry.js served as JavaScript (not HTML/text)
- MF-03: MF assets have CORS headers (required for cross-origin MF loading)
- MF-04: JS chunks return 200 not redirect (proxy rewrite must not double-prefix)
- MF-05: unknown studio asset returns 404 not 500
- MF-06: all remoteEntry URLs in manifest resolve to 200
- MF-07: studio app boots in browser without JS errors (Playwright page test)

**`e2e/studio/specs/mf-widgets.spec.ts`**

- MW-01: REST /api/v1/studio/registry — returns plugin list
- MW-02: every remoteEntryUrl resolves to application/javascript (not HTML)
- MW-03: workflow remoteEntry.js — returns JavaScript through gateway
- MW-04: gateway /plugins/* proxy — no double-prefix redirect
- MW-05: remoteEntry.js has short Cache-Control (must-revalidate)
- MW-06: hash-named JS chunks served correctly through gateway
- MW-07: remoteEntry.js has CORS headers for cross-origin MF loading

**`e2e/studio/specs/realtime.spec.ts`**

- RT-01: WebSocket upgrade succeeds through gateway (/hosts/connect)
- RT-02: SSE endpoint streams events (not buffered by proxy)
- RT-03: gateway WS route is registered (not 404)
- RT-04: workflow status updates delivered over WS in real-time
- RT-05: SSE reconnects after gateway restart within 5s

### `@kb-labs/e2e-workflows`

#### SSE integration tests

**`e2e/workflows/specs/sse/backpressure.spec.ts`**

- SE-B01: burst — all events received in order without loss
- SE-B02: no duplicate events even after burst
- SE-B03: stream does not hang after terminal event

**`e2e/workflows/specs/sse/reconnect.spec.ts`**

- SE-R01: reconnect does not duplicate live events
- SE-R02: run.finished is not missed on reconnect to already-terminal run
- SE-R03: rapid reconnect delivers run.finished without data loss

**`e2e/workflows/specs/sse/runs-events.spec.ts`**

- SE-01: run.snapshot arrives first, terminal event closes stream
- SE-02: no duplicate events in the stream
- SE-03: already-terminal run closes stream immediately (< 3s)
- SE-04: invalid runId returns 404, not an SSE stream
- SE-05: multiple simultaneous subscribers receive same events

#### WebSocket integration tests

**`e2e/workflows/specs/ws/logs-channel.spec.ts`**

- WS-L01: subscribe → receive log stream
- WS-L02: level filter — warn level excludes debug/info messages
- WS-L03: unknown jobId — server sends error message, does not hang
- WS-L04: unsubscribe stops the log stream
- WS-L05: client disconnect does not leak — server handles cleanup

**`e2e/workflows/specs/ws/progress-channel.spec.ts`**

- WS-P01: subscribe → server acknowledges (partial implementation)
- WS-P02: unknown jobId → error message, no hang

#### E2E / journey tests

**`e2e/workflows/specs/basic.spec.ts`**

- W-01: create workflow run returns run ID
- W-02: GET /runs/:id returns a valid initial status
- W-03: e2e-hello workflow reaches completed within 30s
- W-04: e2e-fail workflow reaches failed status within 30s

**`e2e/workflows/specs/discovery.spec.ts`**

- WFD-01: workflow catalog endpoint is accessible
- WFD-02: cron catalog is populated from .kb/workflows
- WFD-03: workflow refresh rescans .kb/workflows without restart
- WFD-04: workflow found by name from .kb/workflows is runnable
- WFD-05: workflow defined in projectRoot/.kb/workflows overrides platformRoot definition
- WFD-06: invalid workflow YAML in .kb/workflows is reported in /ready diagnostics

**`e2e/workflows/specs/engine.spec.ts`**

- WF-01: workflow stats endpoint responds with counts
- WF-02: workflow catalog is populated (e2e-hello is present)
- WF-03: e2e-hello run reaches terminal state within 30s
- WF-04: runs list is accessible and includes recent run
- WF-05: stats running count increases during active run
- WF-06: e2e-fail workflow ends with failed status
- WF-07: cron catalog endpoint is accessible

**`e2e/workflows/specs/plugin-templates.spec.ts`**

- WFD-P01: plugin workflow templates appear in catalog
- WFD-P02: plugin workflow template has source=plugin
- WFD-P03: plugin workflow template has description
- WFD-P04: plugin template is resolvable by ID

**`e2e/workflows/specs/runs.spec.ts`**

- WR-01: GET /runs — returns a runs array
- WR-02: GET /runs?status=success — returns only success runs
- WR-03: POST /workflows/:id/runs — returns a valid runId
- WR-04: GET /runs/:runId — returns run detail
- WR-05: run reaches terminal state within 30s
- WR-06: POST cancel — accepted for a running or pending run
- WR-07: GET /jobs — returns a jobs array
- WR-08: REST proxy GET /plugins/workflow/runs responds 200
- WR-09: REST proxy — POST /plugins/workflow/workflows/:id/run returns runId

### `@kb-labs/gateway-app`

#### Integration tests

**`plugins/gateway/app/src/__tests__/server.integration.test.ts`**

- **GET /health (public)**
  - returns 200 without auth
- **POST /hosts/register (public)**
  - registers a host and returns 201 with hostId + machineToken
  - returns 400 for missing required fields
  - returns 400 for invalid capability enum value
  - does not require auth
- **Auth middleware**
  - returns 401 for protected routes without Authorization
  - returns 401 for unknown Bearer token (no CLI fallback)
  - passes protected routes with valid machine token
  - /health is public — no 401
  - /hosts/register is public — no 401
  - machine token resolves correctly
- **POST /internal/dispatch**
  - returns 403 with wrong secret
  - returns 403 with no secret header
  - returns 400 when namespaceId is missing
  - returns 400 when adapter is missing
  - returns 503 when no host is connected in namespace
  - routes call to provided hostId and returns result
  - falls back to firstHost when no hostId provided
  - returns 502 when dispatcher.call throws

#### E2E / journey tests

**`plugins/gateway/app/src/__tests__/gateway-auth-errors.e2e.test.ts`**

- **Gateway auth errors**
  - POST /api/v1/execute → 401
- **Gateway auth errors > missing Authorization header**
  - GET /hosts → 401
- **malformed Authorization header**
  - plain token without "Bearer " prefix → 401
  - empty Bearer value → 401
- **invalid JWT**
  - GET /hosts with garbage JWT → 401
  - GET /hosts with wrong-signature JWT → 401
- **invalid client credentials**
  - POST /auth/token with unknown clientId → 401
  - POST /auth/token with wrong secret for real clientId → 401
- **invalid refresh token**
  - POST /auth/refresh with garbage refreshToken → 401

**`plugins/gateway/app/src/__tests__/gateway-cancel.e2e.test.ts`**

- **Gateway execution cancel**
  - POST /api/v1/execute/:id/cancel → execution:done(exitCode=130)
  - POST /api/v1/execute/:id/cancel with nonexistent id → 4xx
  - POST /api/v1/execute/:id/cancel without auth → 401

**`plugins/gateway/app/src/__tests__/live-gateway.e2e.test.ts`**

- returns { status: healthy|degraded }
- POST /auth/register returns clientId + clientSecret + hostId
- POST /auth/token returns accessToken + refreshToken
- POST /auth/token returns 401 for wrong credentials
- POST /auth/refresh rotates refresh token
- JWT token is accepted on protected endpoint GET /hosts
- static dev-studio-token is accepted on protected endpoint
- GET /hosts returns 401 without token
- POST /hosts/register returns hostId + machineToken
- host WS handshake: hello → connected (machineToken auth)
- host goes online in registry after WS handshake
- heartbeat gets ack response
- WS closes with 1008 if no Authorization header
- streams execution:done(exitCode=0) when host responds to call
- streams execution:error + execution:done(exitCode=1) when host returns error
- returns 503 when no host is connected for namespace
- returns 401 without token
- returns 400 for missing required fields
- cancels an in-flight execution and streams execution:cancelled + done(exitCode=130)
- client receives execution events via WS subscription
- client:subscribe to nonexistent execution returns client:error
- client:cancel cancels execution and aborts host call

**`plugins/gateway/app/src/__tests__/ws-client-connect.e2e.test.ts`**

- **WebSocket /clients/connect: auth rejection**
  - closes immediately when no Authorization header
  - closes with 1008 for invalid token
- **WebSocket /clients/connect: handshake**
  - completes client:hello → client:connected flow
  - closes with 1008 if client:hello not sent within timeout
  - closes with 1008 if client:hello has invalid format
  - access_token query param is accepted as fallback auth
- **WebSocket /clients/connect: subscribe / unsubscribe**
  - receives client:error(EXECUTION_NOT_FOUND) when subscribing to unknown execution
  - receives client:error(FORBIDDEN) when subscribing to execution in different namespace
  - does not error on unsubscribe from unknown execution (graceful)
  - receives client:error(INVALID_MESSAGE) for malformed JSON
  - receives client:error(INVALID_MESSAGE) for unknown message type
- **WebSocket /clients/connect: cancel**
  - receives client:error(EXECUTION_NOT_FOUND) when cancelling unknown execution
  - cancels execution and aborts the signal
  - receives client:error(CANCEL_FAILED) when execution already cancelled

**`plugins/gateway/app/src/__tests__/ws-handshake.e2e.test.ts`**

- **WebSocket: connection refused without auth**
  - closes immediately when no Authorization header
  - closes with 1008 for invalid machine token
- **WebSocket: full handshake**
  - completes hello → connected flow
  - heartbeat gets ack response
  - rejects unsupported protocol version
  - closes with 1008 if hello not sent within timeout
- **WebSocket: host status lifecycle**
  - host goes online after successful handshake

#### Unit tests

**`plugins/gateway/app/src/__tests__/auth-routes.test.ts`**

- **POST /auth/register**
  - returns 201 with clientId, clientSecret, hostId, namespaceId on success
  - rejects client-supplied namespaceId (must not be passed to authService)
  - returns 400 when name is missing
  - returns 400 when body is empty
  - forwards capabilities array to authService
- **POST /auth/token**
  - returns 200 with token pair on valid credentials
  - returns 401 when credentials are invalid (issueTokens returns null)
  - returns 400 when clientId is missing
  - returns 400 when clientSecret is missing
  - returns 400 when body is empty
- **POST /auth/refresh**
  - returns 200 with new token pair on valid refresh token
  - returns 401 when refresh token is invalid (refreshTokens returns null)
  - returns 400 when refreshToken field is missing
  - returns 400 when body contains wrong field
- **Auth middleware — public routes**
  - /auth/register is accessible without Authorization header

**`plugins/gateway/app/src/__tests__/bootstrap-jwt-secret.test.ts`**

- **bootstrap — JWT secret validation (H6)**
  - throws when NODE_ENV=production and GATEWAY_JWT_SECRET is not set
  - does not throw in dev mode when GATEWAY_JWT_SECRET is not set
  - does not throw in production when GATEWAY_JWT_SECRET is set

**`plugins/gateway/app/src/__tests__/execute-routes.test.ts`**

- **POST /api/v1/execute — success**
  - returns 200 with ndjson content-type
  - response body contains execution:done as last event
  - X-Execution-Id header is present in response
  - dispatches to correct namespace and host
  - broadcasts execution:done event to WS subscribers
  - execution is removed from registry after completion
- **POST /api/v1/execute — error cases**
  - returns 400 when pluginId is missing
  - returns 400 when handlerRef is missing
  - returns 503 when no host is connected for namespace
  - on dispatch failure: streams execution:error + execution:done(exitCode=1)
- **POST /api/v1/execute/:id/cancel**
  - returns 200 when execution is found and cancelled
  - returns 404 when execution does not exist
  - returns 403 when execution belongs to different namespace
  - returns 409 when execution is already cancelled
  - defaults reason to "user" when not provided
- **POST /api/v1/execute — auth guard**
  - returns 401 when authContext is absent
  - cancel returns 401 when authContext is absent

**`plugins/gateway/app/src/__tests__/execution-registry.test.ts`**

- **ExecutionRegistry**
- **register()**
  - returns an AbortSignal
  - signal is not aborted initially
  - stores execution metadata
  - increments size
- **cancel()**
  - returns true and aborts signal
  - stores cancellation reason on the entry
  - returns false for non-existent executionId
  - returns false when already cancelled (idempotent)
  - abort signal reason matches provided reason
- **remove()**
  - removes execution from registry
  - decrements size
  - is no-op for non-existent id
- **get()**
  - returns undefined for unknown id
  - returns the active execution
- **cancelByHost()**
  - cancels all executions for given hostId
  - returns empty array when no executions for host
  - skips already-aborted executions
  - returns executionIds of cancelled executions
- **size**
  - starts at 0
  - tracks additions and removals correctly
- **CancelledError**
  - stores reason
  - has descriptive message
  - has correct name
  - is instanceof Error
  - is instanceof CancelledError
  - supports all cancellation reasons

**`plugins/gateway/app/src/__tests__/health.test.ts`**

- **Gateway /health endpoint**
  - returns healthy when all adapters available
  - returns degraded when non-critical adapter missing
  - returns unhealthy when LLM unavailable
  - includes uptime and timestamp
  - reports adapter latency
  - probes upstream health
  - logs structured diagnostics when upstream health probe fails

**`plugins/gateway/app/src/__tests__/llm-gateway.test.ts`**

- **POST /llm/v1/chat/completions**
  - returns 401 without auth token
  - returns 400 with empty body
  - returns 400 with invalid tier
  - returns 400 with no messages
  - returns 200 with OpenAI-compatible response (non-streaming)
  - passes temperature and max_tokens to LLM
  - extracts system prompt from messages
  - returns tool_calls in OpenAI format when LLM requests tools
  - returns SSE stream with correct format
  - returns 500 when LLM throws
  - accepts all valid tiers: small, medium, large

**`plugins/gateway/app/src/__tests__/observability-collector.test.ts`**

- **GatewayObservabilityCollector**
  - builds versioned describe payload
  - builds health payload and exposes canonical metrics

**`plugins/gateway/app/src/__tests__/platform-api.test.ts`**

- **Unified Platform API – POST /platform/v1/:adapter/:method**
  - returns 401 without auth
  - returns 404 for unknown adapter
  - returns 403 for disallowed method
  - returns 503 when adapter not configured
  - cache/get returns cached value
  - cache/set stores value
  - llm/complete proxies to LLM
  - analytics/track proxies to analytics
  - returns 502 when adapter method throws
  - includes durationMs in response
  - passes args correctly to adapter method
  - llm/stream returns SSE format

**`plugins/gateway/app/src/__tests__/registry.test.ts`**

- **HostRegistry.register**
  - creates a new host descriptor with offline status
  - stores descriptor and token in cache
  - generates unique hostId and token per registration
- **HostRegistry.setOnline / setOffline**
  - sets status to online and adds connectionId
  - deduplicates connections on repeated setOnline
  - each setOnline replaces connections with the new connectionId
  - enters reconnecting status when last connection removed (grace period)
  - stays online when disconnected connectionId is not the current one
  - does nothing if host not found
- **HostRegistry.heartbeat**
  - updates lastSeen timestamp
- **HostRegistry.get**
  - returns host descriptor by id
  - returns null for unknown host
- **HostRegistry.resolveToken**
  - returns hostId/namespaceId for valid machine token
  - returns null for unknown token
- **HostRegistry with IHostStore (dual-layer)**
- **HostRegistry with IHostStore (dual-layer) > register**
  - writes to both cache and store
- **restore**
  - loads all hosts from store into cache as offline
  - returns 0 when store is empty
  - returns 0 when no store provided
- **get (cache miss → store fallback)**
  - falls through to store when cache misses
  - returns null when both cache and store miss
  - prefers cache over store
- **resolveToken (cache miss → store fallback)**
  - falls through to store when cache misses
- **list (store authoritative)**
  - uses store as source of truth, enriches with cache status
- **deregister**
  - removes from both cache and store
  - returns false for non-existent host
- **ensureRegistered**
  - persists to store when host does not exist
  - updates capabilities in store when they change
- **HostRegistry grace period**
  - sets status to reconnecting on last connection close
  - cancels grace timer when host reconnects before expiry
  - transitions to offline after grace period expires
  - stays online when other connections remain

**`plugins/gateway/app/src/__tests__/retry-executor.test.ts`**

- **executeWithRetry — success paths**
  - returns result on first attempt
  - succeeds after one failure when maxAttempts=2
  - returns value from later attempt
- **executeWithRetry — exhausted retries**
  - throws last error after maxAttempts exhausted
  - emits execution:retry event between attempts
  - does not retry non-retryable errors by default (onlyRetryable=true)
  - retries non-retryable when onlyRetryable=false
- **executeWithRetry — cancellation**
  - throws CancelledError immediately if signal is already aborted
  - throws CancelledError when signal aborts during dispatch
  - aborts during retry backoff delay
  - does not retry after CancelledError
- **executeWithRetry — retryable error classification**
  - retries on: "${msg}"
  - does not retry on: "${msg}"
- **executeWithRetry — maxAttempts edge cases**
  - treats maxAttempts=0 as 1 attempt
  - default config (undefined) is single attempt

**`plugins/gateway/app/src/__tests__/server-security.test.ts`**

- **H11 — access_token query param redacted from logs**
  - does not log a raw JWT token present in ?access_token= query string
  - logs [REDACTED] in place of the token value
  - logs other query params normally (only access_token is redacted)
- **H12 — CORS does not reflect arbitrary origin**
  - does not echo back a random attacker origin in CORS header

**`plugins/gateway/app/src/__tests__/subscription-registry.test.ts`**

- **SubscriptionRegistry — subscribe / unsubscribe**
  - subscribe adds to both indexes
  - multiple connections can subscribe to same execution
  - one connection can subscribe to multiple executions
  - subscribe is idempotent
  - unsubscribe removes from both indexes
  - unsubscribe GCs empty sets
  - unsubscribe only removes one connection, other subscribers remain
  - unsubscribe on non-existent connection/execution does not throw
- **SubscriptionRegistry — registerSocket / removeSocket**
  - registerSocket stores socket for broadcast
  - removeSocket prevents broadcast after removal
- **SubscriptionRegistry — broadcast**
  - broadcasts to all open subscribers
  - does not broadcast to closed sockets
  - does not broadcast to connections without a registered socket
  - broadcast sends JSON-serialised event
  - broadcast to execution with no subscribers is a no-op
  - broadcast only reaches subscribers of the target execution
- **SubscriptionRegistry — removeConnection**
  - returns orphaned executionIds (those with zero subscribers after removal)
  - does not return executionId still subscribed by another connection
  - returns empty array if connection had no subscriptions
  - cleans up all indexes for removed connection
  - removes socket during removeConnection
  - returns orphaned list with multiple executions
- **SubscriptionRegistry — metrics**
  - connectionCount tracks unique connections with subscriptions
  - subscriptionCount is the sum of all (connection, execution) pairs
  - counts decrease after unsubscribe
  - counts reset to 0 after all connections removed

**`plugins/gateway/app/src/__tests__/telemetry-ingest.test.ts`**

- **POST /telemetry/v1/ingest**
  - returns 401 without auth token
  - returns 400 with empty body
  - returns 400 with empty events array
  - returns 400 when event missing required source field
  - returns 503 when analytics adapter not configured
  - ingests single event and calls analytics.track()
  - ingests batch of multiple events
  - uses current timestamp when event.timestamp is omitted
  - passes tags as flat properties
  - returns 422 when all events fail
  - handles partial failure (some events succeed, some fail)

**`plugins/gateway/app/src/__tests__/tokens.test.ts`**

- **extractBearerToken**
  - extracts token from Bearer header
  - is case-insensitive
  - returns null for missing header
  - returns null for non-Bearer scheme
  - returns null for empty string
  - handles token with special characters
- **resolveToken**
  - resolves machine token from cache
  - returns null for unknown token (no CLI fallback)
  - machine token resolves correctly, unknown token returns null
  - checks correct cache key for machine token

### `@kb-labs/gateway-auth`

#### Unit tests

**`plugins/gateway/auth/src/__tests__/jwt.test.ts`**

- **signAccessToken / verifyAccessToken**
  - issues a verifiable access token
  - returns null for tampered token
  - returns null for wrong secret
- **signRefreshToken / verifyRefreshToken**
  - issues a verifiable refresh token
  - returns null for access token passed as refresh
  - returns null for wrong secret

**`plugins/gateway/auth/src/__tests__/service.test.ts`**

- **AuthService**
- **register**
  - returns clientId, clientSecret, hostId, and server-generated namespaceId
  - generates a unique namespaceId per registration (no impersonation)
  - saves public key when provided
- **issueTokens**
  - returns token pair on valid credentials
  - returns null on wrong secret
  - returns null for unknown clientId
- **verify**
  - returns AuthContext for valid access token
  - returns null for garbage token
- **refreshTokens**
  - issues new token pair on valid refresh token
  - rejects the same refresh token twice (rotation)
  - returns null for garbage refresh token

**`plugins/gateway/auth/src/__tests__/store.test.ts`**

- **id generators**
  - generates unique clientIds
  - generates unique secrets
  - generates unique hostIds
- **client record**
  - buildClientRecord hashes the secret
  - saveClient / getClient round-trips
  - verifyClientSecret returns record on correct secret
  - verifyClientSecret returns null on wrong secret
  - verifyClientSecret returns null for unknown clientId
- **refresh tokens**
  - saveRefreshToken / consumeRefreshToken round-trips
  - consumeRefreshToken deletes the token (rotation)
  - consumeRefreshToken returns null for unknown token
- **public keys**
  - savePublicKey / getPublicKey round-trips
  - getPublicKey returns null if not set

### `@kb-labs/gateway-contracts`

#### Unit tests

**`plugins/gateway/contracts/src/__tests__/schemas.test.ts`**

- **GatewayConfigSchema**
  - parses valid config with defaults
  - parses config with upstreams
  - rejects upstream with invalid URL
  - rejects upstream prefix without leading slash
- **HostRegistrationSchema**
  - parses valid registration
  - accepts all valid capability values
  - rejects invalid capability value
  - requires name
  - requires namespaceId
  - allows empty capabilities array
- **HostDescriptorSchema**
  - parses valid descriptor
  - accepts all status values
  - rejects invalid status
- **AuthContextSchema**
  - parses user auth context
  - rejects invalid tier
  - accepts all type values
- **HelloMessageSchema**
  - parses minimal hello
  - parses hello with reconnect hostId
  - rejects wrong type literal
- **CallMessageSchema**
  - parses valid call
  - parses bulk call
  - requires trace field
- **ErrorMessageSchema**
  - parses valid error
- **SUPPORTED_PROTOCOL_VERSIONS**
  - contains 1.0

### `@kb-labs/gateway-core`

#### Unit tests

**`plugins/gateway/core/src/__tests__/adaptive-buffer.test.ts`**

- **AdaptiveBuffer.ttlFor**
  - returns minTTL at full load (1.0)
  - returns minTTL exactly at threshold
  - returns maxTTL at zero load
  - interpolates linearly between min and max
  - clamps to minTTL above threshold
  - respects custom policy
- **AdaptiveBuffer.enqueue**
  - stores a call in cache
  - appends to existing buffer
  - throws HOST_BUFFER_FULL when maxSize exceeded
  - uses adaptive TTL when calling cache.set
- **AdaptiveBuffer.flush**
  - returns buffered calls and clears buffer
  - returns empty array when buffer is empty
  - buffer is empty after flush

**`plugins/gateway/core/src/__tests__/dispatcher.test.ts`**

- **HostCallDispatcher**
- **registerConnection / connectedCount / firstHost**
  - counts zero initially
  - increments count after register
  - returns first hostId
  - returns undefined firstHost when namespace empty
  - namespaces are isolated
- **removeConnection**
  - decrements count after remove
  - removeConnection on unknown host is a no-op
- **call**
  - sends a call message over the socket
  - rejects immediately if host is not connected
  - resolves with single chunk data on result
  - resolves with all chunks as array when multiple chunks arrive
  - resolves with undefined when result arrives with no chunks
  - rejects on error message with error.message
  - rejects on error message with plain string error
- **handleInbound**
  - ignores messages with no requestId
  - ignores messages for unknown requestId
  - cleans up pending map after resolve

**`plugins/gateway/core/src/__tests__/gateway-dispatch-transport.test.ts`**

- **GatewayDispatchTransport**
  - POSTs to dispatchEndpoint with correct headers and body
  - sends correct body with adapter:execution, method:execute, args:[request]
  - throws on HTTP 503
  - throws on HTTP 403
  - throws when fetch rejects (network error)
  - returns data from response.result
  - returns data: undefined when result field is absent
- **retryOn503**
  - retries on 503 and succeeds on next attempt
  - throws after exhausting all retry attempts
  - does not retry on 503 when retryOn503 is not set
  - retries correct number of times before success
  - does not retry on other 4xx/5xx errors

**`plugins/gateway/core/src/__tests__/gateway-host-resolver.test.ts`**

- **GatewayHostResolver**
  - resolves host from Gateway response
  - returns null on 404 (no host found)
  - returns null on 5xx (Gateway error)
  - returns null on network error
  - sends target and namespace in request body
  - defaults namespace to "default"
  - strips trailing slash from gatewayUrl
  - throws on empty internalSecret
  - returns null and logs on invalid response body

**`plugins/gateway/core/src/__tests__/sqlite-host-store.test.ts`**

- **SqliteHostStore**
- **save + get**
  - persists and retrieves a host descriptor
  - returns null for non-existent host
  - upserts on save with same hostId+namespace
- **list**
  - lists hosts in a namespace
  - listAll returns all namespaces
- **delete**
  - removes a host and returns true
  - returns false for non-existent host
- **tokens**
  - saves and resolves a machine token
  - returns null for unknown token
  - deleteToken removes the mapping
  - delete host also removes its tokens

### `@kb-labs/gateway-runtime-server`

#### Unit tests

**`plugins/gateway/runtime-server/src/__tests__/runtime-server.test.ts`**

- **RuntimeServer**
  - registers execution handler on construction
  - calls GatewayClient.connect() on start()
  - calls GatewayClient.stop() on stop()
- **handleExecution**
  - calls runInProcess with correct params and returns data
  - throws when method is not execute
  - throws when handlerRef is missing
  - throws when pluginRoot is missing
  - propagates runInProcess errors
  - handles relative handlerRef (resolved against pluginRoot)
- **getHostId**
  - returns null before connected
  - returns hostId after onConnected callback

### `@kb-labs/host-agent-app`

#### Unit tests

**`plugins/host-agent/app/host-agent-app/src/__tests__/execution-handler-validation.test.ts`**

- **ExecutionHandler — descriptor defaults**
  - defaults permissions when descriptor omits them

**`plugins/host-agent/app/host-agent-app/src/__tests__/local-plugin-resolver.test.ts`**

- **LocalPluginResolver**
  - resolves plugin by id from manifest
  - lists discovered plugins
  - throws when plugin not found
  - rejects path traversal in handlerRef
  - rejects handlerRef with .. segments
  - throws when handler file does not exist
  - scans packages/ subdirectory (monorepo layout)
  - rescan clears cache and rediscovers

**`plugins/host-agent/app/src/__tests__/execution-handler-validation.test.ts`**

- **ExecutionHandler — descriptor defaults**
  - defaults permissions when descriptor omits them

**`plugins/host-agent/app/src/__tests__/local-plugin-resolver.test.ts`**

- **LocalPluginResolver**
  - resolves plugin by id from manifest
  - lists discovered plugins
  - throws when plugin not found
  - rejects path traversal in handlerRef
  - rejects handlerRef with .. segments
  - throws when handler file does not exist
  - scans packages/ subdirectory (monorepo layout)
  - rescan clears cache and rediscovers

### `@kb-labs/host-agent-client`

#### CLI handler tests

**`plugins/host-agent/client/src/__tests__/client.test.ts`**

- **HostAgentClient**
  - status() returns daemon status
  - execute() streams events and resolves on done
  - execute() throws on error response
  - status() rejects on timeout

### `@kb-labs/host-agent-core`

#### Unit tests

**`plugins/host-agent/core/src/__tests__/gateway-client-adapter.test.ts`**

- **GatewayClient.sendAdapterCall**
  - sends adapter:call message and resolves on adapter:response
  - resolves with error on adapter:error
  - rejects on timeout
  - rejects when WS is not connected
  - rejects all pending on disconnect
  - matches responses by requestId (concurrent calls)

**`plugins/host-agent/core/src/__tests__/gateway-transport.test.ts`**

- **GatewayTransport**
  - sends AdapterCall through GatewayClient and returns response
  - maps error response correctly
  - throws when closed
  - propagates workspaceId from default context
  - propagates timeout from AdapterCall

**`plugins/host-agent/core/src/__tests__/token-manager.test.ts`**

- **TokenManager**
  - start() returns accessToken from fetchTokens
  - schedules refresh before expiry and calls onRefreshed
  - retries with exponential backoff after refresh failure
  - calls onRefreshFailed after maxRetries exceeded
  - stop() prevents further refresh
  - throws if accessToken accessed before start

### `@kb-labs/host-agent-entry`

#### CLI handler tests

**`plugins/host-agent/entry/src/__tests__/cli/list.cli.test.ts`**

- **workspace:list**
  - CL-01: returns exitCode 0 and prints success when hosts are returned
  - CL-02: --json flag outputs hosts array and returns exitCode 0
  - CL-03: empty host list prints info message and returns exitCode 0
  - CL-04: gateway returns HTTP 401 — exitCode 1, error reported
  - CL-05: fetch throws (gateway unreachable) — exitCode 1, error reported
  - CL-06: multiple hosts — all returned in result

**`plugins/host-agent/entry/src/__tests__/cli/register.cli.test.ts`**

- **workspace:register**
  - CR-01: successful registration returns exitCode 0 and configPath
  - CR-02: --json flag outputs configPath, hostId, clientId and returns exitCode 0
  - CR-03: missing --gateway returns exitCode 1 with validation error
  - CR-04: invalid gateway URL (no http/https prefix) returns exitCode 1
  - CR-05: workspace path does not exist — exitCode 1, error reported
  - CR-06: gateway returns HTTP 500 — exitCode 1, error reported
  - CR-07: fetch throws (unreachable gateway) — exitCode 1, error reported
  - CR-08: gateway response missing required fields — exitCode 1

**`plugins/host-agent/entry/src/__tests__/cli/status.cli.test.ts`**

- **workspace:status**
  - CS-01: daemon not running — exitCode 0, running: false
  - CS-02: daemon running and connected — exitCode 0, running: true, connected: true
  - CS-03: daemon running but disconnected — exitCode 0, running: true, connected: false
  - CS-04: --json flag with daemon not running outputs { running: false }
  - CS-05: --json flag with connected daemon outputs full status JSON

### `@kb-labs/host-agent-fs`

#### Unit tests

**`plugins/host-agent/fs/src/__tests__/filesystem-handler.test.ts`**

- **FilesystemHandler**
- **readFile**
  - reads existing file
  - throws on file outside allowedPaths
- **writeFile**
  - writes file
  - rejects write outside allowedPaths
- **listDir**
  - returns file names in directory
- **stat**
  - returns stat for existing file
  - returns stat for directory
- **exists**
  - returns true for existing file
  - returns false for missing file
- **unknown method**
  - throws for unsupported method
- **fetchWorkspace**
  - returns all text files with relative paths
  - excludes node_modules directory
  - excludes .git directory
  - excludes dist directory
  - returns empty array for empty directory
  - rejects workspace path outside allowedPaths
  - recurses into nested subdirectories
- **security**
  - rejects path with prefix bypass (no sep)
  - rejects empty string arg
  - rejects non-string arg
  - rejects path traversal sequence

**`plugins/host-agent/fs/src/__tests__/search-handler.test.ts`**

- **SearchHandler**
  - grep finds matches in files
  - grep returns empty for no matches
  - glob finds files by pattern
  - rejects path outside allowedPaths
  - throws on unknown method

**`plugins/host-agent/fs/src/__tests__/shell-handler.test.ts`**

- **ShellHandler**
  - executes simple command
  - returns exit code on failure
  - blocks dangerous commands
  - blocks fork bombs
  - rejects cwd outside allowedPaths
  - throws on unknown method
  - captures stderr on error

### `@kb-labs/host-agent-transport`

#### Unit tests

**`plugins/host-agent/transport/src/__tests__/transport.test.ts`**

- **TcpTransport**
  - sends and receives a message round-trip
  - handles multiple messages in sequence
- **UnixSocketTransport**
  - sends and receives a message round-trip
- **createTransport**
  - creates TcpTransport for mode=tcp
  - creates platform-appropriate transport for mode=auto
  - throws for unknown mode

### `@kb-labs/impact-core`

#### Unit tests

**`plugins/impact/core/tests/core/build-analyzer.test.ts`**

- **analyzeBuildImpact**
  - returns topologically sorted build order
  - handles independent packages (no deps between them)
  - returns empty plan for no packages

**`plugins/impact/core/tests/core/dependency-graph.test.ts`**

- **buildReverseDependencyGraph**
  - builds forward and reverse edges

**`plugins/impact/core/tests/core/doc-analyzer.test.ts`**

- **analyzeDocImpact**
  - matches exact package names
  - matches suffix glob (*-cli)
  - matches prefix glob (@kb-labs/mind-*)
  - deduplicates matching docs
  - returns empty for no matching rules

**`plugins/impact/core/tests/core/formatter.test.ts`**

- **formatHumanReadable**
  - formats full result with all sections
  - formats empty result

**`plugins/impact/core/tests/core/package-analyzer.test.ts`**

- **analyzePackageImpact**
  - classifies direct, dependent, and transitive packages
  - deduplicates across levels
  - handles empty input
- **generateRecommendations**
  - generates rebuild and test recommendations

**`plugins/impact/core/tests/core/test-analyzer.test.ts`**

- **analyzeTestImpact**
  - detects packages with tests → mustRun
  - detects packages without tests → noTests

### `@kb-labs/infra-worker-core`

#### E2E / journey tests

**`plugins/infra-worker/plugin-template-core/tests/cli/infra-worker-commands.spec.ts`**

- **infra-worker commands**
  - prepare should materialize workspace and return IDs
  - prepare should provision env, attach workspace and capture snapshot when requested
  - capture-snapshot should run through sdk/testing harness
  - restore-snapshot should fail when snapshotId is missing

### `@kb-labs/llm-router`

#### Unit tests

**`core/llm-router/test/resolver.test.ts`**

- **TierResolver**
  - should return configured tier for each tier level
- **TierResolver > exact match**
  - should return configured tier when request matches
- **no request (default)**
  - should return configured tier when no tier requested
  - should work with all configured tiers
- **escalation (request < configured)**
  - should escalate small → medium silently
  - should escalate small → large silently
  - should escalate medium → large silently
- **degradation (request > configured)**
  - should degrade large → medium with warning
  - should degrade large → small with warning
  - should degrade medium → small with warning
- **getConfiguredTier**
  - should return the configured tier
- **CapabilityResolver**
  - should return all capabilities from getCapabilities
- **CapabilityResolver > simple config (no capabilities set)**
  - should report all capabilities as available
  - should return true for hasAllCapabilities with any set
- **advanced config (specific capabilities)**
  - should only report configured capabilities as available
  - should return only configured capabilities
  - should correctly check multiple capabilities
  - should work with single capability
- **empty array vs undefined**
  - should treat empty set as "all available"
  - should treat undefined as "all available"

**`core/llm-router/test/router.test.ts`**

- **LLMRouter**
- **basic initialization**
  - should initialize with simple config (no tierMapping)
  - should use legacy tier field if defaultTier not provided
- **tierMapping initialization**
  - should select model from defaultTier on initialization
  - should select highest priority model (lowest priority number)
  - should extract capabilities from tierMapping
- **resolve() with tierMapping**
  - should resolve to correct model for each tier
  - should resolve to default tier when no tier specified
  - should filter by capabilities when specified
  - should fall back to first model if no model matches capabilities
- **ILLM delegation with model injection**
  - should pass selected model to complete()
  - should pass selected model after resolve()
  - should preserve other options when injecting model
  - should not override explicitly provided model
  - should pass model to stream()
- **edge cases**
  - should handle empty tierMapping gracefully
  - should handle missing tier in tierMapping
  - should handle empty array for tier
  - should work without logger
- **chatWithTools support**
  - should report supportsChatWithTools correctly
  - should delegate chatWithTools when available
- **multi-adapter support**
  - should initialize with adapter from default tier
  - should switch adapter when resolving to different tier
  - should load adapter on first complete() call
  - should cache loaded adapters
  - should use correct adapter for each tier
  - should include provider and resource in resolution result
  - should extract provider from new provider field when specified
  - should fall back to default adapter when adapter loader fails
  - should use default adapter when no adapter specified in tierMapping
  - should work without adapterLoader (backward compatible)
  - should handle stream() with multi-adapter
  - should handle chatWithTools() with multi-adapter

### `@kb-labs/marketplace-api`

#### E2E / journey tests

**`plugins/marketplace/api/src/routes/health.spec.ts`**

- **marketplace observability surfaces**
  - returns health and versioned observability documents
  - renders canonical metrics as prometheus text
  - tracks runtime marketplace operations in observability health and metrics

### `@kb-labs/marketplace-core`

#### E2E / journey tests

**`plugins/marketplace/core/src/__tests__/marketplace-discovery-integration.spec.ts`**

- **Marketplace → Discovery Integration**
  - install → discover: plugin is available after marketplace install
  - install → uninstall → discover: plugin is gone after uninstall
  - install → disable → discover: disabled plugin not found
  - link → discover: local linked plugin is available

**`plugins/marketplace/core/src/__tests__/marketplace-service.spec.ts`**

- **MarketplaceService**
- **install**
  - installs a plugin and writes to marketplace.lock
  - installs multiple plugins in one call
  - catches afterInstall hook errors as warnings
- **uninstall**
  - removes plugin from lock and cache
  - uninstalls multiple plugins
- **link**
  - links a local plugin directory
  - rejects path outside workspace root (path traversal)
- **unlink**
  - removes linked plugin from lock
- **enable / disable**
  - enables a disabled plugin
  - throws when enabling a non-existent package
  - throws when disabling a non-existent package
- **list / getEntry**
  - lists installed plugins
  - filters by kind
  - returns empty list when no lock file
  - getEntry returns entry or null
- **doctor**
  - returns ok: true when all packages are healthy
  - reports error when package directory is missing
  - warns about integrity mismatch
  - reports info about unsigned packages
  - returns ok: true with empty lock
- **sync**
  - discovers plugins from glob patterns
  - skips packages already in lock
  - marks new entries as disabled by default
  - auto-enables entries when autoEnable is true
- **mergeScopedEntries — project wins**
  - returns platform-only entries unchanged
  - returns project-only entries unchanged
  - merges non-overlapping entries from both scopes
  - project entry wins on collision and emits diagnostic
  - project wins regardless of input order

**`plugins/marketplace/core/src/manifest-cache.spec.ts`**

- **manifest-cache**
- **readManifestCache**
  - returns null when cache does not exist
  - returns null for invalid JSON
  - returns null for wrong schema
  - reads valid cache
- **writeManifestCache**
  - creates .kb dir and writes atomically
- **setCacheEntry**
  - adds entry and persists
  - overwrites existing entry
- **removeCacheEntry**
  - removes entry and persists
  - no-op if entry does not exist

**`plugins/marketplace/core/src/strategies/plugin-strategy.spec.ts`**

- **PluginStrategy**
  - kind is plugin
- **detectKind**
  - returns null for empty directory
  - returns plugin when ManifestV3 exists via kb.plugin.json
- **extractProvides**
  - returns [plugin] as minimum

### `@kb-labs/marketplace-entry`

#### CLI handler tests

**`plugins/marketplace/entry/src/__tests__/cli/install.cli.test.ts`**

- **marketplace:install**
  - CI-01: installs package successfully, prints success message
  - CI-02: --json flag outputs JSON result
  - CI-03: HTTP error — returns exitCode 1 with error captured
  - CI-04: missing argv — returns exitCode 1, validation error shown
  - CI-05: scope resolution failure — returns exitCode 1
  - CI-06: installs with --dev flag — passes dev:true to HTTP
  - CI-07: warnings present — success still returned, result includes warnings

**`plugins/marketplace/entry/src/__tests__/cli/sync.cli.test.ts`**

- **marketplace:sync**
  - CS-01: syncs new entries, prints success message
  - CS-02: nothing new added — prints info "up to date"
  - CS-03: --json flag outputs JSON result
  - CS-04: HTTP error — returns exitCode 1 with error captured
  - CS-05: no sync.include configured — returns exitCode 1 with validation error
  - CS-06: scope resolution failure — returns exitCode 1

**`plugins/marketplace/entry/src/__tests__/cli/uninstall.cli.test.ts`**

- **marketplace:uninstall**
  - CU-01: uninstalls package successfully, prints success message
  - CU-02: --json flag outputs JSON result
  - CU-03: HTTP error — returns exitCode 1 with error captured
  - CU-04: missing argv — returns exitCode 1 without calling HTTP
  - CU-05: scope resolution failure — returns exitCode 1
  - CU-06: result.result contains removed packages and scope

**`plugins/marketplace/entry/src/__tests__/cli/update.cli.test.ts`**

- **marketplace:update**
  - CUP-01: updates packages, prints success message
  - CUP-02: nothing to update — prints info message
  - CUP-03: --json flag outputs JSON result
  - CUP-04: HTTP error — returns exitCode 1 with error captured
  - CUP-05: scope resolution failure — returns exitCode 1
  - CUP-06: update all — no argv, sends request without packageIds
  - CUP-07: specific packages — sends packageIds in body
  - CUP-08: warnings present — result still exitCode 0

#### Unit tests

**`plugins/marketplace/entry/src/commands/__tests__/sync.test.ts`**

- **marketplace sync — autoEnable behaviour**
  - sends autoEnable: true when NODE_ENV=development
  - sends autoEnable: true when NODE_ENV is unset (dev default)
  - sends autoEnable: false when NODE_ENV=production
  - explicit --auto-enable flag overrides NODE_ENV=production
  - explicit --no-auto-enable flag overrides NODE_ENV=development
  - passes include patterns from kb.config.json
  - returns exitCode 1 when no include patterns configured

### `@kb-labs/marketplace-npm`

#### E2E / journey tests

**`plugins/marketplace/npm/src/npm-source.spec.ts`**

- **NpmPackageSource**
  - resolves scoped package without version
- **NpmPackageSource > resolve**
  - resolves scoped package with version
  - resolves unscoped package with version
  - resolves unscoped package without version
  - throws for invalid spec

### `@kb-labs/mind-adapters`

#### E2E / journey tests

**`plugins/mind/adapters/src/__tests__/git.spec.ts`**

- **Git Adapters**
  - should handle git diff since revision
  - should handle staged files listing
  - should handle non-git directories gracefully
  - should handle nonexistent directories

### `@kb-labs/mind-contracts`

#### Unit tests

**`plugins/mind/contracts/tests/contracts.manifest.test.ts`**

- **pluginContractsManifest**
  - matches the published schema
  - declares the expected schema identifier
  - uses a valid SemVer contracts version

### `@kb-labs/mind-core`

#### E2E / journey tests

**`plugins/mind/core/src/__tests__/error.spec.ts`**

- **MindError**
  - should create error with code and message
  - should create error with hint and meta
  - should map error codes to exit codes
  - should have error hints for all codes

**`plugins/mind/core/src/__tests__/hash.spec.ts`**

- **Hash Utilities**
  - should compute SHA256 for string
  - should produce consistent hashes
  - should produce different hashes for different content
  - should compute SHA256 for Buffer
  - should produce same hash for string and equivalent buffer

**`plugins/mind/core/src/__tests__/paths.spec.ts`**

- **Path Utilities**
  - should convert to POSIX format
  - should convert from POSIX format
  - should ignore common patterns
  - should allow .kb/mind/ but ignore other .kb/ paths
  - should not ignore regular source files
  - should ignore log and temp files

**`plugins/mind/core/src/__tests__/token.spec.ts`**

- **Token Estimation**
  - should estimate tokens for simple text
  - should handle empty text
  - should apply code bonus for code-like content
  - should truncate text correctly
  - should handle truncation modes

**`plugins/mind/core/src/__tests__/utils.spec.ts`**

- **Token Utilities**
  - should estimate tokens correctly
  - should handle empty strings
  - should handle long text
- **Token Truncation**
  - should truncate to specified tokens
  - should handle truncation modes
  - should return original text if within limit
- **Hash Utilities**
  - should generate consistent hashes
  - should generate different hashes for different inputs
  - should handle empty strings
- **Default Budget**
  - should have valid budget structure
  - should have reasonable token caps
  - should have valid truncation mode

#### Unit tests

**`plugins/mind/core/src/utils/math.test.ts`**

- **cosineSimilarity**
  - should return 1 for identical vectors
  - should return 0 for orthogonal vectors
  - should return 0 for vectors with different lengths
  - should return 0 for zero magnitude vectors
  - should calculate correct similarity for known vectors
  - should handle negative values
  - should handle sparse vectors with undefined values
- **dotProduct**
  - should calculate dot product correctly
  - should return 0 for orthogonal vectors
  - should return 0 for different length vectors
  - should handle negative values
- **magnitude**
  - should calculate magnitude correctly
  - should return 0 for zero vector
  - should handle negative values
  - should handle single dimension
- **normalize**
  - should normalize vector to unit length
  - should return original vector if magnitude is 0
  - should handle already normalized vector
  - should handle negative values

### `@kb-labs/mind-engine`

#### E2E / journey tests

**`plugins/mind/engine/src/chunking/__tests__/chunker-registry.spec.ts`**

- **ChunkerRegistry**
  - should register chunker
  - should find chunker by extension
  - should find chunker by language
  - should return null if no chunker found
  - should handle multiple chunkers with same extension
  - should get all registered chunkers

**`plugins/mind/engine/src/chunking/__tests__/line-based.spec.ts`**

- **LineBasedChunker**
  - should have correct id and extensions
  - should chunk simple text into lines
  - should respect maxLines option
  - should respect minLines option
  - should handle empty file
  - should handle whitespace-only file
  - should create overlapping chunks
  - should set correct span ranges

**`plugins/mind/engine/src/chunking/__tests__/markdown.spec.ts`**

- **MarkdownChunker**
  - should have correct id and extensions
  - should chunk markdown by headings
  - should extract code blocks
  - should handle empty markdown
  - should handle markdown without headings
  - should preserve heading hierarchy
  - should respect maxLines option

**`plugins/mind/engine/src/optimization/__tests__/context-optimizer.spec.ts`**

- **ContextOptimizer**
- **ContextOptimizer > optimize**
  - should deduplicate similar chunks
  - should diversify across files
  - should respect maxChunks limit
  - should handle empty matches
  - should apply adaptive selection when enabled

**`plugins/mind/engine/src/reasoning/__tests__/complexity-detector.spec.ts`**

- **ComplexityDetector**
  - should detect long queries as high complexity
- **ComplexityDetector > heuristic detection**
  - should detect simple queries as low complexity
  - should detect multi-concept queries as complex
- **LLM-based detection**
  - should use LLM when enabled
  - should fallback to heuristics if LLM fails

**`plugins/mind/engine/src/reasoning/__tests__/parallel-executor.spec.ts`**

- **ParallelExecutor**
  - should execute queries sequentially when parallel disabled
  - should execute queries in parallel when enabled
  - should respect maxConcurrency limit
  - should stop early when conditions met
  - should handle errors gracefully

**`plugins/mind/engine/src/reasoning/__tests__/query-planner.spec.ts`**

- **QueryPlanner**
  - should generate multiple sub-queries when LLM available
- **QueryPlanner > planning**
  - should generate single query plan when LLM not available
  - should handle LLM errors gracefully
  - should limit sub-queries to maxSubqueries

**`plugins/mind/engine/src/reasoning/__tests__/reasoning-engine.spec.ts`**

- **ReasoningEngine**
  - should execute simple queries without reasoning
  - should execute complex queries with reasoning
  - should respect maxDepth limit
  - should detect cycles

**`plugins/mind/engine/src/reasoning/__tests__/synthesizer.spec.ts`**

- **ResultSynthesizer**
- **deduplication**
  - should deduplicate chunks by id
  - should deduplicate chunks by text similarity
- **LLM synthesis**
  - should use LLM when enabled
  - should fallback to concatenation if LLM fails
- **chunk sorting**
  - should sort chunks by score descending

**`plugins/mind/engine/src/reranking/__tests__/reranker.spec.ts`**

- **HeuristicReranker**
  - should rerank results based on keyword matching
  - should respect topK limit
  - should handle empty matches
  - should boost chunks with metadata
- **CrossEncoderReranker**
  - should rerank using external LLM
  - should handle LLM errors gracefully
  - should fallback on errors
  - should batch requests when needed

**`plugins/mind/engine/src/search/__tests__/adaptive-hybrid.spec.ts`**

- **boostExactIdentifiers**
  - prioritizes code chunks with exact identifier match over docs
  - keeps ordering stable when no identifier evidence exists
  - matches identifiers against hyphenated file paths

**`plugins/mind/engine/src/search/__tests__/conflicts.spec.ts`**

- **conflict resolution**
  - keeps fresh winner and penalizes older loser
  - uses trust as deterministic tie-breaker

**`plugins/mind/engine/src/search/__tests__/freshness.spec.ts`**

- **freshness ranking**
  - prioritizes fresher docs over stale docs
  - reports soft stale based on indexedAt threshold
  - falls back to auto for unknown mode

**`plugins/mind/engine/src/search/__tests__/hybrid.spec.ts`**

- **hybridSearch**
  - should combine vector and keyword results
  - should respect limit
  - should handle empty results

**`plugins/mind/engine/src/search/__tests__/keyword.spec.ts`**

- **keywordSearch**
  - should find chunks containing query terms
  - should rank results by relevance
  - should respect limit
  - should handle empty query
  - should handle empty chunks
  - should filter by pathMatcher if provided

**`plugins/mind/engine/src/search/__tests__/query-classifier.spec.ts`**

- **query-classifier retrieval profile**
  - uses exact_lookup for identifier-centric lookup query
  - uses semantic_explore for conceptual query
  - uses exact_lookup for CLI command lookup query
- **extractIdentifiers**
  - does not treat sentence words as technical identifiers
  - extracts kebab-case command identifiers
- **classifyQueryWithLLMFallback**
  - uses LLM tool-call decision in uncertainty band

**`plugins/mind/engine/src/search/__tests__/reliability.spec.ts`**

- **reliability**
  - fails closed on hard-stale in strict mode
  - marks below confidence floor for agent mode
  - builds confidence adjustments breakdown

**`plugins/mind/engine/src/vector-store/__tests__/local.spec.ts`**

- **LocalVectorStore**
  - should store and retrieve chunks
  - should search chunks
  - should replace scope (delete and recreate)
  - should filter by pathMatcher
  - should handle empty scope
  - should get all chunks
  - should provide scoped adapter batch operations
  - should return file metadata for filtering stage

**`plugins/mind/engine/src/vector-store/__tests__/retry-search.spec.ts`**

- **LocalVectorStore – search retry**
  - returns results immediately when search succeeds on the first try
  - retries on a transient error and succeeds on the second attempt
  - retries up to maxAttempts then throws the last transient error
  - does NOT retry on a permanent error and propagates it immediately
  - respects custom maxAttempts
- **PlatformVectorStoreAdapter – search retry**
  - returns mapped matches when the platform search succeeds on the first try
  - retries on a transient HTTP 503 error and succeeds on the third attempt
  - retries up to maxAttempts then throws on persistent transient errors
  - does NOT retry on a permanent error
  - applies sourceIds filter after successful retry

### `@kb-labs/mind-entry`

#### E2E / journey tests

**`plugins/mind/entry/src/__tests__/utils.spec.ts`**

- **CLI Utilities**
  - should apply green color
- **CLI Utilities > colors**
  - should apply red color
  - should apply yellow color
  - should apply blue color
  - should apply cyan color
  - should apply gray color
  - should apply bold formatting
  - should apply dim formatting
- **safeColors**
  - should be same as colors
- **safeSymbols**
  - should have all required symbols
- **TimingTracker**
  - should initialize with current time
  - should track elapsed time
  - should return elapsed time in milliseconds
- **formatTiming**
  - should format milliseconds correctly
  - should format seconds correctly
- **box**
  - formats content with a single left border
  - handles empty content
- **keyValue**
  - formats string values as key-value pairs
  - formats numeric values consistently
- **createSpinner**
  - should create spinner with text
  - should stop spinner with final text
  - should stop spinner without final text

### `@kb-labs/mind-indexer`

#### E2E / journey tests

**`plugins/mind/indexer/src/__tests__/e2e.spec.ts`**

- **E2E Mind Indexer**
  - should complete full indexing workflow
  - should handle time budget exceeded gracefully
  - should handle missing files gracefully

**`plugins/mind/indexer/src/__tests__/index.spec.ts`**

- **Mind Indexer**
  - should export main functions

**`plugins/mind/indexer/src/__tests__/orchestrator.spec.ts`**

- **Mind Indexer**
  - should initialize mind structure
  - should update indexes
  - should respect time budget
  - should handle empty workspace
  - removes deleted files from indexes
- **Mind Indexer with path aliases**
  - resolves tsconfig path aliases and external packages
  - falls back to manual alias resolution when baseUrl is missing

### `@kb-labs/mind-orchestrator`

#### E2E / journey tests

**`plugins/mind/orchestrator/src/__tests__/orchestrator-context-consistency.spec.ts`**

- **AgentQueryOrchestrator context consistency**
  - uses cache on valid context
  - fails closed and invalidates scope cache on indexRevision mismatch
  - fails closed on engineConfigHash mismatch
  - fails closed on sourcesDigest mismatch

**`plugins/mind/orchestrator/src/decomposer/__tests__/query-decomposer.spec.ts`**

- **QueryDecomposer**
  - classifies technical "what is <Identifier>" lookup as simple/instant
  - classifies short debug lookup as simple/instant to avoid noisy decomposition
  - preserves simple/instant heuristic even when autoDetectComplexity is enabled
  - always keeps original query in decomposition output
  - respects maxSubqueries while preserving original query

**`plugins/mind/orchestrator/src/gatherer/__tests__/chunk-gatherer-merge.spec.ts`**

- **normalizeSubqueryScores**
  - normalizes to [0, 1] when maxScore >= 0.5
  - skips normalization when maxScore < 0.5 (weak subquery)
- **applyMultiSubqueryBoost**
  - boosts file found by 2 subqueries by 1.10x
  - boosts file found by 3+ subqueries by 1.20x
  - does not boost file found by only 1 subquery
- **deduplicateSubqueries**
  - removes near-duplicate subqueries (Jaccard > 0.7)
  - keeps distinct subqueries
  - keeps partial overlaps below threshold

**`plugins/mind/orchestrator/src/gatherer/__tests__/chunk-gatherer-rerank.spec.ts`**

- **rerankGatheredChunks**
  - boosts code chunk with exact technical identifier in thinking mode
  - keeps non-technical query order untouched
  - demotes ADR when query mentions specific filename
  - boosts ADR for design-intent why-queries without specific file
  - demotes plan docs vs ADR for architecture queries
- **classifySubqueryWeights**
  - prefers keyword for lookup-like technical query
  - prefers vector for conceptual query
  - prefers keyword for CLI command query

### `@kb-labs/perm-presets`

#### Unit tests

**`shared/perm-presets/src/__tests__/combine.test.ts`**

- **combine()**
  - should return empty spec when nothing added
  - should add a single preset
  - should merge two presets
  - should deduplicate env vars
  - should merge fs.allow arrays and convert to runtime format
  - should merge network domains
- **withEnv()**
  - should add custom env vars
- **withFs()**
  - should add fs permissions and convert to runtime format
  - should merge with existing fs
- **withQuotas()**
  - should add quotas
  - should override quotas (last wins)
- **withPlatform()**
  - should add platform permissions with booleans
  - should add platform permissions with arrays
  - should add platform permissions with objects
  - should merge platform arrays (union)
  - should merge platform objects (spread)
  - should override booleans (last wins)
  - should merge with existing platform permissions from presets
  - should deduplicate array values in platform permissions
  - should handle undefined platform gracefully
  - should pass through platform to runtime format unchanged
- **with raw PermissionSpec**
  - should accept raw spec without id/description
- **combinePresets()**
  - should combine multiple presets at once
  - should work with single preset
  - should return empty spec with no presets
- **Real-world scenarios**
  - should create commit-plugin permissions (git + kb-platform)
  - should create release-plugin permissions (git + npm + kb-platform)
  - should create commit-plugin with platform permissions (git + kb + llm + cache)

**`shared/perm-presets/src/__tests__/presets.test.ts`**

- **Permission Presets**
  - should include basic system env vars
- **Permission Presets > minimal**
  - should have id and description
  - should NOT include HOME or USER
- **gitWorkflow**
  - should have id and description
  - should include HOME for git config
  - should include USER for git author
  - should include GIT_* wildcard
  - should include SSH env vars for auth
  - should allow .git directory access
- **npmPublish**
  - should have id and description
  - should include HOME for .npmrc
  - should include npm token vars
  - should include npm_* wildcard
  - should allow package.json and lock files
  - should allow npm registry domains
- **fullEnv**
  - should have id and description
  - should include wildcard for all env vars
- **kbPlatform**
  - should have id and description
  - should include KB_* wildcard
  - should allow .kb directory access
- **llmAccess**
  - should have id and description
  - should include OpenAI env vars
  - should include Anthropic env vars
  - should allow LLM API domains
- **vectorStore**
  - should have id and description
  - should include Qdrant env vars
  - should include Pinecone env vars
  - should allow vector store domains
- **ciEnvironment**
  - should have id and description
  - should include CI indicator
  - should include GitHub Actions vars
  - should include GitLab CI vars

### `@kb-labs/platform-client`

#### CLI handler tests

**`sdk/platform-client/src/__tests__/client.test.ts`**

- **KBPlatform**
  - call() sends correct request format
  - call() includes auth header
  - call() handles error responses
  - call() handles network errors
  - llm.complete() passes arguments correctly
  - cache.get() returns typed value
  - cache.set() sends key, value, ttl
  - vectorStore.search() passes query
  - telemetry.event() buffers without sending
  - telemetry.flush() sends buffered events
  - shutdown() flushes remaining events
  - generic call() works for any adapter

### `@kb-labs/plugin-contracts`

#### Unit tests

**`core/plugin-contracts/src/logger-metadata.test.ts`**

- **getLoggerMetadataFromHost**
- **getLoggerMetadataFromHost > REST host context**
  - should extract all REST fields
  - should handle missing optional fields
- **CLI host context**
  - should extract CLI fields
  - should handle empty argv
- **Workflow host context**
  - should extract all workflow fields
  - should handle missing optional workflow fields
- **Webhook host context**
  - should extract webhook fields
  - should handle missing optional webhook fields
- **Cron host context**
  - should extract all cron fields
  - should handle missing lastRunAt (first run)
- **Type safety**
  - should handle discriminated union correctly
- **Integration scenarios**
  - should provide context for REST API logging
  - should provide context for workflow step logging
  - should provide context for webhook event logging

### `@kb-labs/plugin-execution`

#### WebSocket integration tests

**`core/plugin-execution/src/ws/__tests__/connection-registry.test.ts`**

- **ConnectionRegistry**
- **register**
  - should register a new connection
  - should track multiple connections in same channel
  - should track connections across different channels
  - should store tenant ID if provided
- **unregister**
  - should remove a connection
  - should remove connection from channel tracking
  - should not affect other connections in same channel
  - should handle unregistering non-existent connection gracefully
- **broadcast**
  - should broadcast message to all connections in channel
  - should not broadcast to other channels
  - should exclude connection when excludeConnectionId provided
  - should only send to connections with OPEN readyState
  - should handle empty channel gracefully
- **sendTo**
  - should send message to specific connections
  - should only send to connections with OPEN readyState
  - should handle non-existent connections gracefully
  - should handle empty connection list
- **getStats**
  - should return stats for empty registry
  - should return correct connection count
  - should return per-channel stats

**`core/plugin-execution/src/ws/__tests__/sender.test.ts`**

- **createWSSender**
- **send**
  - should send message when socket is OPEN
  - should not send message when socket is not OPEN
  - should serialize message with all fields
- **broadcast**
  - should call connectionRegistry.broadcast with channel path
  - should exclude self by default
  - should not exclude self when excludeSelf = false
  - should exclude self when excludeSelf = true
- **sendTo**
  - should call connectionRegistry.sendTo with connection IDs
  - should handle empty connection list
  - should handle single connection
- **close**
  - should close socket with default code 1000
  - should close socket with custom code
  - should close socket with custom code and reason
  - should use code 1000 when undefined provided
- **getConnectionId**
  - should return the connection ID
  - should return correct ID for different connections
- **integration scenarios**
  - should handle sequence of operations
  - should handle rapid message sending

#### Unit tests

**`core/plugin-execution/src/execution-contracts.invariant.test.ts`**

- **plugin-execution execution contracts invariant**
  - does not define duplicated execution contract interfaces

### `@kb-labs/plugin-execution-factory`

#### Integration tests

**`core/plugin-execution-factory/src/__tests__/remote-dispatch.integration.test.ts`**

- **RemoteBackend + GatewayDispatchTransport integration**
  - full flow: RemoteBackend → transport → dispatcher → mock runtime → result
  - remaps handlerRef before sending to transport
  - returns ok:false on 403 (wrong secret)
  - returns ok:false when no host registered (503)

#### Unit tests

**`core/plugin-execution-factory/src/__tests__/errors.test.ts`**

- **isKnownErrorCode()**
  - returns true for all known codes
  - returns false for unknown strings
  - returns false for non-strings
- **ExecutionLayerError**
  - sets name, message, code
  - defaults code to UNKNOWN_ERROR
  - stores details
  - toJSON() returns ExecutionError shape
- **isExecutionLayerError()**
  - returns true for ExecutionLayerError instances
  - returns false for plain errors
- **TimeoutError**
  - sets name, code, timeoutMs
  - works without timeoutMs
- **AbortError**
  - has default message and ABORTED code
  - accepts custom message
- **HandlerContractError**
  - sets name and HANDLER_CONTRACT_ERROR code
- **HandlerNotFoundError**
  - includes path in message and details
- **WorkspaceError / PermissionDeniedError / ValidationError**
  - WorkspaceError has correct code
  - PermissionDeniedError has correct code
  - ValidationError has correct code
- **Pool errors**
  - QueueFullError formats message with sizes
  - AcquireTimeoutError formats message with ms
  - WorkerCrashedError formats message with exitCode and signal
  - WorkerCrashedError works without exitCode/signal
  - WorkerUnhealthyError includes reason

**`core/plugin-execution-factory/src/__tests__/execution-contracts.invariant.test.ts`**

- **plugin-execution-factory execution contracts invariant**
  - does not define duplicated execution contract interfaces

**`core/plugin-execution-factory/src/__tests__/isolated-backend.test.ts`**

- **createIsolatedExecutionBackend**
  - shutdown resolves without throwing
- **createIsolatedExecutionBackend > without strictIsolation**
  - returns a backend that has health/stats/shutdown
- **with strictIsolation**
  - routes to remote when request.target.environmentId is present
  - routes to local when no environmentId
  - routes to local when target has no environmentId
  - creates a fresh remote backend per-job (factory called each time)
  - passes workspaceRootOnHost to RemoteBackend
  - health/stats delegate to local backend
  - shutdown delegates to local backend

**`core/plugin-execution-factory/src/__tests__/remote-backend.test.ts`**

- **RemoteBackend**
  - returns ok:false when transport throws
- **RemoteBackend > execute**
  - delegates to transport and returns data
  - passes the (possibly remapped) request to transport
  - includes backend:remote in metadata on success
  - includes backend:remote in metadata on failure
- **handlerRef remapping**
  - remaps absolute host path to /workspace
  - does not remap when workspaceRootOnHost is not set
  - does not remap relative handlerRef
  - strips trailing slash from workspaceRootOnHost
- **stats**
  - accumulates after executions
  - counts errors separately
- **health / shutdown**
  - health returns healthy:true
  - shutdown resolves without throwing

**`core/plugin-execution-factory/src/__tests__/routing-backend.test.ts`**

- **createExecutionBackend with mode: remote (RoutingBackend)**
  - routes to RemoteBackend when request.target.environmentId is set
  - routes to local InProcessBackend when no environmentId
  - routes to local when target has no environmentId
  - throws when mode is remote but transport is missing
  - shutdown resolves without throwing

**`core/plugin-execution-factory/src/__tests__/routing-workspace-agent.test.ts`**

- **RoutingBackend — workspace-agent routing**
  - dispatches to workspace-agent when target.type matches and host resolves
  - falls back to local when host not found and fallback=local
  - returns NO_HOST_AVAILABLE error when fallback=error
  - skips workspace-agent routing when no hostResolver configured
  - prioritizes environmentId over workspace-agent routing

**`core/plugin-execution-factory/src/__tests__/subprocess-backend.test.ts`**

- **SubprocessBackend diagnostics**
  - logs structured handler_not_found event before returning error

**`core/plugin-execution-factory/src/__tests__/target-resolver.test.ts`**

- **resolveExecutionTarget()**
  - returns request unchanged when no target
  - throws when target has no namespace
  - resolves request with target.workdir — sets workspace.cwd
  - workdir does not override existing workspace.type
  - resolves target.environmentId when no environmentManager — skips check
  - throws when environmentManager reports terminated status
  - throws when environmentManager reports failed status
  - passes when environmentManager reports ready status
  - throws when workspaceManager reports released status
  - throws when workspaceManager reports failed status
  - does not mutate original request

### `@kb-labs/plugin-runtime`

#### Integration tests

**`core/plugin-runtime/src/__tests__/subprocess-integration.test.ts`**

- **Subprocess Integration Tests**
  - should execute handler in real subprocess and inject metadata
  - should handle subprocess timeout
  - should handle abort signal in subprocess

#### Unit tests

**`core/plugin-runtime/src/__tests__/bootstrap-dynamic-import.test.ts`**

- **Bootstrap Dynamic Import Tests**
  - should successfully load initPlatform via dynamic import when core-runtime exists
  - should handle missing core-runtime gracefully (no crash)
  - should parse KB_RAW_CONFIG_JSON correctly
  - should skip platform init when KB_RAW_CONFIG_JSON is not set
  - should skip platform init when platform config is missing

**`core/plugin-runtime/src/__tests__/context-factory-streaming.test.ts`**

- **createPluginContextV3 — streaming logger**
  - should NOT wrap logger when eventEmitter is absent
  - should wrap logger when eventEmitter is provided
  - should emit stderr for warn/error when eventEmitter present
  - should NOT emit events for debug/trace even with eventEmitter
  - should also emit via ctx.api.events.emit when eventEmitter present
- **createPluginContextV3 — streaming UI**
  - wraps UI with StreamingUI when eventEmitter is provided
  - does NOT wrap UI when eventEmitter is absent
  - emits warn and error on stderr

**`core/plugin-runtime/src/__tests__/context-factory.test.ts`**

- **createPluginContextV3**
  - should pass platform services directly without wrapping
  - should create context with all required fields
  - should wire runtime API correctly
  - should wire plugin API correctly
  - should create cleanup stack
  - should use provided signal when given
  - should preserve optional fields from descriptor
  - should preserve requestId from descriptor
  - should derive traceId from requestId when host context has no trace
  - should preserve traceId from host context when available
  - should allow reading all context fields

**`core/plugin-runtime/src/__tests__/context-structure.test.ts`**

- **Context Structure (Runtime Verification)**
  - should provide complete context structure to handlers
  - should provide UI facade with all 13 methods
  - should provide Runtime API with fs, fetch, env
  - should provide Plugin API with lifecycle, state, etc.
  - should provide Platform services
  - should provide trace context
  - should preserve optional fields (tenantId, outdir)
  - should pass signal if provided

**`core/plugin-runtime/src/__tests__/e2e-context.test.ts`**

- **E2E Context Tests**
  - should provide complete context in real execution
  - should provide working fs.exists in execution
  - should provide working trace API
  - should handle handler execution and exit code correctly
  - should provide working platform services (llm, logger, etc.)
  - should provide ui.colors and ui.write APIs

**`core/plugin-runtime/src/__tests__/environment-api.test.ts`**

- **EnvironmentAPI**
  - creates environment with full permission
  - checks status with read permission
  - destroys environment with destroy permission
  - renews lease with renewLease permission
  - denies create without platform.environment permission
  - denies specific operation when flag is missing
  - enforces create template scope when templates are set
  - allows create template by wildcard scope
  - enforces namespace scope on create
  - noop API reports unavailable manager

**`core/plugin-runtime/src/__tests__/error-handling.test.ts`**

- **Error Handling**
  - should store error details
- **Error Handling > PluginError base class**
  - should create error with message and code
  - should have stack trace
- **JSON serialization**
  - should serialize to JSON
  - should deserialize from JSON
  - should preserve stack trace through serialization
- **Specialized error types**
  - should create PermissionError with correct code
  - should create TimeoutError with correct code
  - should create AbortError with default message
  - should create ConfigError with details
  - should create ValidationError
  - should create NotFoundError
  - should create RateLimitError with retry info
  - should create PlatformError with service name
- **Error wrapping utilities**
  - should identify PluginError instances
  - should wrap Error as PluginError
  - should wrap TypeError with custom code
  - should not re-wrap PluginError
  - should wrap non-Error objects as string
- **Error code constants**
  - should have all error codes defined
- **IPC error transport**
  - should preserve all error info through IPC round-trip
  - should handle nested error details
- **Error instanceof checks**
  - should work with instanceof after deserialization
  - should recommend checking error.code for deserialized errors

**`core/plugin-runtime/src/__tests__/fs-shim-glob.test.ts`**

- **fs-shim glob pattern matching**
- **read permissions with glob patterns**
  - should allow reading from double-star glob pattern
  - should allow reading from single-star glob pattern
  - should allow reading with prefix match (no glob chars)
  - should reject reading from non-matching glob pattern outside cwd
  - should always allow reading from cwd (default)
- **write permissions with glob patterns**
  - should allow writing to double-star glob pattern
  - should allow writing with prefix match
  - should reject writing outside allowed patterns
  - should allow writing to outdir by default
- **hardcoded security patterns**
  - should deny access to .env files regardless of permissions
  - should deny access to .ssh directory regardless of permissions
  - should deny access to .key and .secret files regardless of permissions
  - should allow regular files that do not match security patterns
- **glob edge cases**
  - should handle question mark wildcard
  - should not match multi-char with question mark for explicit patterns
  - should handle multiple patterns
  - should handle deeply nested paths with double-star
  - should handle absolute paths in patterns
- **mkdir with glob permissions**
  - should allow mkdir in writable pattern
  - should reject mkdir outside writable pattern
- **copy and move with glob permissions**
  - should allow copy when src is readable and dest is writable
  - should reject copy when dest is not writable

**`core/plugin-runtime/src/__tests__/host-wrappers.test.ts`**

- **Host Wrappers**
  - should wrap non-zero exitCode CommandResult
- **Host Wrappers > wrapCliResult**
  - should wrap CommandResult with exitCode
  - should wrap raw data as exitCode 0
  - should wrap void/undefined as exitCode 0
  - should wrap null as exitCode 0
  - should preserve custom meta from CommandResult
- **wrapRestResult**
  - should wrap data with metadata headers
  - should handle missing optional fields
- **unwrapRestData**
  - should extract raw data from RunResult
  - should handle primitive data types
  - should handle null
  - should handle arrays
- **Type preservation**
  - should preserve generic type through wrapCliResult
  - should preserve generic type through wrapRestResult

**`core/plugin-runtime/src/__tests__/ipc-log-message.test.ts`**

- **IPC Protocol — LogMessage**
  - should create valid log message
  - should support optional meta field
  - should be recognized as ChildMessage by isChildMessage
  - should NOT be recognized as ParentMessage
  - should be part of ChildMessage union
  - should serialize and deserialize through JSON (IPC transport)

**`core/plugin-runtime/src/__tests__/ipc-protocol.test.ts`**

- **IPC Protocol**
  - should be recognized as ParentMessage
- **IPC Protocol > ExecuteMessage**
  - should create valid execute message
- **AbortMessage**
  - should create valid abort message
  - should be recognized as ParentMessage
- **ReadyMessage**
  - should create valid ready message
  - should be recognized as ChildMessage
- **ResultMessage**
  - should carry raw handler output as data
  - should carry CommandResult as data when handler returns it
  - should allow undefined data (void handler)
  - should allow primitive data
  - should be recognized as ChildMessage
- **ErrorMessage**
  - should create valid error message
  - should be recognized as ChildMessage
- **Type Guards**
  - should accept abort message
- **Type Guards > isParentMessage**
  - should accept execute message
  - should reject child messages
  - should reject null and undefined
  - should reject non-objects
  - should reject objects without type field
  - should reject objects with invalid type
- **isChildMessage**
  - should accept ready message
  - should accept result message
  - should accept error message
  - should reject parent messages
  - should reject null and undefined
  - should reject non-objects
  - should reject objects without type field
  - should reject objects with invalid type
- **JSON Serialization**
  - should serialize and deserialize ExecuteMessage
  - should serialize and deserialize ResultMessage
  - should serialize and deserialize ErrorMessage
- **Union Types**
  - should allow ParentMessage union to accept both execute and abort
  - should allow ChildMessage union to accept ready, result, error

**`core/plugin-runtime/src/__tests__/jobs-api.test.ts`**

- **JobsAPI**
  - should submit job and return job ID
- **JobsAPI > createJobsAPI**
  - should pass job options to scheduler.submit
  - should schedule job with cron expression
  - should schedule job with specific date
  - should wait for job completion
  - should throw if job fails during wait
  - should throw if job is cancelled during wait
  - should throw if job not found during wait
  - should get job status
  - should return null if job not found
  - should cancel job
  - should return false if job not found on cancel
  - should list jobs with filters
  - should allow jobs matching types scope
  - should deny jobs not matching types scope
  - should allow all job types with wildcard scope
  - should allow jobs when types is not specified
  - should check types scope for schedule operation
- **createNoopJobsAPI**
  - should throw on submit/schedule/wait operations
  - should return null/false for status/cancel operations
  - should return empty array for list

**`core/plugin-runtime/src/__tests__/metadata-injection.test.ts`**

- **Metadata Injection**
  - should handle void result (no return from handler)
- **Metadata Injection > injectStandardMeta**
  - should inject all standard metadata fields
  - should handle result with no custom metadata
  - should allow custom metadata to override standard metadata
  - should handle optional fields (tenantId, commandId)
  - should calculate duration correctly
  - should format executedAt as ISO 8601 string

**`core/plugin-runtime/src/__tests__/permissions-wildcards.test.ts`**

- **Wildcard Permissions - fs.read**
  - should allow reading ANY path with ** wildcard
  - should still block DENIED_PATTERNS even with ** wildcard
  - should allow glob patterns with * wildcard for specific directories
- **Wildcard Permissions - fs.write**
  - should allow writing to ANY path with ** wildcard
  - should still block dangerous writes even with ** wildcard
- **Wildcard Permissions - network.fetch**
  - should allow fetching ANY URL with * wildcard
  - should block fetch when no wildcard permission
- **Wildcard Permissions - env**
  - should allow reading ANY env var with * wildcard
  - should block env vars when no wildcard permission
- **SYSTEM_UNRESTRICTED_PERMISSIONS**
  - should grant full access with all wildcards
  - should still enforce DENIED_PATTERNS security boundary

**`core/plugin-runtime/src/__tests__/permissions.test.ts`**

- **Permission Enforcement**
- **Permission Enforcement > fs.read - cwd always readable**
  - should allow reading from cwd by default
  - should allow reading subdirectories of cwd
  - should allow reading additional paths via fs.read
  - should block reading from absolute paths outside cwd
- **fs.read - DENIED_PATTERNS**
  - should block reading .env files
  - should block reading from .git directory
  - should block reading from node_modules
- **fs.write - outdir always writable**
  - should allow writing to outdir by default
  - should block writing outside outdir
  - should allow writing to additional paths via fs.write
- **Path normalization security**
  - should prevent path traversal with ../
  - should normalize relative paths correctly
- **net.fetch permissions**
  - should block fetch when network permission not granted
  - should allow fetch from allowed domains (permission check only)
  - should block fetch to non-allowed domains
- **env permissions**
  - should allow always-allowed env vars (NODE_ENV, DEBUG, etc)
  - should return undefined for non-allowed env vars
  - should allow reading whitelisted env vars

**`core/plugin-runtime/src/__tests__/platform-api-calls.test.ts`**

- **Platform API calls from handler**
  - handler can call ctx.platform.logger.info without throwing
  - handler can call ctx.platform.analytics.track without throwing
  - handler can call ctx.platform.eventBus.publish without throwing
  - handler can call ctx.ui methods without throwing
  - handler can read ctx.requestId and ctx.pluginId
  - handler receives input and returns data correctly
  - executionMeta contains all required fields

**`core/plugin-runtime/src/__tests__/platform-context.test.ts`**

- **platformContext (AsyncLocalStorage)**
  - should return undefined outside of run()
  - should return platform inside run()
  - should propagate through async/await
  - should isolate nested contexts
  - should isolate parallel executions
  - should be shared across bundled copies via Symbol.for

**`core/plugin-runtime/src/__tests__/plugin-api.test.ts`**

- **Plugin API**
- **lifecycle API**
  - should register cleanup functions
  - should execute cleanups in LIFO order
  - should continue cleanup on errors
- **state API**
  - should prefix keys with tenant:plugin
  - should use "default" tenant when tenantId not provided
  - should get, set, delete, has
  - should support getMany and setMany
  - should support setMany with object
- **artifacts API**
  - should write artifact to outdir
  - should support nested artifact paths
  - should list artifacts
  - should read artifact
  - should read artifact as buffer
  - should check artifact existence
  - should return artifact path
- **shell API**
  - should block shell when permission not granted
  - should allow whitelisted commands
  - should block non-whitelisted commands
  - should block dangerous commands
  - should allow all commands when whitelist is empty
- **events API**
  - should use noop implementation when no emitter provided
  - should call provided emitter with prefixed event
- **invoke API**
  - should use noop implementation when no invoker provided
  - should block invoke when permission not granted
  - should call provided invoker when permission granted
  - should check plugin whitelist
  - should deny targeted invoke when execution target permission is missing
  - should allow targeted invoke when execution target scope is granted
  - should emit target execution audit event for targeted invoke

**`core/plugin-runtime/src/__tests__/runtime-api.test.ts`**

- **Runtime API**
- **fs.readFile**
  - should read file from cwd
  - should read file with custom encoding
  - should reject reading outside cwd
- **fs.readFileBuffer**
  - should read file as Uint8Array
- **fs.writeFile**
  - should write file to outdir
  - should create parent directories automatically
  - should support append mode
  - should reject writing outside outdir without permission
- **fs.readdir**
  - should list directory contents
- **fs.readdirWithStats**
  - should list directory with file type info
- **fs.stat**
  - should return file stats
  - should return directory stats
- **fs.exists**
  - should return true for existing file
  - should return false for non-existent file
  - should return false for forbidden paths (not throw)
- **fs.mkdir**
  - should create directory in outdir
  - should support recursive option
- **fs.rm**
  - should remove file from outdir
  - should support recursive removal
- **fs.copy**
  - should copy file from cwd to outdir
  - should copy directories recursively
- **fs.move**
  - should move file within outdir
- **fs path utilities**
  - should resolve paths relative to cwd
  - should make paths relative to cwd
  - should join path segments
  - should get dirname
  - should get basename
  - should get extname
- **fetch permissions**
  - should allow fetch with wildcard patterns (permission check only)
  - should block fetch without permission
- **env permissions**
  - should allow always-allowed env vars
  - should support prefix patterns (KB_*)
  - should return undefined for non-allowed vars

**`core/plugin-runtime/src/__tests__/sandbox-runner.test.ts`**

- **Sandbox Runner**
- **runInProcess - RunResult contract**
  - should return RunResult with data and executionMeta
  - should return raw CommandResult structure when handler returns it
  - should handle void return (undefined data)
  - should include tenantId in executionMeta when provided
- **runInProcess - Error handling**
  - should throw PluginError if handler missing execute function
  - should propagate handler errors
- **runInProcess - Cleanup handlers**
  - should execute cleanup handlers after success
  - should execute cleanup handlers even after error
- **runInProcess - Context**
  - should pass signal to context
  - should handle named exports (module with execute function)
  - should provide complete context to handler
- **runInSubprocess**
  - should find bootstrap.js in dist directory
  - should execute handler in subprocess with IPC and metadata injection
  - should handle subprocess timeout
  - should handle abort signal in subprocess

**`core/plugin-runtime/src/__tests__/snapshot-api.test.ts`**

- **SnapshotAPI**
  - captures snapshot with full permission
  - enforces namespace scope on capture
  - enforces namespace scope on gc
  - denies restore when permission is missing
  - noop API reports unavailable manager

**`core/plugin-runtime/src/__tests__/streaming-logger.test.ts`**

- **createStreamingLogger**
  - should call base logger for info/warn/error/fatal
  - should emit logger.line events (NOT log.line) for info/warn/error/fatal
  - should never emit log.line (ui/shell namespace)
  - should NOT emit events for trace and debug
  - should increment lineNo monotonically
  - should pass additional args to base logger
  - should create streaming child loggers
  - should map stream correctly: info=stdout, warn/error/fatal=stderr
  - should not throw if emitter throws
  - should delegate getLogBuffer to base

**`core/plugin-runtime/src/__tests__/streaming-ui.test.ts`**

- **createStreamingUI**
  - delegates all calls to the base UI
  - emits log.line for info → stdout
  - emits log.line for success → stdout
  - emits log.line for warn → stderr
  - emits log.line for error (string) → stderr
  - emits log.line for error (Error object) using .message
  - strips ANSI escape codes from emitted lines
  - buffers write() and emits complete lines by newline
  - holds incomplete write() lines until newline arrives
  - skips empty/whitespace-only lines
  - skips ANSI-only lines (e.g. spinner animation frames)
  - emits spinner start/succeed/fail/update events
  - emits spinner fail with error stream
  - delegates spinner stop to base without emitting
  - increments lineNo monotonically across calls
  - does not throw if emitter rejects (fire-and-forget)
  - log() delegates to base and emits log.line
  - log() with fields appends JSON-encoded fields to the emitted line
  - log() error level → stderr stream
  - passes through non-overridden methods unchanged

**`core/plugin-runtime/src/__tests__/trace-context.test.ts`**

- **TraceContext**
- **trace/span IDs**
  - should store trace and span IDs
  - should store parent span ID when provided
- **addEvent**
  - should record events with timestamps
  - should record events without attributes
  - should record multiple events
- **setAttribute**
  - should store string attributes
  - should store number attributes
  - should store boolean attributes
  - should allow overwriting attributes
- **recordError**
  - should record error as event with exception details
  - should include stack trace in error recording
  - should handle errors without custom name
- **integration - typical usage flow**
  - should support full trace lifecycle
  - should maintain trace ID across multiple events

**`core/plugin-runtime/src/__tests__/ui-facade.test.ts`**

- **UIFacade**
- **colors API**
  - should have all semantic color functions
  - should have all accent color functions
  - should have all formatting helper functions
  - should accept string and return string
  - should work with all color functions
- **symbols API**
  - should have all status symbols
  - should have all UI element symbols
  - should have all box-drawing symbols
  - should return expected symbol values
  - should be usable in write calls
- **write method**
  - should exist and be callable
  - should accept string argument
  - should handle empty string
  - should handle multiline text
- **info method**
  - should accept string message
  - should accept message with options
  - should handle empty message
- **success method**
  - should accept string message
  - should accept message with options
- **warn method**
  - should accept string message
  - should accept message with options
- **error method**
  - should accept string message
  - should accept message with options
- **debug method**
  - should accept string message
  - should accept message
- **table method**
  - should accept columns and rows
  - should handle empty rows
- **box method**
  - should accept content and title
  - should accept content without title
- **sideBox method**
  - should accept options with title
  - should accept options with status
  - should accept options with summary
  - should accept options with sections
  - should accept options with timing
  - should accept full options
- **json method**
  - should accept any JSON-serializable value
  - should accept arrays
  - should accept primitives
- **newline method**
  - should be callable without arguments
  - should be callable multiple times
- **divider method**
  - should be callable without arguments
  - should accept optional title
- **spinner method**
  - should return spinner object
- **confirm method**
  - should accept message and return boolean
  - should accept message with default value
- **prompt method**
  - should accept message and options
  - should handle empty options
- **combined usage**
  - should allow chaining multiple UI calls
  - should allow using colors with write
  - should allow combining colors and symbols
  - should work with symbols in output methods
  - should allow mixing text output methods

**`core/plugin-runtime/src/__tests__/unix-socket-client.test.ts`**

- **UnixSocketClient**
- **Connection**
  - should connect to Unix socket server
  - should throw error if server not running
  - should reuse existing connection
  - should use default socket path if not specified
- **RPC Calls**
  - should send RPC call and receive response
  - should handle multiple concurrent calls
  - should reject if server returns error
  - should timeout after specified duration
  - should use default timeout of 30s
- **Cleanup**
  - should close connection
  - should reject pending calls on close
  - should throw if calling after close
  - should allow multiple close calls
- **Message Protocol**
  - should send newline-delimited JSON
  - should handle buffered responses

**`core/plugin-runtime/src/__tests__/workflows-api.test.ts`**

- **WorkflowsAPI**
  - should run workflow and return run ID
- **WorkflowsAPI > createWorkflowsAPI**
  - should pass options to engine.execute
  - should wait for workflow completion
  - should throw if workflow fails during wait
  - should throw if workflow is cancelled during wait
  - should throw if workflow not found during wait
  - should get workflow status
  - should return null if workflow not found
  - should cancel workflow
  - should list workflows with filters
  - should calculate progress correctly
- **workflow permissions**
  - should deny all operations when workflows permission is false
  - should deny all operations when workflows permission is undefined
  - should deny all operations when permissions object is undefined
  - should allow specific operations with granular permissions
  - should deny specific operations when granular permission is missing
  - should allow workflows matching workflowIds scope
  - should deny workflows not matching workflowIds scope
  - should allow all workflows with wildcard scope
  - should allow workflows when workflowIds is not specified
  - should deny targeted workflow run when target namespace is missing
  - should deny targeted workflow run outside execution namespace scope
  - should call target execution audit callback for targeted workflow run
- **createNoopWorkflowsAPI**
  - should throw on all operations

**`core/plugin-runtime/src/__tests__/workspace-api.test.ts`**

- **WorkspaceAPI**
  - materializes workspace with full permission
  - enforces source scope on materialize
  - enforces namespace scope on materialize
  - enforces path scope on attach
  - denies release when permission is missing
  - noop API reports unavailable manager

**`core/plugin-runtime/src/platform/__tests__/governed.test.ts`**

- **createGovernedPlatformServices**
- **Logger**
  - should always allow logger and create child with plugin context
- **LLM**
  - should proxy complete() method when permission granted
  - should proxy stream() method when permission granted
  - should enforce model whitelist when specified
  - should deny access when permission not granted
- **Embeddings**
  - should proxy embed() method when permission granted
  - should proxy embedBatch() method when permission granted
  - should proxy dimensions property when permission granted
  - should proxy getDimensions() method when permission granted
  - should deny access when permission not granted
- **Cache**
  - should allow cache operations with namespace permission
  - should enforce namespace restrictions
  - should proxy sorted set operations with namespace check
  - should proxy setIfNotExists with namespace check
  - should require full permission for clear()
  - should allow clear() with pattern when full permission granted
  - should allow cache operations when permission is string[] (builder format)
  - should enforce namespace restrictions when permission is string[]
  - should deny access when permission not granted
- **Storage**
  - should allow storage operations within permitted paths
  - should enforce path restrictions
  - should deny access when permission not granted
- **VectorStore**
  - should pass through vectorStore when permission granted
  - should deny access when permission not granted
- **Analytics**
  - should always allow analytics access
- **EventBus**
  - should always allow eventBus access
- **Edge Cases**
  - should handle boolean true permission (full access)
  - should handle boolean false permission (denied)
  - should handle undefined permission (denied)
  - should handle empty namespace/path arrays (deny all)

### `@kb-labs/policy-contracts`

#### Unit tests

**`plugins/policy/contracts/tests/contracts.manifest.test.ts`**

- **pluginContractsManifest**
  - is valid according to the schema
  - rejects malformed manifests
  - uses a semver-compatible contractsVersion

**`plugins/policy/contracts/tests/format-violation.test.ts`**

- **formatViolation**
  - includes the message
- **formatViolation > first line**
  - includes severity and rule in brackets/dash format
  - does NOT include package by default
  - includes package when includePackage is true
- **detail line**
  - includes detail by default when present
  - omits detail when includeDetail is false
  - does not add a detail line when the field is absent
- **file line**
  - includes file by default when present
  - omits file when includeFile is false
  - does not add a file line when the field is absent
- **line ordering**
  - emits: first line → detail → file
  - returns a single line for a minimal violation
- **no trailing newline**
  - does not end with a newline
- **severity variants**
  - formats "warning" severity correctly
  - formats "error" severity correctly

**`plugins/policy/contracts/tests/get-violation-summary.test.ts`**

- **getViolationSummary(PolicyViolation[])**
  - returns an empty object for an empty array
  - counts a single violation
  - accumulates multiple violations for the same rule
  - counts distinct rules independently
  - handles three different rules
  - omits rules that are not present in the input
  - all values in the summary are positive integers
- **getViolationSummary(RepoCheckResult)**
  - returns an empty object when the repo has no violations
  - counts violations from a single repo
  - ignores the passed[] list — only violations are counted
- **getViolationSummary(CheckReport)**
  - returns an empty object when no repos have violations
  - aggregates violations across all repos
  - handles a report with mixed passing and failing repos
  - returns an empty object for an empty repos array
  - sums the same rule across multiple repos correctly
- **getViolationSummary — return value contract**
  - returns a plain object (not null, not an array)
  - is JSON-serialisable

**`plugins/policy/contracts/tests/is-policy-passing.test.ts`**

- **isPolicyPassing**
  - returns true for a passing report
  - returns false for a failing report

**`plugins/policy/contracts/tests/merge-violations.test.ts`**

- **mergeViolations**
  - returns a clean zeroed report for an empty array
  - passes through a single clean report unchanged
  - passes through a single failing report and recomputes its summary correctly
  - merges two clean reports into one passing report
  - sets passed:false when one of two reports has violations
  - aggregates violations from two failing reports
  - preserves repo order: report[0] repos first, then [1], then [2]
  - summary.total equals the sum of all repo counts across input reports
  - summary.passed (count) is distinct from the top-level passed (boolean)
  - handles reports that each contain multiple repos
  - does not mutate the original reports\
  - keeps duplicate-path repos as separate entries (no deduplication)

### `@kb-labs/qa-core`

#### E2E / journey tests

**`plugins/qa/core/tests/build-order.spec.ts`**

- **computeBuildLayers**
  - returns empty array for empty input
  - puts all packages in layer 0 when no workspace deps
  - sorts linear dependency chain into layers
  - groups independent packages in same layer
  - handles devDependencies
  - ignores external (non-workspace) dependencies
  - handles circular dependencies by appending as last layer
  - handles diamond dependency pattern
  - handles single package
- **sortByBuildLayers**
  - returns packages in dependency order
  - returns empty array for empty input
  - preserves all packages

**`plugins/qa/core/tests/custom-check-runner.spec.ts`**

- **runCustomChecks — exitcode parser**
  - passes when command exits 0
  - fails when command exits non-zero
- **runCustomChecks — json parser + TypedCheckOutput**
  - passes when TypedCheckOutput.ok is true
  - fails when TypedCheckOutput.ok is false
  - populates details[] from TypedCheckOutput.items on failure
  - no details when TypedCheckOutput.items is empty
  - falls back to legacy json format (success field)
- **runCustomChecks — runIn: perPackage**
  - runs once per package
  - records individual package failures
- **runCustomChecks — diffOnly**
  - runs only on packages touched by diff
  - skips all packages when diff is empty (same as base)
- **runCustomChecks — newFiles**
  - runs only on packages with added files, skips modified-only
- **runCustomChecks — progress callback**
  - fires progress events for each package

**`plugins/qa/core/tests/history-store.spec.ts`**

- **createHistoryEntry**
  - creates entry with passed status
  - creates entry with failed status
  - attaches runContext when provided
  - no runContext field when not provided
  - handles non-git directory gracefully
  - includes timestamp as ISO string
  - reads git commit and branch
- **appendEntry**
  - creates history.json on first write
  - accumulates entries
  - trims to max 50 entries
  - does NOT write trend file when trending is false
  - writes trend file when trending is true
  - trend file contains the entry
  - does NOT write trend file for check not present in results
  - runContext is persisted to history.json

**`plugins/qa/core/tests/json-reporter.spec.ts`**

- **buildJsonReport — status**
  - passed when all checks pass
  - failed when a blocker check has failures
  - passed when only warning check fails
  - passed when only info check fails
  - failed when blocker fails even if warning also fails
  - defaults to blocker severity when no config provided
- **buildJsonReport — blockers**
  - empty when all checks pass
  - populated for failed blocker check
  - uses structured details when available
  - warning check not in blockers
- **buildJsonReport — warnings**
  - empty when no warning checks fail
  - populated for failed warning check
  - blocker check not in warnings
  - info check not in warnings or blockers
- **buildJsonReport — backward compat**
  - summary still populated
  - failures still populated
  - blockers defaults to blocker severity when no config

**`plugins/qa/core/tests/trend-store.spec.ts`**

- **loadTrendHistory**
  - returns empty array when file does not exist
  - returns empty array on corrupt JSON
- **appendTrendEntry**
  - creates file and writes first entry
  - creates parent directory if missing
  - accumulates multiple entries
  - keeps check IDs isolated — different check IDs write to different files
  - trims to max 50 entries
  - oldest entry is dropped when over limit
  - preserves runContext in stored entries

### `@kb-labs/quality-contracts`

#### Unit tests

**`plugins/quality/contracts/tests/contracts.manifest.test.ts`**

- **pluginContractsManifest**
  - is valid according to the schema
  - rejects malformed manifests
  - uses a semver-compatible contractsVersion

### `@kb-labs/release-manager-core`

#### Unit tests

**`plugins/release/manager-core/src/__tests__/checkpoint.test.ts`**

- **checkpoint — read/write lifecycle**
  - returns null when no checkpoint file exists
  - round-trips write → load
  - deleteCheckpoint removes the file
  - deleteCheckpoint does not throw when file is missing
- **checkpoint — updateCheckpointGitRoot**
  - updates gitRoots entry
  - is a no-op when no checkpoint exists
- **checkpoint — markCheckpointComplete**
  - sets completedAt timestamp
- **isCheckpointResumable**
  - returns true when publish done and git not fully pushed
  - returns false when completedAt is set
  - returns false when flow does not match
  - returns false when version does not match and not independent
  - accepts any version when version param is "independent"
  - returns false when no packages published
  - returns false when all git roots are already pushed

**`plugins/release/manager-core/src/__tests__/checks.test.ts`**

- **runReleaseChecks — parser: exitcode**
  - passes when command exits 0
  - fails when command exits non-zero
- **runReleaseChecks — parser: json**
  - passes when stdout contains { ok: true }
  - passes when stdout contains { success: true }
  - passes when stdout contains { status: "ok" }
  - fails when json reports { ok: false }
  - fails when stdout is not valid JSON
- **runReleaseChecks — parser: function**
  - uses custom parser function
  - custom parser receives stdout, stderr, exitCode
- **runReleaseChecks — optional**
  - continues after optional failure
  - stops after required failure

**`plugins/release/manager-core/src/__tests__/dep-rewrite.test.ts`**

- **rewriteWorkspaceDeps**
  - rewrites workspace:* to ^version for non-pnpm
  - does NOT rewrite workspace:* for pnpm (handled natively)
  - rewrites link: deps regardless of package manager
  - uses versionMap to rewrite link: when available
  - rewrites peerDependencies as well as dependencies
  - restore returns original content verbatim

**`plugins/release/manager-core/src/__tests__/lock.test.ts`**

- **acquireLock**
  - creates a lock file and returns a release function
  - release removes the lock file
  - release is idempotent — calling twice does not throw
  - throws if another live process holds the lock
  - overwrites a stale lock from a dead process (PID 1 is init, not us)
  - proceeds when lock file is corrupted/missing

**`plugins/release/manager-core/src/__tests__/planner.test.ts`**

- **matchesPackagePattern**
  - does not match different name
- **matchesPackagePattern > package name patterns**
  - matches exact name
  - matches wildcard scope pattern
  - wildcard scope does not match other scope
  - matches suffix wildcard
  - suffix wildcard does not match non-matching name
  - matches unscoped package name
- **path patterns**
  - matches path glob
  - does not match different dir
  - matches nested path
- **multiple patterns**
  - returns true if any pattern matches
  - returns false if no pattern matches
- **planRelease — packages filter**
  - discovers all non-private packages by default
  - exclude removes specific package by name
  - exclude supports wildcard
  - include restricts to matching packages only
  - paths restricts discovery to given dirs
  - per-scope exclude merges with global exclude

**`plugins/release/manager-core/src/__tests__/verifier.test.ts`**

- **verifyPackage — shell injection safety (H9)**
  - calls spawnSync for npm pack with args array (no shell string)
  - calls spawnSync for tar with args array (no shell string)
  - calls spawnSync for node --check with args array (no shell string)
  - node --check receives path containing shell metacharacters as a literal arg
  - skips private packages without calling spawnSync

### `@kb-labs/rest-api-app`

#### E2E / journey tests

**`plugins/rest-api/app/src/middleware/__tests__/cache.spec.ts`**

- **generateETag**
  - should generate consistent ETag for same string content
  - should generate different ETags for different content
  - should handle object content by stringifying
  - should handle Buffer content
  - should handle empty string
  - should handle empty object
- **registerCacheMiddleware**
- **ETag handling**
  - should add ETag header to GET responses when If-None-Match is provided
  - should return 304 when ETag matches
  - should return 304 when weak ETag matches
  - should not add ETag to POST requests
- **Last-Modified handling**
  - should add Last-Modified header when If-Modified-Since is provided
- **streaming responses**
  - should skip cache for SSE responses

**`plugins/rest-api/app/src/middleware/__tests__/envelope.spec.ts`**

- **registerEnvelopeMiddleware**
- **successful responses**
  - should wrap JSON response in envelope
  - should add x-schema-version header
  - should handle empty object response
  - should handle null response
  - should handle array response
  - should not double-wrap already enveloped responses
- **error responses**
  - should wrap errors in error envelope
  - should default to E_INTERNAL for unknown errors
  - should include error details when provided
  - should include traceId when provided
  - should always produce ok:false for error responses — never ok:true wrapping
- **onSend object payload guard**
  - should not crash when an object payload reaches onSend (defensive)
- **streaming responses**
  - should skip envelope for SSE responses
- **non-JSON responses**
  - should pass through non-JSON string responses
- **cache middleware ordering**
  - ETag reflects the envelope-wrapped payload, not the raw data

**`plugins/rest-api/app/src/middleware/__tests__/metrics.redis.spec.ts`**

- **metricsCollector.recordRedisStatus**
  - tracks redis health transitions and role states

**`plugins/rest-api/app/src/middleware/__tests__/rate-limit.spec.ts`**

- **extractTenantId**
  - should extract tenant ID from X-Tenant-ID header
  - should fall back to KB_TENANT_ID env var when header is missing
  - should use default when no header and no env var
  - should prefer header over env var
- **createRateLimitMiddleware**
- **allowed requests**
  - should allow request and add rate limit headers
  - should store tenantId in request context
- **rate limited requests**
  - should return 429 when rate limit exceeded
  - should include Retry-After header when rate limited
  - should still include rate limit headers when limited
- **tenant isolation**
  - should check rate limit for each tenant separately
- **default tenant**
  - should use default tenant when no header provided

**`plugins/rest-api/app/src/middleware/__tests__/request-id.spec.ts`**

- **registerRequestIdMiddleware**
- **request ID generation**
  - should generate request ID if not provided
  - should use provided request ID from header
  - should use provided trace ID from header
- **logger integration**
  - should attach logger to request object
  - should attach logger to reply object
  - should allow calling logger methods without errors

**`plugins/rest-api/app/src/routes/__tests__/adapters-date-helpers.spec.ts`**

- **extractDateRange**
- **extractDateRange > valid inputs**
  - should extract both from and to dates when provided
  - should extract only from date when to is not provided
  - should extract only to date when from is not provided
  - should return empty object when neither date is provided
  - should handle ISO 8601 dates with milliseconds
  - should handle ISO 8601 dates without timezone
  - should handle ISO 8601 dates with timezone offset
  - should handle short ISO 8601 date format
- **invalid inputs**
  - should throw error for invalid from date format
  - should throw error for invalid to date format
  - should throw error for malformed date string
  - should throw error for numeric date value
  - should throw error when both dates are invalid
- **edge cases**
  - should handle query object with other unrelated parameters
  - should handle null query object
  - should handle undefined query object
- **isValidISODate**
  - should return true for valid ISO 8601 datetime with milliseconds
- **isValidISODate > valid dates**
  - should return true for valid ISO 8601 datetime with Z timezone
  - should return true for valid ISO 8601 datetime without timezone
  - should return true for valid ISO 8601 datetime with timezone offset
  - should return true for short ISO 8601 date format
  - should return true for valid date in the past
  - should return true for valid date in the future
  - should return true for leap year date
- **invalid dates**
  - should return false for completely invalid string
  - should return false for empty string
  - should return false for malformed ISO 8601 date
  - should return false for invalid month
  - should return false for invalid day
  - should return false for non-date strings
- **edge cases**
  - should return false for numeric value
  - should return false for null
  - should return false for undefined
  - should return false for object
  - should return false for array

**`plugins/rest-api/app/src/routes/__tests__/adapters.spec.ts`**

- **Adapter Analytics Routes - Date Range Support**
- **Adapter Analytics Routes - Date Range Support > GET /api/v1/adapters/llm/usage**
  - should return LLM usage stats without date range
  - should return LLM usage stats with date range
  - should return 400 for invalid from date
  - should return 400 for invalid to date
  - should return 501 when analytics does not support getEvents
- **GET /api/v1/adapters/embeddings/usage**
  - should return embeddings usage stats with date range
  - should return 400 for invalid date range
- **GET /api/v1/adapters/vectorstore/usage**
  - should return vectorstore usage stats with date range
  - should return 400 for invalid date
- **GET /api/v1/adapters/cache/usage**
  - should return cache usage stats with date range
  - should work without date parameters for backward compatibility
- **GET /api/v1/adapters/storage/usage**
  - should return storage usage stats with date range
  - should handle only from parameter
  - should handle only to parameter
- **Date format support**
  - should accept ISO 8601 with milliseconds
  - should accept ISO 8601 with timezone offset
  - should accept short ISO 8601 date format

**`plugins/rest-api/app/src/routes/__tests__/cache.spec.ts`**

- **registerCacheRoutes**
- **POST /api/v1/cache/invalidate**
  - successfully invalidates cache and refreshes registry
  - returns previous and new revision numbers
  - includes plugin registry count
  - handles cache refresh errors gracefully
  - handles non-Error exceptions
  - handles snapshot() errors
  - tracks request timing in metadata
  - includes timestamp in ISO format
  - handles empty plugin list after refresh

**`plugins/rest-api/app/src/routes/__tests__/events.spec.ts`**

- **registerEventRoutes**
- **GET /api/v1/events/registry**
  - returns SSE stream with correct headers
  - sends initial registry event
  - sends health event after registry event
  - subscribes to event hub
  - includes Redis status in health event when Redis is enabled
  - includes plugin mount statistics in health event
  - includes readiness state in health event
  - handles system health fetch errors gracefully
  - sets CORS headers for localhost:3000 origin
  - sets CORS headers for localhost:5173 origin (Vite dev)
  - does not set CORS headers for other origins
  - handles snapshot with missing checksum fields
  - handles snapshot with null expiration fields
  - handles missing getRedisStatus method gracefully
  - handles missing plugin mount snapshot
  - handles null lastPluginMountTs

**`plugins/rest-api/app/src/routes/__tests__/health.spec.ts`**

- **registerHealthRoutes**
  - returns kb.health/1 snapshot with readiness metadata and degraded status when plugin failures exist
  - ready endpoint waits for registry load and mounted routes
  - reports redis_unavailable when redis is enabled but unhealthy
  - exposes observability describe and health surfaces
  - metrics endpoint exposes canonical observability families

**`plugins/rest-api/app/src/routes/__tests__/logs.spec.ts`**

- **Log Routes Integration Tests**
- **GET /api/v1/logs**
  - should query logs with filters
  - should use search when search query provided
  - should handle pagination
  - should return stats in response
  - should handle errors gracefully
- **GET /api/v1/logs/:id**
  - should return log by ID
  - should return 404 when log not found
  - should handle errors
- **GET /api/v1/logs/stream**
  - should return 503 when streaming not supported
- **GET /api/v1/logs/stats**
  - should return combined statistics
  - should handle only buffer stats
  - should handle errors
- **POST /api/v1/logs/summarize**
  - should summarize logs with LLM
  - should handle missing LLM gracefully
  - should handle errors

**`plugins/rest-api/app/src/routes/__tests__/observability.spec.ts`**

- **registerObservabilityRoutes**
- **GET /api/v1/observability/state-broker**
  - returns State Broker statistics
  - uses KB_STATE_DAEMON_URL environment variable
  - falls back to default URL when env var not set
  - returns 503 when State Broker responds with error status
  - returns 503 when State Broker times out
  - returns 503 when State Broker connection fails
  - handles non-Error exceptions

**`plugins/rest-api/app/src/routes/__tests__/plugins.spec.ts`**

- **registerPluginRoutes**
  - successfully mounts plugin routes with REST endpoints
  - handles plugins without REST routes
  - validates handler file existence and skips missing handlers
  - handles invalid handler references gracefully
  - mounts multiple plugins in parallel
  - continues mounting other plugins when one fails
  - handles partial/stale registry snapshots
  - uses custom basePath from manifest.rest.basePath
  - falls back to default basePath when manifest does not specify one
  - handles discovery failure gracefully
  - works without readiness tracking
  - registers route budgets for metrics tracking
- **registerPluginSnapshotRoutes**
  - logs a structured diagnostic when plugin registry refresh fails
  - logs discovery diagnostics when plugin registry refresh succeeds with blocking issues
  - returns plugin registry from legacy endpoint
  - handles registry errors gracefully
  - handles studio registry generation errors
  - returns empty manifests when registry is empty
  - returns discovery diagnostics in plugin registry payload
  - returns discovery diagnostics in plugin health payload

**`plugins/rest-api/app/src/routes/__tests__/sse-auth.spec.ts`**

- **buildRegistrySseAuthHook**
  - returns undefined when auth is not configured
  - allows requests with valid bearer token
  - rejects requests with invalid token

**`plugins/rest-api/app/src/routes/__tests__/workflows.spec.ts`**

- **registerWorkflowRoutes**
- **GET /workflows/runs/:runId/events (SSE)**
  - returns SSE headers and connected comment
  - emits workflow.event for matching runId
  - sets CORS headers for localhost:3000
  - sets CORS headers for localhost:5173
  - does not set CORS headers for other origins
  - calls unsubscribe on terminal event
  - filters events by runId — ignores other runs
- **GET /workflows/runs/:runId/logs (SSE)**
  - returns SSE headers and connected comment
  - emits workflow.log events
  - sets CORS headers for localhost:3000
  - does not set CORS headers for unknown origins
  - respects custom idleTimeoutMs query param

### `@kb-labs/rest-api-core`

#### E2E / journey tests

**`plugins/rest-api/core/src/__tests__/config-schema.spec.ts`**

- **restApiConfigSchema**
  - should provide cors defaults
- **restApiConfigSchema > defaults**
  - should provide all defaults for empty object
- **port validation**
  - should accept valid port numbers
  - should reject invalid port numbers
- **cors validation**
  - should accept valid cors profiles
  - should reject invalid cors profile
  - should accept array of origins
- **timeouts validation**
  - should accept valid timeout values
  - should reject negative timeout values
- **rateLimit validation**
  - should accept valid rate limit config
  - should reject non-positive max value
- **redis validation**
  - should accept valid redis config
  - should provide default namespace for redis
  - should reject empty redis url
  - should allow config without redis
- **startup validation**
  - should accept valid startup config
  - should allow queueLimit of zero
- **events validation**
  - should accept valid events config with registry token
  - should provide defaults for registry header and query param
  - should reject empty registry token
- **plugins array**
  - should accept array of plugin strings
  - should accept empty plugins array
- **mockMode**
  - should accept boolean mockMode
  - should default to false
- **full config**
  - should accept complete valid config

#### Unit tests

**`plugins/rest-api/core/src/__tests__/studio-registry.test.ts`**

- **manifestToRegistryEntry**
  - returns null for manifest without studio config
  - returns null for manifest with non-v2 studio config
  - returns entry for valid V2 studio config
- **combineManifestsToRegistry**
  - returns empty registry for no manifests
  - skips manifests without V2 studio config
  - generates remoteEntryUrl matching gateway route pattern
  - computes widgetBundleDir from pluginRoot
  - combines multiple V2 manifests

### `@kb-labs/review-entry`

#### CLI handler tests

**`plugins/review/entry/src/__tests__/cli/review-run.cli.test.ts`**

- **review:run**
  - RR-01: no changed files — exitCode 1 with validation message
  - RR-02: changed files found — runs review, exitCode 0 when no blockers
  - RR-03: blocker finding — exitCode 1
  - RR-04: --json flag — outputs ReviewResult via ctx.ui.json
  - RR-05: --scope=staged uses includeStaged filter
  - RR-06: --repos flag skips getReposWithChanges, uses resolveGitScope directly
  - RR-07: runReview throws — exitCode 1, loader.fail called
  - RR-08: --files flag reads specific files via ctx.runtime.fs.glob

### `@kb-labs/scaffold`

#### CLI handler tests

**`plugins/scaffold/entry/src/__tests__/cli/doctor.cli.test.ts`**

- **scaffold:doctor**
  - SD-01: successful scan with no findings — exitCode 0, prints package count
  - SD-02: --json outputs structured JSON with findings and packagesScanned
  - SD-03: findings with severity=error — exitCode 1, error message shown
  - SD-04: --path is forwarded to scanRoot
  - SD-05: scanRoot throws — exitCode 1, error captured

**`plugins/scaffold/entry/src/__tests__/cli/scaffold.cli.test.ts`**

- **scaffold:run**
  - SS-01: missing entity name — exitCode 1, write or info called
  - SS-02: --dry-run — writeFiles is not called, exitCode 0
  - SS-03: loadEntity throws — command propagates the error
  - SS-04: successful scaffold — exitCode 0, success message shown
  - SS-05: --dry-run with --json — captured.json contains result

#### Unit tests

**`plugins/scaffold/entry/tests/v3-validity.test.ts`**

- **generated plugin manifest passes V3 validation**
  - is V3-valid for: ${blocks.join(' + ')}

### `@kb-labs/scaffold-core`

#### E2E / journey tests

**`plugins/scaffold/core/tests/engine.e2e.test.ts`**

- **engine e2e: plugin/base block**
  - renders base block into a coherent tree
  - switches to standalone (pnpm-workspace.yaml present, semver deps)
  - composes blocks: base + cli + rest + contracts
  - renders adapter entity: base + provider-example
  - actually writes the tree to disk

#### Unit tests

**`plugins/scaffold/core/tests/doctor-lock-sync.test.ts`**

- **doctor lock sync**
  - warns when lock file is absent
  - warns when plugin is scaffolded but its id is missing from existing lock
  - is silent when plugin is properly registered in lock
  - surfaces info when lock entry is disabled
  - warns when lock resolvedPath points elsewhere
  - skips lock checks when workspaceRoot is not given

**`plugins/scaffold/core/tests/patch.test.ts`**

- **deepMerge**
  - merges nested objects
  - unions arrays of objects by id
  - unions arrays of objects by name when id absent
  - dedupes primitive arrays
  - overwrites primitives

**`plugins/scaffold/core/tests/resolver.test.ts`**

- **resolveBlocks**
  - returns a single block when no deps
  - expands requires
  - detects cycles
  - detects conflicts
  - rejects unknown block ids
  - rejects unknown requires

**`plugins/scaffold/core/tests/validators.test.ts`**

- **validators**
  - accepts good package names
  - rejects uppercase
  - rejects reserved
  - accepts valid scopes
  - rejects bad scopes
  - semver

### `@kb-labs/sdk`

#### Unit tests

**`sdk/sdk/src/__tests__/deep-behavior.test.ts`**

- **SDK Deep Behavior**
  - defineCommand rejects unsupported host at runtime
  - defineRoute executes cleanup even when handler throws
  - defineWebhook validates webhook event mismatch
  - defineCommandFlags maps schema fields to manifest flags
  - generateExamples omits false boolean flags and keeps descriptions

**`sdk/sdk/src/__tests__/entrypoint-contracts.test.ts`**

- **SDK Entrypoint Contracts**
  - keeps required root exports
  - keeps required command entrypoint exports
  - keeps required manifest entrypoint exports
  - keeps required hooks entrypoint exports
  - keeps required contracts entrypoint exports
  - keeps required testing entrypoint exports

**`sdk/sdk/src/__tests__/exports.snapshot.test.ts`**

- **SDK Exports Snapshot**
  - should match snapshot of all exported names
  - should match snapshot of exported functions
  - should have createTestContext function
  - should have command definition helpers
- **Test Context Structure**
  - should create context with all required properties
  - should provide ui.colors API
  - should provide ui.write method
  - should match UI methods snapshot
  - should match platform services snapshot
  - should match colors API snapshot
- **Type Exports**
  - should export core contract types

### `@kb-labs/shared-cli-ui`

#### E2E / journey tests

**`shared/cli-ui/src/__tests__/artifacts-display.spec.ts`**

- **artifacts display formatting**
  - renders detailed artifact list with metadata
  - renders compact artifact summary

**`shared/cli-ui/src/__tests__/format.spec.ts`**

- **format helpers**
  - formats timestamp in ISO mode with offset
  - formats timestamp in local mode for a specific timezone
  - returns human-friendly relative time

**`shared/cli-ui/src/__tests__/retry.spec.ts`**

- **withRetry**
  - returns value on first try without calling onRetry
  - succeeds after retries
  - throws the last error when all attempts exhausted
  - applies fixed delay between retries
  - applies exponential backoff between retries
  - calls onRetry with error and attempt number
- **parameter validation**
  - throws RangeError when attempts < 1
  - throws RangeError when delay < 0
  - throws TypeError when fn is not a function

#### Unit tests

**`shared/cli-ui/src/__tests__/loader-json-mode.test.ts`**

- **setJsonMode / isJsonMode**
  - defaults to false
  - setJsonMode(true) flips the flag
  - setJsonMode(false) resets the flag
- **useLoader picks up global json mode**
  - creates a Loader with jsonMode=false by default
  - when setJsonMode(true), useLoader returns a no-op loader
  - explicit jsonMode option overrides global flag
- **Loader in json mode suppresses all output**
  - start() does not write to stdout
  - succeed() does not write to stdout
  - fail() does not write to stdout
- **Loader in normal mode writes to stdout**
  - succeed() writes success symbol to stdout
  - fail() writes error symbol to stdout

### `@kb-labs/shared-command-kit`

#### E2E / journey tests

**`shared/command-kit/src/__tests__/define-command.spec.ts`**

- **defineCommand**
  - should call handler with correct input (V3 API)
  - should enforce CLI host restriction
  - should allow workflow host
  - should handle handler returning number
  - should handle handler returning object with ok
  - should handle errors in handler
  - should call cleanup if provided
  - should handle handler returning exit code 2
  - should handle handler returning object with ok: false
  - should pass through custom result fields
  - should work with async handlers
  - should work with sync handlers

**`shared/command-kit/src/__tests__/define-system-command.spec.ts`**

- **defineSystemCommand**
- **Basic Command Creation**
  - should create a command with correct metadata
  - should default category to "system"
  - should default aliases to empty array
  - should default examples to empty array
- **Flag Schema Conversion**
  - should convert flag schema to FlagDefinition[]
  - should handle choices for string flags
- **Handler Execution**
  - should call handler with EnhancedCliContext
  - should return 0 for successful CommandResult
  - should return 1 for failed CommandResult
  - should return numeric exit code directly
  - should handle synchronous handlers
- **Flag Validation**
  - should validate required flags
  - should pass validation when required flags provided
  - should apply default values
- **Error Handling**
  - should handle handler errors
  - should log errors to platform logger
  - should output error in JSON mode
- **Analytics Integration**
  - should accept analytics config (legacy)
- **Formatter**
  - should call formatter with result
- **Tracker Integration (REMOVED)**
  - tracker is no longer added - context is pure PluginContextV3
- **Logging**
  - should log command execution
- **defineSystemCommandGroup**
  - should create a command group
  - should handle empty command array

**`shared/command-kit/src/__tests__/errors.spec.ts`**

- **formatError**
  - should format Error object
  - should format string error
  - should include stack trace when showStack is true
  - should include timing when provided
  - should not include stack when showStack is false

**`shared/command-kit/src/__tests__/flags.spec.ts`**

- **defineFlags**
  - should create flag schema with type inference
- **validateFlags**
  - should validate boolean flags
  - should validate string flags
  - should validate number flags
  - should validate array flags
  - should apply default values
  - should throw error for required flags
  - should validate choices
  - should validate pattern
  - should validate min/max for numbers
  - should coerce string to boolean
  - should coerce string to number
  - should coerce comma-separated string to array
- **validateFlagsSafe**
  - should return success result for valid flags
  - should return error result for invalid flags
- **validateFlags - advanced features**
  - should validate conflicts
  - should validate dependsOn
  - should validate implies
  - should apply custom validator
  - should apply async custom validator
  - should apply transform
  - should apply async transform
  - should validate array minLength
  - should validate array maxLength
  - should validate string minLength
  - should validate string maxLength

**`shared/command-kit/src/__tests__/plugin-context-v3.snapshot.spec.ts`**

- **PluginContextV3 Structure Snapshot**
  - should have ONLY PluginContextV3 fields (snapshot)
  - should NOT have legacy EnhancedCliContext fields
  - should have ui field with all required methods (snapshot)
  - should have platform field with all required services (snapshot)
  - should have runtime field with all required methods (snapshot)

#### Unit tests

**`shared/command-kit/src/__tests__/define-route.test.ts`**

- **defineRoute**
- **host validation**
  - should throw error if host is not rest
  - should throw error if HTTP method mismatch
  - should be case-insensitive for HTTP methods
  - should accept valid REST host context
- **handler execution**
  - should call execute with context and input
  - should return handler result
  - should handle void return from handler
- **cleanup**
  - should call cleanup after successful execution
  - should call cleanup even if handler throws
  - should not call cleanup if validation fails
  - should propagate handler error after cleanup
- **isRESTHost**
  - should return true for REST host context
  - should return false for non-REST host context

**`shared/command-kit/src/__tests__/define-webhook.test.ts`**

- **defineWebhook**
- **host validation**
  - should throw error if host is not webhook
  - should throw error if event name mismatch
  - should accept valid webhook host context
- **cleanup**
  - should call cleanup after successful execution
  - should call cleanup even if handler throws
- **isWebhookHost**
  - should return true for webhook host context
  - should return false for non-webhook host context

**`shared/command-kit/src/__tests__/define-websocket.test.ts`**

- **defineWebSocket**
- **host validation**
  - should throw error if host is not ws
  - should throw error if channel path mismatch
  - should throw error if sender not provided for connect event
  - should accept valid ws host context with sender
- **message validation**
  - should validate message structure
  - should reject message without type field
  - should reject message with empty type
- **lifecycle events**
  - should call onConnect handler
  - should call onDisconnect handler without sender
- **cleanup**
  - should call cleanup after successful execution
  - should call cleanup even if handler throws
- **isWSHost**
  - should return true for ws host context
  - should return false for non-ws host context

**`shared/command-kit/src/__tests__/platform-api.test.ts`**

- **Platform API composables**
- **usePlatform()**
  - returns an object with platform services
  - always returns the same object reference
- **useLogger()**
  - returns same constructor as usePlatform().logger
  - returned logger has all required methods
  - logger methods are callable without throwing
  - child() returns a logger with same interface
- **useCache()**
  - returns same constructor as usePlatform().cache
  - returned cache has get/set/delete interface
  - cache.get returns undefined for unknown key
  - cache.set then get returns stored value
  - cache.delete removes a key
- **isCacheAvailable()**
  - returns true when platform.cache is set
- **isPlatformConfigured()**
  - returns false for adapter named with "noop" constructor
  - returns true for logger (always present)

**`shared/command-kit/src/__tests__/ws-types.test.ts`**

- **MessageBuilder**
  - should create message with messageId
- **MessageBuilder > create**
  - should create message with type and payload
  - should create message without messageId
  - should generate unique timestamps for sequential calls
- **is**
  - should return true for matching message type
  - should return false for non-matching message type
  - should narrow type when used in if statement
- **defineMessage**
  - should create MessageBuilder instance
  - should create type-safe message builder
  - should support different payload types
- **MessageRouter**
- **on**
  - should register message handler
  - should support chaining
  - should register multiple handlers for different message types
- **handle**
  - should call handler for matching message type
  - should return false for unhandled message type
  - should pass payload to handler
  - should handle async handlers
  - should route to correct handler among multiple
  - should propagate handler errors
- **integration**
  - should handle complete message flow

### `@kb-labs/shared-http`

#### Unit tests

**`shared/http/src/__tests__/log-correlation.test.ts`**

- **createServiceLogBindings**
  - builds canonical correlation fields for request-scoped logs
  - uses explicit route when provided and resolves instanceId by default
  - merges canonical correlation fields into every log call

**`shared/http/src/__tests__/operation-metrics-tracker.test.ts`**

- **OperationMetricsTracker**
  - tracks operations and exposes top operations
  - renders canonical metric lines grouped by status
  - observes async work and records failures

### `@kb-labs/shared-testing`

#### Unit tests

**`shared/testing/src/__tests__/create-test-context.test.ts`**

- **createTestContext**
- **basic creation**
  - should create context with default values
  - should accept custom pluginId and host
  - should accept custom config
- **singleton sync**
  - should sync platform adapters to global singleton by default
  - should not sync when syncSingleton is false
  - cleanup should reset the global singleton
- **default mocks are spies**
  - should provide LLM with vi.fn() spies
  - should provide cache with vi.fn() spies
  - should provide UI with vi.fn() spies
  - should provide runtime with vi.fn() spies
  - should provide plugin API with vi.fn() spies
  - should provide environment/workspace/snapshot APIs by default
- **platform overrides**
  - should use custom LLM mock
  - should use custom cache mock
- **host contexts**
  - should generate CLI host context by default
  - should generate REST host context
  - should generate workflow host context
  - should accept custom host context

**`shared/testing/src/__tests__/mock-cache.test.ts`**

- **mockCache**
  - should return null for missing keys
- **mockCache > basic operations**
  - should store and retrieve values
  - should delete values
  - should clear all values
  - should clear by prefix pattern
- **TTL**
  - should expire entries after TTL
  - should not expire entries without TTL
- **setIfNotExists**
  - should set value if key does not exist
  - should not overwrite existing value
- **sorted sets**
  - should add and retrieve by score range
  - should remove members
  - should update score for existing member
- **initial data**
  - should accept pre-populated data
- **spy tracking**
  - all methods should be vi.fn() spies
- **internal state access**
  - should expose .store for assertions
  - should expose .sortedSets for assertions
- **reset**
  - should clear all data and spy history

**`shared/testing/src/__tests__/mock-llm.test.ts`**

- **mockLLM**
  - should track calls with vi.fn() spy
- **mockLLM > basic usage**
  - should return default response
  - should record calls in .calls array
  - should expose lastCall
- **onComplete() with string matcher**
  - should match substring and return specific response
- **onComplete() with regex matcher**
  - should match regex pattern
- **onComplete() with function matcher**
  - should use custom matcher function
- **onComplete() with function response**
  - should accept a response factory function
- **onComplete() with LLMResponse object**
  - should accept full LLMResponse
- **onAnyComplete()**
  - should set default response
- **rule priority**
  - should match first matching rule
- **streaming**
  - should yield configured chunks
- **failing**
  - should throw on complete()
  - should throw on stream()
  - should throw on chatWithTools()
- **chatWithTools**
  - should return configured tool calls
  - should record tool call history
- **resetCalls**
  - should clear all recorded calls and spies

**`shared/testing/src/__tests__/setup-platform.test.ts`**

- **setupTestPlatform**
  - should set LLM adapter in the global singleton
  - should set cache adapter in the global singleton
  - should set logger adapter in the global singleton
  - should set multiple adapters at once
  - should not set adapters that are not provided
  - cleanup should reset the platform
  - should reset previous state before setting new adapters

**`shared/testing/src/__tests__/test-command.test.ts`**

- **testCommand**
- **CLI commands**
  - should execute a simple handler with flags
  - should use default flags when none provided
  - should pass argv to handler
  - should handle non-zero exit code
  - should handle void return
- **platform mocks**
  - should inject LLM mock into context
  - should inject cache mock into context
  - should use default mocks when no platform provided
- **REST handlers**
  - should pass query and body for REST host
  - should handle REST route handler with CommandResult
  - should handle REST route handler failure
  - should pass params for REST
- **raw input**
  - should pass raw input directly when provided
  - raw input should override flags/argv
- **config**
  - should pass config to context
- **meta**
  - should return meta from CommandResult
  - should return undefined meta for raw-data handlers
- **cleanup & lifecycle**
  - should call handler.cleanup() after execute
  - should call handler.cleanup() even if execute throws
  - should propagate handler errors
- **context properties**
  - should set host on context
  - should expose ctx in result for advanced assertions
  - should pass tenantId to context
  - should pass cwd to context
- **UI spies**
  - should track all UI method calls
  - should support UI override
- **raw return value**
  - should expose raw return for CommandResult handlers
  - should expose raw return for data-returning handlers
  - should expose null raw for void handlers

### `@kb-labs/shared-testing-e2e`

#### E2E / journey tests

**`shared/testing-e2e/src/__tests__/http-client.test.ts`**

- **HttpClient**
  - GET parses JSON response
  - POST serializes JSON body and echoes back
  - leaves body undefined for non-JSON responses but populates text
  - returns ok=false for non-2xx
  - supports default headers

**`shared/testing-e2e/src/__tests__/isolated-project-root.test.ts`**

- **createIsolatedProjectRoot**
  - creates a temp dir with a valid .kb/devservices.yaml
  - cleanup() removes the temp dir
  - cleanup() is idempotent
  - copyDevConfigFrom copies an existing workspace devservices.yaml

**`shared/testing-e2e/src/__tests__/kb-dev-controller.test.ts`**

- **KbDevController smoke**
  - constructs against the real workspace root
  - throws when kbDevBin does not exist

**`shared/testing-e2e/src/__tests__/sse-reader.test.ts`**

- **readSse**
  - yields parsed events in order and terminates on untilEvent

**`shared/testing-e2e/src/__tests__/workspace-root.test.ts`**

- **workspace-root**
  - resolveWorkspaceRoot() finds the monorepo with a devservices file
  - findWorkspaceRoot() returns null for an unrelated dir

### `@kb-labs/shared-tool-kit`

#### Unit tests

**`shared/tool-kit/src/__tests__/factory.test.ts`**

- **createTool**
  - returns a factory function
  - factory returns tool with correct definition
  - tool executor calls spec.execute with input and context
  - executor returns result from spec.execute
  - each factory call creates independent tool bound to its context
  - parameters shape is preserved verbatim
  - executor propagates errors from spec.execute

**`shared/tool-kit/src/__tests__/mock-tool.test.ts`**

- **mockTool**
  - returns a tool with correct definition
  - executor records calls
  - getLastCall returns most recent call
  - getLastCall returns undefined when never called
  - wasCalled returns false before any call
  - wasCalled returns true after a call
  - callCount tracks number of calls
  - returns default response {} when no response provided
  - returns configured response
  - respondWith changes future responses
  - respondWith returns the same mock instance for chaining
  - getCalls returns readonly snapshot (not mutated by new calls)

### `@kb-labs/studio-app`

#### Unit tests

**`studio/app/src/modules/analytics/utils/formatters.test.ts`**

- **formatCost**
  - should format slightly larger costs
- **formatCost > real backend data - single LLM calls**
  - should format very small costs (individual API calls)
- **aggregated daily costs**
  - should format daily totals
  - should format typical production daily costs
- **edge cases**
  - should handle zero cost
  - should handle very tiny costs (scientific notation)
  - should handle large costs
- **precision thresholds**
  - should use 2 decimals for >= $1
  - should use 4 decimals for >= $0.01
  - should use 6 decimals for >= $0.0001
  - should use scientific notation for < $0.0001
- **what we expect to see in the chart tooltip**
  - should show readable values for typical daily aggregates
  - should NOT show $0.00 for small but non-zero values
- **real world scenario: what chart shows on Jan 16**
  - should handle sum of all LLM calls on Jan 16
  - should handle very low daily total (few requests)

### `@kb-labs/studio-data-client`

#### E2E / journey tests

**`studio/data-client/src/sources/__tests__/http-workflow-source.spec.ts`**

- **HttpWorkflowSource**
  - lists runs via plugin route with query filters
  - lists runs without filters
  - gets run via plugin route
  - returns null when run is not found
  - throws non-404 errors from getRun
  - cancels run via plugin route and fetches updated run
  - returns fallback when cancelled run is not found
  - runs workflow via plugin route
  - gets stats via plugin route
  - lists workflows via plugin route
  - returns null when workflow not found
  - runs workflow by ID via plugin route
  - lists jobs with filters
  - returns null when job not found
  - lists cron jobs via plugin route
  - gets pending approvals via plugin route
  - resolves approval via plugin route
  - cancels workflow run via plugin route
  - gets workflow runs history with filters

**`studio/data-client/src/sources/__tests__/prometheus-metrics-parser.spec.ts`**

- **parsePrometheusMetrics**
  - builds studio metrics from canonical Prometheus text

### `@kb-labs/studio-event-bus`

#### Unit tests

**`studio/event-bus/src/__tests__/event-bus.test.ts`**

- **StudioEventBus**
  - should deliver events to subscribers
  - should not deliver events to unsubscribed handlers
  - should unsubscribe all handlers for a page
  - should record event history
  - should limit history to 100 entries
  - should not crash if handler throws
  - should reset all state
  - should support multiple subscribers for the same event
  - should not fire handlers for different event names

### `@kb-labs/studio-federation`

#### Unit tests

**`studio/federation/src/__tests__/page-error-boundary.test.ts`**

- **PageErrorBoundary**
  - captures different error types
- **PageErrorBoundary > getDerivedStateFromError**
  - sets hasError=true and captures the error
- **handleRetry**
  - resets error state
- **componentDidCatch**
  - calls onError callback when provided
  - logs error to console

**`studio/federation/src/__tests__/widget-loader-cache.test.ts`**

- **initFederation**
  - calls init() on first invocation with all remotes
  - does not call init() on subsequent invocations
  - does not call registerRemotes on first init
- **syncRemoteEntry**
  - does nothing when URL is unchanged
  - calls registerRemotes({ force: true }) when URL changes
  - updates the tracked URL after sync so subsequent calls are no-ops
  - registers a previously unknown remote without force semantics issue
  - handles multiple remotes independently
- **loadPageComponent**
  - calls syncRemoteEntry before loadRemote when remoteEntryUrl is provided
  - does not call registerRemotes when URL is unchanged
  - skips syncRemoteEntry when remoteEntryUrl is not provided
  - strips leading ./ from exposed module in the loadRemote path
  - returns the loaded module
  - retries on failure and eventually throws PageLoadError
- **resetFederation**
  - allows initFederation to run init() again after reset
  - clears known remotes so syncRemoteEntry treats all URLs as new

**`studio/federation/src/__tests__/widget-loader-errors.test.ts`**

- **loadPageComponent — error scenarios**
  - throws PageLoadError after all retries exhausted
  - PageLoadError includes cause chain
  - throws when module resolves to null
  - succeeds on retry after initial failure
  - handles module without default export (warning path)
  - strips ./ prefix from exposed module path
  - handles exposed module without ./ prefix

**`studio/federation/src/__tests__/widget-loader.test.ts`**

- **PageLoadError**
  - stores remote name and exposed module
  - stores cause error
  - is instanceof Error

### `@kb-labs/telemetry-client`

#### CLI handler tests

**`core/telemetry-client/src/__tests__/client.test.ts`**

- **KBTelemetry**
  - buffers events without sending until flush()
  - flush() sends buffered events to ingestion endpoint
  - flush() is no-op when buffer is empty
  - metric() creates event with type "metric"
  - log() creates event with type "log"
  - auto-flushes when buffer reaches batchSize
  - merges defaultTags into all events
  - per-event tags override defaultTags
  - retries on server error (up to maxRetries)
  - shutdown() flushes remaining events
  - includes ISO timestamp on each event
  - strips trailing slash from endpoint

### `@kb-labs/workflow-contracts`

#### Unit tests

**`plugins/workflow/contracts/src/__tests__/constants.test.ts`**

- **STEP_STATES**
  - includes waiting_approval
  - includes all RUN_STATES values
  - does not contain waiting_approval in RUN_STATES
  - does not contain waiting_approval in JOB_STATES
  - StepState type accepts waiting_approval as a value at runtime
  - contains all expected states

**`plugins/workflow/contracts/src/__tests__/expressions.test.ts`**

- **Expression Evaluation**
  - should evaluate boolean literals
  - should evaluate equality
  - should evaluate inequality
  - should evaluate contains function
  - should evaluate startsWith function
  - should evaluate endsWith function
  - should handle parentheses
  - should interpolate strings
  - should interpolate multiple expressions
  - should handle step outputs
  - should handle empty expressions
- **resolveValue**
  - resolves env variables
  - resolves trigger.type and trigger.actor
  - resolves trigger.payload fields
  - resolves step outputs
  - returns empty string for unknown step
  - resolves matrix values
  - parses numeric literals
  - parses boolean literals
  - strips surrounding quotes from string literals
  - returns the raw path string for unrecognised paths
- **resolveExpression**
  - returns raw value when the whole string is a single expression (boolean)
  - returns raw value when the whole string is a single expression (number)
  - returns raw value when the whole string is a single expression (object)
  - returns a string when there is surrounding text mixed with an expression
  - returns a string when multiple expressions are present
  - returns the original string when there are no expressions
  - handles whitespace inside expression delimiters
- **interpolateObject**
  - interpolates top-level string values
  - preserves type of single-expression values (boolean)
  - preserves type of single-expression values (number)
  - passes through non-string primitives unchanged
  - recursively interpolates nested objects
  - recursively interpolates elements inside arrays
  - handles arrays of objects
  - returns an empty object unchanged
- **|| (logical OR / default value) operator**
  - resolveExpression: returns value when present
  - resolveExpression: returns default when value is empty string
  - resolveExpression: returns default when value is missing
  - resolveExpression: chains multiple fallbacks
  - resolveExpression: preserves non-string types
  - resolveExpression: returns numeric default
  - interpolateString: works in embedded expressions
  - interpolateString: uses value when present
  - interpolateObject: works with || in object values
  - evaluateExpression: || still works for boolean evaluation
  - handles || inside quotes (no split)

**`plugins/workflow/contracts/src/__tests__/schemas.test.ts`**

- **StepSpecSchema**
- **StepSpecSchema > run: shorthand**
  - converts run: to builtin:shell
  - merges existing with: fields when converting run:
  - does not override existing uses: when run: is present
  - passes through uses: builtin:shell as-is
- **uses: plugin: syntax**
  - accepts non-scoped plugin reference
  - accepts scoped plugin reference (@scope/name/handler)
  - accepts scoped plugin with nested handler path
- **builtins**
  - accepts builtin:approval
  - accepts builtin:gate
- **validation**
  - requires name
  - accepts step with neither uses nor run (noop)
- **WorkflowSpecSchema — run: in jobs**
  - parses workflow with run: steps

### `@kb-labs/workflow-daemon`

#### Integration tests

**`plugins/workflow/daemon/src/api/__tests__/api-contract.integration.test.ts`**

- **Workflow API Contract Integration**
- **Jobs**
  - GET /api/v1/jobs — lists jobs with envelope
  - GET /api/v1/jobs/:jobId — returns job
  - GET /api/v1/jobs/:jobId — 404 when not found
  - POST /api/v1/jobs/:jobId/cancel — cancels job
  - GET /api/v1/jobs/:jobId/steps — returns steps
  - GET /api/v1/jobs/:jobId/logs — returns logs
  - GET /api/v1/jobs/:jobId/steps — 404 when job not found
- **Crons**
  - GET /api/v1/crons — lists crons
  - POST /api/v1/crons — registers cron
  - DELETE /api/v1/crons/:id — unregisters cron
  - POST /api/v1/crons/:id/trigger — triggers cron
  - POST /api/v1/crons/:id/pause — pauses cron
  - POST /api/v1/crons/:id/resume — resumes cron
  - old /api/v1/cron path returns 404
- **Workflows**
  - GET /api/v1/workflows — lists workflows
  - GET /api/v1/workflows/:id — returns definition
  - GET /api/v1/workflows/:id — 404 when not found
  - GET /api/v1/workflows/:id/runs — lists runs for workflow
  - POST /api/v1/workflows/:id/runs — starts a run
  - POST /api/v1/workflows/:id/runs — 404 when workflow not found
  - old POST /api/v1/workflows/:id/run path returns 404
  - POST /api/v1/workflows/refresh — refreshes definitions
- **Runs**
  - GET /api/v1/runs — lists all runs
  - GET /api/v1/runs/:runId — returns run
  - GET /api/v1/runs/:runId — 404 when not found
  - POST /api/v1/runs/:runId/cancel — cancels run
  - POST /api/v1/runs/:runId/cancel — 404 when not found
  - POST /api/v1/runs/:runId/cancel — 409 when already terminal
  - old /workflows/runs/:runId/cancel path returns 404
  - GET /api/v1/runs/:runId/logs — returns run logs with envelope
  - GET /api/v1/runs/:runId/logs?stepId= — passes stepId to service
  - GET /api/v1/runs/:runId/logs — 500 on service error
- **Approvals**
  - GET /api/v1/runs/:runId/approvals — lists pending approvals
  - GET /api/v1/runs/:runId/approvals — 404 when run not found
  - GET /api/v1/runs/:runId/approvals — empty list when no pending steps
  - POST /api/v1/runs/:runId/approvals/resolve — approves step
  - POST /api/v1/runs/:runId/approvals/resolve — rejects step
  - POST /api/v1/runs/:runId/approvals/resolve — 400 on missing fields
  - POST /api/v1/runs/:runId/approvals/resolve — 400 on invalid action
  - POST /api/v1/runs/:runId/approvals/resolve — 404 when run not found
  - POST /api/v1/runs/:runId/approvals/resolve — 409 when step not waiting
  - old /runs/:runId/pending-approvals path returns 404
  - old /runs/:runId/approve path returns 404

#### E2E / journey tests

**`plugins/workflow/daemon/src/__tests__/worker-lifecycle.e2e.test.ts`**

- **workflow worker lifecycle**
  - processes a job with steps and delegates execution to runner
  - passes target hint from workflow spec to runner
  - worker does not call any adapters directly
  - emits structured diagnostic log when workspace provisioning fails
  - debugMode emits verbose [debug] logs for expr context and interpolation

#### Unit tests

**`plugins/workflow/daemon/src/__tests__/observability.test.ts`**

- **workflow observability surfaces**
  - exposes versioned describe and structured health
  - surfaces bounded workflow API operations in observability health
  - renders canonical metrics as prometheus text

**`plugins/workflow/daemon/src/host/workflow-host-service.test.ts`**

- **WorkflowHostService**
  - submits job and returns jobId
  - rejects invalid tenant id
  - maps run to job status response
  - getRunLogs delegates to jobBroker.getRunLogs with stepId filter
  - getRunLogs works without options (returns all logs for run)
  - runWorkflow resolves inputs from `inputs` field (plural)
  - runWorkflow falls back to legacy `input` field (singular)
  - runWorkflow uses spec defaults when no user inputs supplied
  - passes run target/isolation overrides to engine

### `@kb-labs/workflow-engine`

#### Unit tests

**`plugins/workflow/engine/src/__tests__/concurrency-manager.test.ts`**

- **ConcurrencyManager**
  - acquires lock for empty group
  - blocks second acquire on same group
  - allows acquire after release
  - release only works for the holding runId
  - getActiveRun returns the holding runId
  - getActiveRun returns null when no lock
  - different groups are independent
  - release + re-acquire cycle works

**`plugins/workflow/engine/src/__tests__/engine.test.ts`**

- **WorkflowEngine**
- **Initialization**
  - should create engine with required adapters
  - should use default maxWorkflowDepth if not provided
- **Run Creation**
  - should store run in state store
- **Run from File**
  - should load and create run from YAML file
- **Get Run**
  - should get run by ID
  - should return null for non-existent run
- **Cancel Run**
  - should cancel run and update status
- **Job Failure and Retries**
  - should handle non-existent run gracefully
  - should handle non-existent job gracefully
- **Job Interruption**
  - should log job interruption
- **Dispose**
  - should cleanup resources on dispose

**`plugins/workflow/engine/src/__tests__/retry.test.ts`**

- **calculateBackoff**
  - returns 0 when no policy
  - calculates exponential backoff (default)
  - calculates linear backoff
  - caps at maxIntervalMs
  - uses default 1000ms when initialIntervalMs not set
- **shouldRetry**
  - returns false when no policy
  - returns true when under max retries
  - returns false when max retries reached
  - returns false for max=0 policy (no retries at all)

**`plugins/workflow/engine/src/__tests__/scheduler.test.ts`**

- **Scheduler**
  - enqueues a job to the correct priority queue
  - skips blocked jobs
  - schedules only ready (non-blocked) jobs
  - schedules multiple ready jobs in parallel
  - dequeues high priority jobs first
  - returns null when queue is empty
  - removes dequeued entry from queue
  - reschedules with delay
  - returns configured default priority

**`plugins/workflow/engine/src/__tests__/state-store.test.ts`**

- **StateStore**
  - saves and retrieves a workflow run
  - returns null for non-existent run
  - deletes a run
  - returns all run IDs in order
  - updates run status atomically
  - updateRun returns null for non-existent run
  - updates a specific job within a run
  - updateJob returns null for non-existent job
  - updates a specific step within a job
  - updateStep returns null for non-existent step
  - releases blocked jobs when dependency completes
  - does not release jobs with remaining dependencies

### `@kb-labs/workflow-entry`

#### CLI handler tests

**`plugins/workflow/entry/src/__tests__/cli/health.cli.test.ts`**

- **workflow:health**
  - CH-01: prints success message when daemon is healthy
  - CH-02: --json returns { ok: true, data: { ok, service } }
  - CH-03: daemon unavailable — prints error, returns exitCode 1

**`plugins/workflow/entry/src/__tests__/cli/runs-list.cli.test.ts`**

- **workflow:runs-list**
  - CL-01: renders table when runs exist
  - CL-02: --json returns array of WorkflowRunSummary
  - CL-03: --status running filters by status
  - CL-04: --limit 5 passes limit to client
  - CL-05: --workflow filters by workflowId
  - CL-06: empty list renders empty state, does not throw
  - CL-07: daemon unavailable — error message, exitCode 1

**`plugins/workflow/entry/src/__tests__/cli/runs-watch.cli.test.ts`**

- **workflow:runs-watch**
  - CW-01: missing runId — error message, exitCode 1
  - CW-02: streams events until run.finished, exits with 0
  - CW-03: run.failed terminal event exits with 1
  - CW-04: --json flag emits JSON objects per event
  - CW-05: daemon connection refused — error message, exitCode 1
  - CW-06: log.appended events are forwarded to ctx.ui.log()

**`plugins/workflow/entry/src/__tests__/cli/workflow-run.cli.test.ts`**

- **workflow:run**
  - CR-01: submits workflow and returns runId
  - CR-02: --json returns { ok: true, data: { runId, status } }
  - CR-03: --input passes JSON payload to runWorkflow
  - CR-04: missing --workflow-id returns exitCode 1 with error
  - CR-05: daemon unavailable — readable error, exitCode 1
  - CR-06: invalid --isolation value returns exitCode 1

### `@kb-labs/workflow-runtime`

#### Unit tests

**`plugins/workflow/runtime/src/registry/__tests__/plugin-workflows.test.ts`**

- **extractWorkflows — plugin templates**
  - returns empty array for empty snapshot
  - returns empty array for plugin with no workflows field
  - returns empty array for plugin with empty handlers and no templates
  - extracts templates from plugin manifest
  - extracts multiple templates from one plugin
  - extracts both handlers and templates from same plugin
  - aggregates templates from multiple plugins
  - template without tags has undefined tags
- **findWorkflow — plugin templates**
  - finds template by exact plugin: ID
  - finds template by short ID (without plugin: prefix)
  - returns null for unknown ID
  - returns null for empty snapshot

**`plugins/workflow/runtime/src/registry/__tests__/remote-registry.test.ts`**

- **RemoteWorkflowRegistry — shell injection safety (H8)**
  - passes git clone args as array, not interpolated shell string
  - ref with shell metacharacters is passed as a literal argument
  - url with shell metacharacters is passed as a literal argument
  - uses "main" as default ref when not specified
  - passes update commands as arg arrays when repo already cloned

**`plugins/workflow/runtime/src/registry/__tests__/workspace-registry.test.ts`**

- **WorkspaceWorkflowRegistry**
  - should discover workflows from workspace
  - should generate correct IDs from file paths
  - should resolve workflow by ID
  - should cache workflow list
  - mtime staleness: fast path serves cache when files unchanged
  - mtime staleness: auto-detects new file without manual refresh
  - mtime staleness: auto-detects file modification
  - should refresh cache

**`plugins/workflow/runtime/src/runners/__tests__/output-normalizer.test.ts`**

- **toWorkflowOutputs**
  - passes through nested objects
- **toWorkflowOutputs > object handler output (workflow handlers)**
  - passes through plain objects as-is
  - passes through arrays as objects
- **CommandResult handler output (CLI commands via command: steps)**
  - extracts .result from CommandResult with object payload
  - extracts .result from CommandResult with meta
  - wraps primitive .result in { result }
  - wraps numeric .result in { result }
  - returns {} when CommandResult.result is undefined (key present)
  - returns {} when CommandResult.result is explicitly undefined
  - handles non-zero exitCode (still extracts result)
  - handles CommandResult with null .result
- **primitive handler output**
  - wraps string in { result }
  - wraps number in { result }
  - wraps boolean in { result }
  - wraps false in { result }
  - wraps zero in { result }
  - wraps empty string in { result }
- **void / null / undefined handler output**
  - returns {} for undefined
  - returns {} for null
- **consistency guarantee: in-process vs subprocess produce same outputs**
  - workflow handler returning object → same outputs in both modes
  - CLI handler returning CommandResult → same outputs in both modes
- **edge cases**
  - does not treat object with exitCode but no numeric value as CommandResult
  - passes through object with only exitCode (no result key) as plain object
  - passes through shell handler output as-is (has exitCode but no result key)
  - passes through Date objects

**`plugins/workflow/runtime/src/runners/__tests__/sandbox-runner-resolution.test.ts`**

- **SandboxRunner — plugin reference parsing**
  - parses plugin:name/path/to/handler
- **SandboxRunner — plugin reference parsing > non-scoped packages**
  - parses plugin:name/handler
- **scoped packages (@scope/name)**
  - parses plugin:@scope/name/handler
  - parses plugin:@scope/name/cli/command
  - does NOT split @scope as pluginId (regression for original bug)

### `@kb-labs/workflow-steps`

#### Unit tests

**`plugins/workflow/steps/src/__tests__/approval.test.ts`**

- **ApprovalInput type shape**
  - accepts a minimal ApprovalInput with only the required title
  - accepts a fully specified ApprovalInput
  - accepts arbitrary values in context
  - has correct field types
- **ApprovalOutput type shape**
  - represents an approved outcome
  - represents a rejected outcome
  - accepts extra data from the approver via index signature
  - has correct types for required fields

**`plugins/workflow/steps/src/__tests__/gate.test.ts`**

- **GateInput type shape**
  - accepts a minimal GateInput with required fields only
  - accepts a fully specified GateInput
  - accepts a restartFrom route action with optional context
  - has correct field types
- **GateOutput type shape**
  - represents a continue outcome
  - represents a fail outcome
  - represents a restart outcome with restartFrom
  - accepts any value type for decisionValue
  - has correct field types
- **GateRouteAction type variants**
  - accepts string literal "continue"
  - accepts string literal "fail"
  - accepts an object with restartFrom
  - accepts an object with restartFrom and context

**`plugins/workflow/steps/src/__tests__/shell.test.ts`**

- **mergeJsonOutputs — ::kb-output:: marker**
  - extracts outputs from ::kb-output:: marker line
  - works with marker as the only line
  - extracts from marker even with pnpm noise before it
  - merges multiple marker lines
  - later marker overrides earlier for same key
  - skips malformed marker JSON without crashing
  - ignores marker with array JSON
  - preserves base ShellOutput fields
  - marker takes priority over fallback JSON parse
- **mergeJsonOutputs — fallback (backward compat)**
  - merges a flat JSON object from stdout into the output record
  - preserves the base ShellOutput fields even when merging
  - ignores invalid JSON and returns the base output unchanged
  - ignores partial / malformed JSON and returns the base output unchanged
  - does NOT merge JSON arrays — only plain objects are merged
  - does NOT merge primitive JSON values (string, number, boolean)
  - returns base output unchanged when stdout is empty
  - returns base output unchanged when stdout is only whitespace
  - merges nested objects from JSON stdout
  - handles stdout with leading/trailing whitespace around valid JSON
  - works correctly when command fails (exitCode != 0)
- **ShellInput type shape**
  - accepts a minimal input with only command
  - accepts a fully specified input
- **ShellOutput type shape**
  - has the expected fields
